# Architecture Decision Records

Each record follows: Status, Context/Problem, Alternatives considered, Decision, Rationale,
Consequences, Rejected alternatives (with the decisive reason), and Revisit triggers. Records are
immutable once Accepted; a change is a new record that supersedes the old one.

## ADR number ranges, assigned per branch

**The minting rate outran the check.** "Re-verify the next free number immediately before landing"
is sound advice that stopped working on 2026-09-20, when concurrent branches were producing an ADR
every six to ten minutes -- shorter than the interval between checking and committing. Three
branches minted **ADR-394** independently; one agent renumbered to 395, found it taken, moved to
400, and found that taken too, within six minutes. Thirteen collisions in two days, none of them
carelessness. The protocol was the defect, not the agents.

So numbers are **assigned by range** rather than claimed by checking:

| range | holder |
|---|---|
| ..394 | landed, or held by a branch that is merging |
| 395-409 | `agent/defects` |
| 410-419 | `agent/temporal` |
| 420-439 | `agent/wfx2` -- World Effects phases and the deformation half |
| 440-449 | editor state, removals, and the policy on compatibility |
| 450-459 | `agent/ocean2` -- the Cosmic Ocean |
| 460-479 | `agent/vortex2` -- the Vortex 2.0 rebuild |
| 480-499 | `agent/perf` -- the editor's own resolution |
| 500-519 | `agent/registry` -- the world-effect registry |
| 520-539 | `agent/weather` -- weather and natural particle effects |
| 540-559 | `agent/anim-research` -- autonomous character animation |
| 560-579 | `agent/fog` -- the volumetric foundation and the Fog Bank rebuild |
| 580-599 | `agent/tornado` -- the Tornado world effect, replacing the Vortex |
| 600-649 | `agent/anim-research` -- phases C and D of the motion stack (540-559 exhausted) |
| 650-669 | `agent/anim-cinfra` -- Phase C infrastructure and tooling |
| 670-699 | `agent/anim-phase-d` -- Phase D |
| 700+ | unassigned; ask before taking |

Take the next free number **inside your range** and do not look outside it. A range with gaps is the
cheap failure: this index already skips 301-309, 311-319 and 321-329, and a non-contiguous index
costs a reader nothing, whereas two ADRs sharing a number costs another agent a rewrite of every
citation.

**When you must renumber, match the FILENAME, never the bare number.** A blanket `sed` over a bare
`ADR-382` rewrote seven other branches' files. Before rewriting, diff each file against `main` to
confirm the citation you are about to change is your own.

| ADR | Title | Status |
|---|---|---|
| [001](ADR-001-rendering-backend.md) | Rendering backend: WebGPU via Dawn behind a thin `gpu` module | Accepted |
| [002](ADR-002-windowing.md) | Windowing and input: SDL3 | Accepted |
| [003](ADR-003-audio-engine.md) | Audio decoding and playback: miniaudio | Accepted |
| [004](ADR-004-audio-analysis.md) | Audio analysis: in-house STFT pipeline over a pluggable FFT (KissFFT first) | Accepted |
| [005](ADR-005-asset-format.md) | Asset format: glTF 2.0 canonical; procedural-only in 0.1 | Accepted |
| [006](ADR-006-shader-system.md) | Shader system: WGSL files at runtime now; ISF-style user shaders later | Accepted |
| [007](ADR-007-ui-framework.md) | UI framework: Dear ImGui (docking) + ImPlot | Accepted |
| [008](ADR-008-dependency-management.md) | Dependency management: CPM.cmake with pinned versions | Accepted |
| [009](ADR-009-testing-framework.md) | Testing: Catch2 v3 + CTest; GPU-free by default | Accepted |
| [010](ADR-010-language-and-core-libraries.md) | C++23 allow-list; spdlog/fmt, GLM, nlohmann/json | Accepted |
| [011](ADR-011-parameters-and-modulation.md) | Parameter and modulation model | Accepted |
| [012](ADR-012-time-model.md) | Time model: injected RenderTime, sample-indexed analysis | Accepted |
| [013](ADR-013-image-based-lighting.md) | Image-based lighting: runtime split-sum preprocessing on the GPU | Accepted |
| [014](ADR-014-user-shader-contract.md) | User shader contract: ISF-style header + WGSL body, background/post layers, hot reload | Accepted |
| [015](ADR-015-gpu-particles.md) | GPU particles: compute pool with dead/alive lists, indirect draw, curl noise | Accepted |
| [016](ADR-016-post-processing.md) | Post-processing chain (bloom, grading, lens, DoF, motion blur, tone operators) over a transient pool | Accepted |
| [017](ADR-017-scene-composition.md) | Scene composition: flattened node compositions, nested scene files, asset registry | Accepted |
| [018](ADR-018-timeline.md) | Timeline: keyframe automation writes finals before routes; cues recall presets | Accepted |
| [019](ADR-019-project-system.md) | Project system: relative asset references, explicit migration, bundles, recent files | Accepted |
| [020](ADR-020-offline-rendering.md) | Offline rendering: render jobs, PNG sequences, native/ffmpeg video, queue, determinism | Accepted |
| [021](ADR-021-live-control.md) | Live control: OSC and MIDI as control signals and direct parameter control, live audio input | Accepted |
| [022](ADR-022-outputs-and-sharing.md) | Outputs and sharing: final texture, per-output mapping windows, Syphon (IOSurface) and runtime NDI | Accepted |
| [023](ADR-023-procedural-geometry.md) | Procedural geometry: sources, distributions, seeded variation, GPU-instanced draw, deformer stack as data | Accepted |
| [024](ADR-024-spatial-data-and-attributes.md) | Spatial data: typed point attributes as the procedural currency | Accepted |
| [025](ADR-025-fields-and-effectors.md) | Fields as spatial control signals, effectors, GPU-identical evaluation | Accepted |
| [026](ADR-026-splines.md) | Splines as first-class spatial data | Accepted |
| [027](ADR-027-sdf-architecture.md) | SDF: data tree, GPU sphere tracing, CPU surface nets | Accepted |
| [028](ADR-028-procedural-graph-and-invalidation.md) | Procedural graph as an authoring layer; structural-hash invalidation | Accepted |
| [029](ADR-029-gpu-procedural-execution.md) | GPU point processing, culling, LOD, hierarchical generation | Accepted |
| [030](ADR-030-procedural-materials.md) | Procedural materials as an interpreted op program; colour utilities | Accepted |
| [031](ADR-031-states-macros-authoring.md) | Scene states, world macros, layered authoring, inspection, debug drawing | Accepted |
| [032](ADR-032-simulation-and-volumes.md) | Simulated fields and volumetric atmosphere | Accepted |
| [033](ADR-033-lighting-architecture.md) | Clustered forward lighting, area lights, colour temperature, light rigs | Accepted |
| [034](ADR-034-shadows-and-occlusion.md) | Cascaded shadows, contact shadows, ground-truth ambient occlusion | Accepted |
| [035](ADR-035-render-passes.md) | Auxiliary render targets and a declared frame graph | Accepted |
| [036](ADR-036-layered-materials.md) | Layered materials, geometric inputs, triplanar mapping, decals | Accepted |
| [037](ADR-037-cinematic-camera.md) | Physical camera, exposure and camera behaviours | Accepted |
| [038](ADR-038-composition.md) | Procedural composition, visual hierarchy and negative space | Accepted |
| [039](ADR-039-image-formation.md) | Image formation, selective post and tone mapping | Accepted |
| [040](ADR-040-particles-and-motion.md) | Particle trails, velocity-aware rendering and motion blur | Accepted |
| [041](ADR-041-world-director.md) | World Director, look presets and musical phrasing | Accepted |
| [042](ADR-042-bevels.md) | Bevels as a first-class operation on generated primitives | Accepted |
| [043](ADR-043-organic-curves.md) | Tubes: one swept curve stands in for every organ | Accepted |
| [044](ADR-044-imported-meshes-as-sources.md) | Imported meshes as instanced sources | Accepted |
| [045](ADR-045-vertex-clustering-decimation.md) | Vertex-clustering decimation for imported meshes | Accepted |
| [046](ADR-046-the-world-map-and-terrain.md) | The world map and terrain: geography as data, chunked ground, water | Accepted |
| [047](ADR-047-biomes.md) | Biomes: soft-edged rules over the map, blended as weights along one ordered axis | Accepted |
| [048](ADR-048-ecology.md) | Ecology: what grows where, decided by the biome weights the ground is coloured with | Accepted |
| [049](ADR-049-hdri-sky.md) | The HDRI sky: the map as the background, sky and lighting intensity split, the key light aimed from the image | Accepted |
| [050](ADR-050-material-interpreter-cost.md) | What the material interpreter was actually spending: the register file and the field evaluator in the loop | Accepted |
| [051](ADR-051-measuring-a-frame.md) | Measuring a frame, and one shared indirect buffer | Accepted |
| [052](ADR-052-highlight-chroma.md) | Holding hue in compressed highlights: chroma retention after the tone curve | Accepted |
| [053](ADR-053-ecology-light-field.md) | The light a glowing ecology casts: aggregate first, then light | Accepted |
| [054](ADR-054-colour-that-clusters.md) | Colour that clusters in space, and glow that is rare | Accepted |
| [055](ADR-055-the-wind-field.md) | A wind field, and vegetation that answers it for nothing | Accepted |
| [056](ADR-056-simulated-plants.md) | Plants that are actually simulated, for the few hundred worth simulating | Accepted |
| [057](ADR-057-the-living-chromatic-field.md) | The living chromatic field: hue that drifts in world space and time | Accepted |
| [058](ADR-058-the-air-and-the-ambient.md) | The air and the ambient | Accepted |
| [059](ADR-059-edges-and-a-block-nobody-read.md) | Edges, and a block nobody read | Accepted |
| [060](ADR-060-semantic-assets-and-world-recipes.md) | Semantic assets and world recipes | Accepted |
| [061](ADR-061-the-world-composer.md) | The world composer | Accepted |
| [062](ADR-062-the-cinematic-director.md) | The cinematic director | Accepted |
| [063](ADR-063-musical-events.md) | Musical events | Accepted |
| [064](ADR-064-the-job-system.md) | The job system | Accepted |
| [065](ADR-065-optional-ai.md) | AI is optional, replaceable, and never in the frame loop | Accepted |
| [066](ADR-066-generate-world.md) | Generate World | Accepted |
| [067](ADR-067-world-art-direction.md) | A generated world's art direction, and the negative space that was never applied | Accepted |
| [068](ADR-068-viewport-interaction.md) | Mouse control and picking in the viewport | Accepted |
| [069](ADR-069-placement.md) | Placing assets by hand | Accepted |
| [070](ADR-070-art-direction-profiles.md) | Art-direction profiles | Accepted |
| [071](ADR-071-cinematic-camera.md) | A camera vocabulary, and a camera cut to the music | Accepted |
| [072](ADR-072-heroes.md) | Heroes | Accepted |
| [073](ADR-073-musical-signals.md) | Musical signals | Accepted |
| [074](ADR-074-authored-heroes.md) | Heroes a scene can declare | Accepted |
| [075](ADR-075-camera-director-connected.md) | The camera director, connected | Accepted |
| [076](ADR-076-editor-shell.md) | The editor shell | Accepted |
| [077](ADR-077-frame-profiler.md) | The frame profiler | Accepted |
| [078](ADR-078-mesh-lod.md) | Mesh LODs from meshoptimizer, and what the existing decimator is still for | Accepted |
| [079](ADR-079-tilt-shift.md) | Tilt-shift | Accepted |
| [080](ADR-080-camera-clearance.md) | Camera clearance | Accepted |
| [081](ADR-081-shadow-stability.md) | Shadow stability | Accepted |
| [082](ADR-082-lod-stability.md) | LOD stability | Accepted |
| [083](ADR-083-composition-and-layers.md) | Composition and layers | Accepted |
| [084](ADR-084-the-editors-frame.md) | The editor's frame: where the swapchain wait stands, and what a drag may cost | Accepted |
| [085](ADR-085-lod-chain-wired.md) | The LOD ladder is built by meshoptimizer, and it has to descend | Accepted |
| [086](ADR-086-skeletal-animation.md) | Skeletal animation: skins, clips, blending and GPU skinning | Accepted |
| [087](ADR-087-shadow-mask.md) | A half-resolution screen-space shadow mask | Accepted |
| [088](ADR-088-entities.md) | Entities and behaviour: driving nodes from signals | Accepted |
| [089](ADR-089-the-cinematic-sequence.md) | The cinematic sequence: choreography through time | Accepted |
| [090](ADR-090-terrain-generation-and-queries.md) | Terrain with geography in it, and one place to ask where the ground is | Accepted |
| [091](ADR-091-simulation-authority.md) | Two-tier simulation authority: what bakes, what lives, and what a scrub may do | Accepted |
| [092](ADR-092-the-world-editor.md) | The world editor: a ghost under the cursor, a record of every edit | Accepted |
| [093](ADR-093-world-navigation.md) | World navigation: a hybrid graph, per-instance obstacles, and a character with somewhere to go | Accepted |
| [094](ADR-094-ai-control-plane.md) | The AI control plane: a tool API the engine owns, and one client of it | Accepted |
| [095](ADR-095-the-help-system.md) | The Help system, staged around "Help should never lie" | Accepted |
| [096](ADR-096-actions-and-intent.md) | Actions, schedules and interactions: the intent layer | Accepted |
| [097](ADR-097-spatial-reactivity.md) | A field scales the reactions an entity already has | Accepted |
| [098](ADR-098-cinematic-events.md) | Cinematic events: three tiers, and where the bake stops | Accepted |
| [099](ADR-099-water.md) | Water occupies a channel | Accepted |
| [100](ADR-100-city-layout.md) | A city is a plan, and the plan is what places the buildings | Accepted |
| [101](ADR-101-edit-system.md) | Editing is an application concern, and the history belongs to the application | Accepted |
| [102](ADR-102-the-transport.md) | The transport owns the timeline position; audio follows it | Accepted |
| [103](ADR-103-audio-arrangement.md) | Several audio files are one arrangement, mixed down to one buffer | Accepted |
| [104](ADR-104-designating-heroes.md) | A hero is declared by starring the object that is one | Accepted |
| [105](ADR-105-the-directed-camera-follows-its-heroes.md) | A directed camera follows its heroes until somebody else takes it | Accepted |
| [106](ADR-106-a-hero-follows-its-object.md) | A hero follows the object it describes | Accepted |
| [107](ADR-107-a-hero-is-one-object.md) | A hero is one object | Accepted |
| [108](ADR-108-one-spatial-instance.md) | One spatial instance, drawn once per material | Accepted |
| [109](ADR-109-a-scene-has-an-identity.md) | A scene has an identity, not just an address | Accepted |
| [110](ADR-110-lod0-through-meshoptimizer.md) | LOD0 through meshoptimizer, like every rung below it | Accepted |
| [111](ADR-111-shadow-terms-use-the-geometric-normal.md) | Shadow terms use the geometric normal, not the shading normal | Accepted |
| [112](ADR-112-the-shadowed-range-is-sized-to-the-texel.md) | The shadowed range is sized to the texel, not to the world | Accepted |
| [113](ADR-113-a-measurement-carries-its-conditions.md) | A measurement carries its conditions, and a comparison lives in one process | Accepted |
| [114](ADR-114-cluster-occupancy-is-measured-uncapped.md) | Cluster occupancy is measured before the cap, not after it | Accepted |
| [115](ADR-115-overdraw-is-counted-in-its-own-pass.md) | Overdraw is counted in its own pass, or not at all | Accepted |
| [116](ADR-116-sigpipe-is-ignored-process-wide.md) | SIGPIPE is ignored process-wide, not blocked per thread | Accepted |
| [117](ADR-117-an-ab-arm-may-be-a-quality-setting.md) | An A/B arm may be a quality setting, not only a missing pass | Accepted |
| [118](ADR-118-the-contact-march-is-memory-bound.md) | The contact march is the frame's largest fragment item, and it is memory-bound | Accepted |
| [119](ADR-119-attachment-bandwidth-is-not-the-constraint.md) | Attachment load/store bandwidth is not this frame's constraint | Accepted |
| [120](ADR-120-what-the-shortened-shadow-range-bought.md) | What the shortened shadow range bought, settled | Accepted |
| [121](ADR-121-framebuffer-fetch-is-not-available.md) | Framebuffer fetch is not available here, and the reason is the driver | Accepted |
| [122](ADR-122-importance-is-pixels-per-triangle.md) | A drawable's importance is measured in pixels per triangle | Accepted |
| [123](ADR-123-representation-is-two-decisions.md) | Representation is two decisions, not one ladder | Accepted |
| [124](ADR-124-the-band-is-a-band.md) | The target is a band of a few hundred pixels per triangle, not the fewest triangles | Accepted |
| [125](ADR-125-hysteresis-is-opt-in-and-never-offline.md) | Hysteresis is opt-in, and offline never gets it | Accepted |
| [126](ADR-126-glowmeres-quad-overdraw-share.md) | Glowmere's quad-overdraw share, and the instrument that had to be built to find it | Accepted |
| [128](ADR-128-the-object-cap-is-an-allocation-policy.md) | The object cap is an allocation policy, not a constant | Accepted |
| [129](ADR-129-object-data-stays-a-uniform-buffer.md) | Object data stays a uniform buffer until multi-draw indirect exists | Accepted |
| [130](ADR-130-the-object-ceiling-is-a-budget-below-the-naming-limit.md) | What remains of the object ceiling is a byte budget, kept below the naming limit | Accepted |
| [131](ADR-131-the-cheap-half-of-the-curve-is-not-measurable.md) | The cheap half of the fragment curve is not measurable, and the target stays where it is | Accepted |
| [132](ADR-132-spread-is-the-transition-mitigation.md) | Spread is the transition mitigation, and temporal AA is not a prerequisite | Accepted |
| [133](ADR-133-material-tiers-are-three-rungs-selected-by-a-uniform.md) | A material tier is three rungs selected by a per-draw uniform | Accepted |
| [134](ADR-134-one-quality-policy-object.md) | A mode is one policy object, not four independently-set structs | Accepted |
| [135](ADR-135-object-uniforms-has-no-free-lane-for-a-tier.md) | `ObjectUniforms` has no free lane, so the material tier is frame-global for now | Accepted |
| [136](ADR-136-what-the-shading-tiers-are-worth-and-what-that-says-about-the-residual.md) | What the shading tiers are worth, and what that says about the 8.4 ms residual | Accepted |
| [137](ADR-137-render-scale-is-a-tier-parameter-and-is-not-applied-yet.md) | Render scale is a tier parameter, and the tonemap binding is what stops it being applied | Accepted |
| [138](ADR-138-what-tier-assignment-can-realize-on-glowmere.md) | What tier assignment can realize on Glowmere is bounded by terrain coverage | Accepted |
| [139](ADR-139-the-volume-has-two-scalability-axes.md) | The volume has two scalability axes, they are tier parameters, and they do not divide evenly | Accepted |
| [140](ADR-140-the-march-and-the-composite-are-two-costs.md) | The march and the composite are two costs, and one of them is below the instrument | Accepted |
| [141](ADR-141-half-the-march-is-not-pixel-work.md) | Half the march is not pixel work, and Constellation's noise floor is not one number | Accepted |
| [142](ADR-142-a-quality-arm-can-be-looked-at.md) | A quality arm can be looked at, not only timed | Accepted |
| [143](ADR-143-temporal-reprojection-of-the-volume-is-rejected.md) | Temporal reprojection of the volume is rejected, because a policy parameter already reaches its ceiling | Accepted |
| [144](ADR-144-a-curve-carries-its-own-noise-floor.md) | A curve carries its own noise floor, or it is not a curve | Accepted |
| [145](ADR-145-a-scene-declares-its-own-density.md) | A scene declares its own density, and the declaration is what is checked | Accepted |
| [146](ADR-146-offline-is-only-half-hysteresis-free.md) | Offline is only half hysteresis-free, and the half that is not is measured | Accepted |
| [147](ADR-147-the-batch-render-path-never-selects-the-offline-tier.md) | The batch render path never selects the offline tier | Accepted |
| [148](ADR-148-the-noise-floor-is-four-numbers.md) | The noise floor is four numbers, and the one that binds is usually the pairs disagreeing | Accepted |
| [150](ADR-150-the-ceiling-is-measured-on-the-frame.md) | The ceiling of a representation system is measured on the frame, not on the curve | Accepted |
| [151](ADR-151-impostors-and-hlod-proxies-are-not-justified-on-this-content.md) | Impostors and HLOD proxies are not justified on this content, and the reason is measured | Accepted |
| [152](ADR-152-the-cull-ladder-omits-the-source-transform.md) | The GPU cull ladder sizes a procedural by its raw mesh, not by the object | Accepted |
| [153](ADR-153-the-impostor-rung-is-unreachable-and-would-cost-more.md) | The impostor rung exists, imported meshes cannot reach it, and reaching it would cost more | Accepted |
| [154](ADR-154-hlod-has-nothing-to-merge-here-and-the-invalidation-rule-is-recorded-anyway.md) | HLOD has nothing left to merge on instanced content, and the invalidation rule is recorded anyway | Accepted |
| [155](ADR-155-the-tier-follows-the-rung.md) | The material tier follows the LOD rung, and the ceiling is not reachable | Accepted |
| [157](ADR-157-a-depth-derived-quantity-needs-the-axis-and-the-filter.md) | A number derived from the depth buffer needs the axis correction and the filter | Accepted |
| [158](ADR-158-a-directed-shot-aims-at-its-hero-and-nothing-else-follows.md) | A directed shot's aim follows its hero, and nothing else follows | Accepted |
| [159](ADR-159-no-tap-may-step-further-than-the-texel-it-reads.md) | No tap may step further than the texel of the texture it reads | Accepted |
| [160](ADR-160-the-distance-ladder-is-authored-and-there-are-two-of-them.md) | The distance ladder is authored, all four rungs of it — and there are two ladders | Accepted |
| [161](ADR-161-root-motion-is-not-in-this-content-and-foot-slip-is-what-was-missing.md) | Root motion is not in this content, and foot slip is the thing that was missing | Accepted |
| [162](ADR-162-a-walker-off-the-navigable-set-cannot-be-steered-back-onto-it.md) | A walker that steps off the navigable set cannot be steered back onto it | Accepted |
| [170](ADR-170-the-gpu-lock-does-not-establish-exclusivity.md) | The GPU lock serialises agents, not the device, and a timing taken beside an open window is not evidence | Accepted |
| [171](ADR-171-glowmere-valley-is-two-scenes-and-the-named-one-is-the-wrong-one.md) | "Glowmere Valley" names two scenes, and the successor is built on the one that is not called that | Accepted |
| [172](ADR-172-an-aesthetic-score-component-is-a-band-not-a-maximum.md) | An aesthetic score component is a band, never a maximum | Accepted |
| [173](ADR-173-the-candidate-sampler-is-sobol-and-the-experiment-that-would-overturn-it.md) | The mushroom candidate sampler is a Sobol sequence, and the experiment that would overturn it is named | Accepted |
| [174](ADR-174-vegetation-is-banded-on-height-above-water.md) | Vegetation is banded on height above the water table, and the cap thins instead of truncating | Accepted |
| [175](ADR-175-a-searched-organism-is-a-parameter-vector-the-scene-owns.md) | A searched organism is a parameter vector the scene owns, not geometry the generator kept | Accepted |
| [176](ADR-176-a-tree-needs-an-internal-economy.md) | Space colonization decides where a tree grows; apical control decides how much | Accepted |
| [177](ADR-177-the-tree-is-skinned-not-bent.md) | The tree is skinned to a branch skeleton, because a per-tier wind uniform cannot hold its joints together | Accepted |
| [178](ADR-178-hierarchy-is-substance-not-order.md) | A branch's tier is its substance, not its depth in the graph | Accepted |
| [179](ADR-179-a-program-that-asserts-emission-owns-it.md) | A material program that asserts emission owns the whole emission contract | Accepted |
| [180](ADR-180-a-search-finds-only-what-its-parameterisation-expresses.md) | A search can only find what its parameterisation can express | Accepted |
| [181](ADR-181-counterbalance-the-arms-and-detect-the-drift.md) | Counterbalance the arms, and detect the drift the counterbalancing hides | Accepted |
| [182](ADR-182-a-diagnostic-arm-that-cannot-fail.md) | A diagnostic arm that cannot fail is worse than no arm | Accepted |
| [183](ADR-183-the-waterline-work-is-reverted-pending-a-different-approach.md) | The waterline work is reverted, pending a different approach | Accepted |
| [184](ADR-184-the-riverbank-work-is-carried-forward.md) | The riverbank work is carried forward; only the shimmer chase was reverted | Accepted |
| [185](ADR-185-a-continuous-shot-carries-its-aim-across-the-cut.md) | A continuous shot carries its aim across the cut, not just its eye | Accepted |
| [186](ADR-186-an-offline-render-lifts-the-limits-playback-needs.md) | An offline render lifts the distance limits playback needs | Accepted |
| [187](ADR-187-fxaa-gets-its-own-arm.md) | FXAA gets its own isolation arm | Accepted |
| [188](ADR-188-parenting-gets-an-appearance.md) | Parenting gets an appearance, and an offset you can type | Accepted |
| [189](ADR-189-fxaa-fades-in-rather-than-switching-on.md) | FXAA fades in rather than switching on | Accepted |
| [190](ADR-190-a-transition-comes-from-where-the-last-one-arrived.md) | A transition comes from where the last one arrived | Accepted |
| [191](ADR-191-the-lod-ladder-is-a-prefilter-not-a-saving.md) | The LOD ladder is a prefilter, not a saving | Accepted |
| [192](ADR-192-six-aliens-from-one-modular-source.md) | Six aliens from one modular source | Accepted |
| [193](ADR-193-the-entity-layer-hears-when-the-world-changes.md) | The entity layer hears when the world changes | Accepted |
| [194](ADR-194-a-body-off-the-ground.md) | A body off the ground | Accepted |
| [195](ADR-195-water-is-a-depth-a-body-is-in.md) | Water is a depth a body is in, not a line it stops at | Accepted |
| [196](ADR-196-a-solid-says-what-getting-past-it-takes.md) | A solid says what getting past it takes; the body says what it can do about that | Accepted |
| [197](ADR-197-the-navigation-layer-gets-an-appearance.md) | The navigation layer gets an appearance, and the overlay gets a camera pointed at it | Accepted |
| [198](ADR-198-secondary-motion-and-four-inhabitants.md) | Secondary motion, and four inhabitants instead of one placeholder | Accepted |
| [199](ADR-199-two-bugs-from-one-assumption.md) | Two bugs from one assumption -- that a thing is centred on its origin | Accepted |
| [200](ADR-200-the-camera-was-fast-in-a-way-the-cap-did-not-measure.md) | The camera was fast in a way the first cap did not measure | Accepted |
| [201](ADR-201-there-is-no-primary-hero.md) | There is no primary hero, only importance | Accepted |
| [202](ADR-202-importance-is-the-whole-of-casting.md) | Importance is the whole of casting | Accepted |
| [203](ADR-203-a-cap-that-broke-the-film.md) | A cap that broke the film, and the control that was missing | Accepted |
| [204](ADR-204-a-clip-starts-where-its-keys-start.md) | A clip starts where its keys start, a facing is not an odometer, and a stride speed is a claim about a clip | Accepted |
| [205](ADR-205-nine-farm-animals-from-one-turntable.md) | Nine farm animals from one turntable scene | Accepted |
| [206](ADR-206-structure-from-repetition-not-from-energy.md) | Structure from repetition, not from energy | Accepted |
| [207](ADR-207-world-effects.md) | World effects propagate through one frame-global block, not through per-object state | Accepted |
| [208](ADR-208-what-world-effects-cost.md) | What world effects cost, and the cross-binary number that is not evidence | Accepted |
| [209](ADR-209-the-kind-the-box-fix-missed.md) | The kind the box fix missed | Accepted |
| [210](ADR-210-the-director-is-a-decision-layer-not-a-behaviour-system.md) | The director is a decision layer, not a second behaviour system | Accepted |
| [211](ADR-211-a-route-you-can-click-and-an-answer-that-is-complete.md) | A route you can click, and an answer that is complete | Accepted |
| [212](ADR-212-an-offline-render-may-spend-pixels.md) | An offline render may spend pixels a realtime one cannot | Accepted |
| [213](ADR-213-an-animal-with-one-clip.md) | An animal with one clip | Accepted |
| [215](ADR-215-a-section-someone-decided-outranks-a-section-someone-detected.md) | A section somebody decided outranks a section somebody detected | Accepted |
| [216](ADR-216-the-sequencer-is-where-the-song-lives.md) | The Sequencer is where the song lives | Accepted |
| [217](ADR-217-the-cut-cannot-know-what-the-world-is-doing.md) | The cut cannot know what the world is doing, so let it be told one thing (the camera holds on a scenario's actor) | Accepted |
| [218](ADR-218-a-body-in-a-beam-has-two-positions-and-a-size.md) | A body in a beam has two positions and a size, and the scenario knew about neither | Accepted |
| [225](ADR-225-a-setting-nobody-keeps.md) | A setting the application does not keep is not a setting (the Auto-director saves with the project) | Accepted |
| [226](ADR-226-the-second-scene-with-the-same-stride-defect.md) | The second scene with the same stride defect, and the bands nobody scaled | Accepted |
| [227](ADR-227-the-blocker-search-gets-its-own-budget.md) | The blocker search gets its own budget, and the lane it needed | Accepted |
| [230](ADR-230-atmospheric-effects.md) | An atmospheric effect is a view ray, not a surface term (comets and aurora) | Accepted |
| [231](ADR-231-the-interface-says-what-it-can-do-and-what-it-is-doing.md) | The interface says what it can do, and what it is doing (cursors, context menus, the processing indicator, a threaded scatter) | Accepted |
| [232](ADR-232-a-parameter-path-carries-the-structure-it-was-saved-against.md) | A parameter path carries the structure it was saved against (the material op table that black-holed an elder) | Accepted |
| [233](ADR-233-two-things-rebuilt-that-nobody-changed.md) | Two things were rebuilt every frame that nobody had changed (the idle editor's 22 regenerations, and the sky rebuilt inside the drag) | Accepted |
| [240](ADR-240-a-placement-is-not-an-offset.md) | A placement is not an offset, a euler triple is not an orientation, a push is not a walk, and a stride speed is still a claim about a clip | Accepted |
| [241](ADR-241-the-director-says-what-it-writes.md) | The director says what it writes (the Inspector's `Kind::Staging`, declared and observed) | Accepted |
| [242](ADR-242-a-target-nobody-reads-is-not-a-feature.md) | A target nobody reads is not a feature (AOV export, and the normal pass that was not a normal) | Accepted |
| [243](ADR-243-the-detector-and-the-eye-disagree.md) | The detector and the eye disagree, and the eye decides what ships (§4 reviewed: the artifact is spatial, FXAA stays, Glowmere supersamples) | Accepted |
| [244](ADR-244-section-7-found-nothing-to-fix.md) | §7 found nothing to fix, and that moved the bottleneck (cinematic lighting reviewed; authoring ergonomics promoted) | Accepted |
| [245](ADR-245-a-camera-is-not-the-camera.md) | A camera is not *the* camera (multiple cameras, authored shots, event-driven cuts, and the one published active camera) | Accepted |
| [246](ADR-246-the-preview-is-not-a-picture-of-the-render.md) | The preview is not a picture of the render, it is the render (output preview, the output frame in the canvas, safe areas) | Accepted |
| [247](ADR-247-the-director-is-never-told-what-a-chorus-is.md) | The director is never told what a chorus is (the song shot language: extensible section types, semantic shot intents, per-field override provenance) | Accepted |
| [249](ADR-249-a-section-says-what-it-wants-not-which-camera.md) | A section says what it wants, not which camera (Song Mode: semantic shot intents, multi-camera execution, and variation that is not randomness) | Accepted |
| [250](ADR-250-the-instrument-is-not-the-engine.md) | The instrument is not the engine, and no single number is allowed to be the verdict (Quality Lab architecture, the metric set and its rejections, the AOV/supersampling split) | Accepted |
| [251](ADR-251-the-supersampled-exr-is-a-corner-of-the-frame.md) | The supersampled EXR is a corner of the frame, and it looks like a render (the HDR readback takes its extent from the output size) | Accepted |
| [252](ADR-252-the-banding-instruments-are-blind-in-opposite-places.md) | The banding instruments are blind in opposite places, and VMAF ranks a defect above its own reference (libvmaf measured and put on the ladder; the `neg` model made the default) | Accepted |
| [253](ADR-253-the-residual-knows-where-the-pixel-came-from.md) | The residual knows where the pixel came from, and that is the whole difference (the motion-compensated residual, the disocclusion mask, and ADR-243 narrowed to its axis) | Accepted |
| [254](ADR-254-a-benchmark-that-cannot-show-the-artifact.md) | A benchmark scene that cannot show the artifact is not a benchmark (the spline camera that looked away, and the FXAA that was never on) | Accepted |
| [255](ADR-255-the-shadow-aov-and-the-tier-that-has-no-shadow-texture.md) | The shadow AOV, and the tier that has no shadow texture to export (the mask pass does not run at `offline`, which is the one tier the Quality Lab measures) | Accepted |
| [256](ADR-256-a-material-id-is-not-a-class.md) | A material id is not a class, and a list of integers would go stale in silence (the mapping has to ship with the frames) | Accepted |
| [257](ADR-257-the-eye-ranked-them-the-way-the-spatial-measure-did.md) | The eye ranked them the way the spatial measure did, and the control in the frame was not a control (Quality Lab Phase 6: the human gate, the temporal detector's coin flip, the orb's interior) | Accepted |
| [258](ADR-258-the-shadow-aov-is-a-second-opinion.md) | The shadow AOV is a second opinion, measured at 0.43% of the frame, and it ships saying so (ADR-255 implemented; the fidelity experiment, and the arm that inverted the plane) | Accepted |
| [259](ADR-259-the-identifiers-material-half-is-not-a-material.md) | The identifier's "material" half is not a material, and the manifest had to be keyed on the half that is (ADR-256 implemented; surface classes, materials.json, vegetationResidual) | Accepted |
| [260](ADR-260-the-character-runtime-has-three-positions-and-only-one-of-them-is-drawn.md) | The character runtime has three positions, and only one of them is drawn (the stride bob that sank a grounded body, and the slope lean that has no fix inside the Euler triple) | Accepted |
| [261](ADR-261-the-lab-suite-is-mostly-already-here.md) | Most of the lab suite was already here under other names, so Phase 1 built the four things that were not (the Engineering Lab Suite: registry, cases, overlay profiles, the visibility vocabulary — and the two debug switches wired to nothing) | Accepted |
| [262](ADR-262-the-beam-is-a-column-not-an-axis-and-the-animal-is-not-its-origin.md) | The beam is a column, not an axis; the animal is not its origin; and the project was overriding the scene (the Tractor Beam Lab, `Anchor::Drawn`, `StepDesc::place`, and why three reported fixes never reached a render) | Accepted |
| [263](ADR-263-one-sphere-was-answering-two-questions.md) | One sphere was answering two questions, and a rung change drew nothing (the LOD / Geometry Lab: the ladder's own radius, the provable level range, the impostor predicate, and the rung overlay wired) | Accepted |
| [264](ADR-264-a-scene-file-is-not-the-state-that-runs.md) | A scene file is not the state that runs, and the entity layer was anchored before the project spoke (a project's 5,489 saved parameters, the beam that ran at 3.07 m, and the 28.661 m that was the diagonal) | Accepted |
| [265](ADR-265-the-shadow-pass-is-a-second-cull-and-only-half-the-engine-knows.md) | The shadow pass is a second cull, and only half the engine knows it (the Shadow Lab: the caster rule made a function, the impostor that faced the camera while the sun was looking, and the ecology's missing second cull) | Accepted |
| [266](ADR-266-nine-of-the-twelve-layers-were-already-here.md) | Nine of the twelve layers were already here, and the three that were not are not the three anyone expected (character intelligence Phase 0: navigation, the action queue and the staging parallel, already built) | Accepted |
| [267](ADR-267-the-scrub-already-matches-the-play.md) | The scrub already matches the play, and what breaks it is the frame rate and where the camera is (play vs seek 0.000022 m; behaviour LOD reads the camera and moves a body 50 m) | Accepted |
| [268](ADR-268-a-navmesh-has-no-triangle-soup-to-be-built-from.md) | A navmesh has no triangle soup here to be built from, and one walkable surface per point is a fact about this world (Recast rejected, and what would change the answer) | Accepted |
| [269](ADR-269-nobody-decides.md) | Nobody decides, and a behaviour tree would be a second interpreter over the queue we already have (utility scoring over `ActionQueue`, not a BT) | Accepted |
| [270](ADR-270-every-character-can-see-everything.md) | Every character can see everything, and the only sight this engine owns costs 1.7 ms a look (perception as the missing layer; the scan measures 0.098 us) | Accepted |
| [271](ADR-271-a-ui-edit-lands-in-the-project-and-must-be-readable-there.md) | A UI edit lands in the project, and the number it leaves there has to be readable (`particles/<name>/extent` becomes absolute metres; the beam guard compares runtime against the parameter stack) | Accepted |
| [272](ADR-272-the-reach-was-computed-for-a-light-the-shader-never-sees.md) | The reach was computed for a light the shader never sees, and the froxel look-up had no reference (the Lighting Lab: an area light cut off while still lighting, `emitterArea` promoted, `ClusterGrid::clusterOf`, the per-light cap report, and the two overlays that draw a light) | Accepted |
| [273](ADR-273-ninety-seconds-is-not-an-amount-of-work.md) | Ninety seconds is not an amount of work (`SeekBudget` prices a scrub in body-steps; the four seek defects; the replay's last step lands on the target and the residual goes to zero; a flatten's texture version becomes conditional) | Accepted |
| [274](ADR-274-a-joint-transform-is-entity-local-and-nothing-had-to-choose.md) | A joint transform is entity-local, and until something implemented it nothing had to choose (`ISkeletonQuery` implemented, `setSkeleton` called, `socketTransform` reports its fallback; the node scale it never carried) | Accepted |
| [275](ADR-275-a-lab-case-that-cannot-be-run-says-what-it-is-waiting-for.md) | A lab case that cannot be run says what it is waiting for (the Character Intelligence Lab: `blockedBy`, the floating hero that read as "obstacles do not contain a character", and the explorer's 1.36 m scrub) | Accepted |
| [276](ADR-276-a-star-is-project-data-and-never-reached-a-render.md) | A star is project data, and until now it never reached a render (session 1 hero, reloaded 0; the third instance of ADR-207's family, and the directed cut it left aimed at nothing) | Accepted |
| [277](ADR-277-the-only-way-to-look-at-a-post-stage-was-to-write-a-gpu-test.md) | The only way to look at a post stage was to write a GPU test, and two readbacks were quietly wrong (the HDR / Exposure / Bloom Lab: `--post-stages`, the metering copy a reset did not discard, and ADR-251's mistake at the call site it did not reach) | Accepted |
| [278](ADR-278-a-scene-file-could-not-author-a-light.md) | A scene file could not author a light, and the key it wrote instead was ignored in silence (top-level `"lights"`, a `"node"` that rides instead of a `NodeKind::Light`, the precedence against the four existing sources, and `core/json_keys.hpp` -- which found fifteen scenes writing `volumeNoiseAmount` at a parser that reads `volumeNoise`) | Accepted |
| [279](ADR-279-the-blooms-reach-is-a-pixel-count.md) | The bloom's reach is a pixel count, so supersampling halves it -- measured, and not changed (r99 flat at 120 px across a fourfold range; 0.506 at renderScale 2 with a control that does not move) | Accepted |
| [285](ADR-285-an-impostor-is-the-size-of-the-thing-it-stands-in-for.md) | An impostor is the size of the thing it stands in for (`makeLodMesh` sizes the billboard through `sourceTransform.scale`, the one step of the chain a camera-facing quad cannot travel; byte-identical on Glowmere, which reaches no impostor rung) | Accepted |
| [286](ADR-286-a-level-that-lost-part-of-the-object-says-so.md) | A LOD level that lost part of the object says so (ADR-085's guard applied to bounds; the reported error floored by a measured lower bound; `CommonTree_1`'s bottom rung goes 183 tris and 59% of its height to 130 tris and 86%) | Accepted |
| [287](ADR-287-the-ecology-gets-its-own-caster-list.md) | The ecology gets its own caster list, and it is not the camera's (one classify, two compactions, two slot ranges; Glowmere's 485/485 becomes 485 visible and 288 casting -- 208 gained, 405 shed) | Accepted |
| [290](ADR-290-a-percept-is-what-a-character-noticed-and-the-budget-is-per-character.md) | A percept is what a character noticed, the cadence is for the replay, and the occlusion budget is per character (`IPerception` built: two grid scans, a pure-function sense tick with a per-body phase, salience without visibility; ADR-270's 1.7 ms sightline confirmed at 1800.6 us and shown to be a property of the world function; the cadence is worth 15x on a scrub and `SeekBudget` cannot see it) | Accepted |
| [295](ADR-295-the-world-was-sampled-twice-and-the-grid-must-prove-it-may-answer.md) | The world was sampled twice, and a grid that answers for terrain has to prove it may (`TerrainQuery::at` took two `WorldMap::sample`s; `steer` 59.754 -> 34.853 us; `NavGrid::vouches` tests its own rule against the world at build and Glowmere refuses) | Accepted |
| [296](ADR-296-the-engine-knew-which-half-of-the-world-it-was-on.md) | The engine knew which half of the world it was on, and which characters could not leave (`NavGridStats::stranded` -- 9,646 of 19,584 cells on Glowmere; `NavDebug::confinedFor` for the body that never asks for a path) | Accepted |
| [297](ADR-297-a-door-that-closes-is-a-rectangle-of-the-graph.md) | A door that closes is a rectangle of the graph, and the labels are the whole of it (`NavGrid::rebuildRect`: 2.05 ms over 24 m against a 287 ms build; regions relabelled whole because a wall divides a region that reaches the other side of the world) | Accepted |
| [300](ADR-300-a-head-is-a-group-of-names-and-the-rig-has-no-word-for-it.md) | A head is a group of names, and the rig has no word for it (the animation layer stack: an aim layer and an additive one over the gait, a joint mask resolved against the rig it runs on, and `head.x` with zero children on a 90-joint alien; `lookTarget` and `reaction` reach a pose for the first time) | Accepted |
| [310](ADR-310-a-sentence-the-editor-draws-is-one-somebody-can-read.md) | A sentence the editor draws is one somebody can read (one `PushTextWrapPos` in 19,000 lines of `src/ui` and 150 unwrapped tooltips; the Control panel's load warnings stopped at `'atmos/`, every modulation route's label was outside the window, the Assets name column was fifteen points wide; the wrap guard is declared where a panel is begun, and a negative wrap position means *do not wrap*) | Accepted |
| [320](ADR-320-the-preview-is-the-file-or-it-is-nothing.md) | The preview is the file's own pixels, or it is nothing (the Render panel shows the frames the encoder is writing, tapped between the hash and the queue; 0.19 ms a frame, and the sequence hash could not have caught the hazard the file comparison did) | Accepted |
| [330](ADR-330-a-removal-is-a-negative-fact-and-nothing-was-left-to-carry-it.md) | A removal is a negative fact, and nothing was left to carry it (deleting an object never reached a render: 80 -> 79 -> 80 on the owner's own film; the project records the node-set **difference** against the scene it saves by reference, spliced into the document before the parse so there is one way to make a node) | Accepted |
| [331](ADR-331-a-cap-on-an-authored-value-is-a-refusal.md) | A cap on an authored value is a refusal, and this one refused in silence (`material/emissive`'s ceiling of 50 clamped an authored 256; the hard range now holds what the file says and the soft range still draws the slider, and every range in the table reports the authored values it overrules -- zero of them across the repository) | Accepted |
| [332](ADR-332-fifteen-scenes-asked-for-noise-and-five-projects-wrote-back-the-zero.md) | Fifteen scenes asked for volumetric noise, and five projects wrote back the zero they got (ADR-278's open trigger closed: the key corrected, the ADR-264 residue that would have made it inert removed, and the picture measured -- SSIM >= 0.9990, CIEDE2000 p95 <= 0.76, nothing worse) | Accepted |
| [333](ADR-333-a-character-kind-is-a-list-of-considerers-and-the-goal-model-had-to-move-without-moving.md) | A character kind is a list of considerers, and the goal model had to move without moving a millimetre (`IConsiderer` built: a selector whose dwell is counted in decision ticks, four stock considerers, the first consumer of a percept, and a guard with no `Guard` class; `Explore`'s goal model extracted against a 3,600-sample byte-identical position trace, and its weighted roll deliberately left where it is) | Accepted |
| [334](ADR-334-a-scale-is-a-ratio-and-glowmere-had-lost-both-ends-of-it.md) | A scale is a ratio, and Glowmere had lost both ends of it (the cast back to 1.0 with every metre-per-second beside it; the tree layers to 8/8/6.5 m so the signature organism stands above the tree line; the hero fungi untouched, and `kSignatureMax` deleted rather than widened because three renders said the elder was never what was wrong) | Accepted |
| [335](ADR-335-the-cast-goes-back-up-to-the-trees-and-half-the-ladder-stops-being-a-canopy.md) | The cast goes back up to the trees, and half the ladder stops being a canopy (the owner's sentence had two readings 1.85x apart and they chose the tree line: 21 bodies x1.94 in four scenes with every metre-per-second beside them; forty-two project speeds that had been putting ADR-334's back since it merged; arm 2 replaced by a band -- five canopies and five grounded -- whose halves fail on 3.6x and on 1.0x) | Accepted |
| [336](ADR-336-a-considerer-that-prices-the-way-and-not-the-place.md) | A considerer that prices the way and not the place, and the river the lab already had (the fifth stock considerer: two `requestPath` calls at two `NavPathCost::wadePenalty`, both scored with the character's own price on a wet metre; the detour wins at 12.0 and loses at 0.4 on the identical pair of routes, and the option's actions are a `Move` per waypoint because an option that named only the far bank forded anyway) | Accepted |
| [337](ADR-337-root-motion-is-a-transfer-of-authority-and-the-root-is-not-the-ancestor.md) | Root motion is a transfer of authority, and on this content the root is not the ancestor (per-clip opt-in writing `MotionAuthority::Simulation`; `Landing`'s -0.567 m reaches `state().position()` and the node parameter as -2.0457 m while the drawn toe falls the same 2.8120 m either way; compensation at `root.x` would leave `spine_01.x` falling 0.567 m, so it lands on the skeleton root; 165 clips compared, 159 bit-identical, 6 changed and all of them `Landing`) | Accepted |
| [338](ADR-338-the-look-was-in-the-blend-file-and-the-glb-came-out-brown.md) | The look was in the .blend file and the GLB came out brown (the Tree of Life hero export has no textures, no UVs and one emissive material out of seven, and a `material` block on a `gltf` node is parsed and dropped -- so the Glowmere palette is restored by a tool that rewrites the GLB's JSON chunk and proves the binary chunk's SHA-256 did not move; the island's Unity mask map repacked to glTF ORM with `_Smoothness` applied; the cottage shipped as its own export and rendered against the bare landmass, where the house is 57 units tall against the tree's 78 and hides the trunk) | Accepted |
| [339](ADR-339-the-glowmere-look-was-four-masks-and-the-export-kept-the-one-that-mattered.md) | The Glowmere look was four masks, and the export kept the one that mattered (`Tree_Glowmere.blend` and `Tree_of_Life_Hero.blend` are byte-identical in content, so there is no separate Glowmere source; the leaf emission is `ObjectInfo.Random > 0.94` per *instance* and the hero GLB's leaves are contiguous 18-vertex blocks, so `instance = vertex // 18` recovers 51,633 and 122,767 leaves and 5.9%/6.1% come out lit; five layer GLBs turn one scalar `emissiveBoost` into §8's five channels with no engine change; the cream-white in the reference is the blend's warm area key, not a material; large procedural points are opaque squares, so the haze is a background shader whose first two versions rendered a plausible flat gradient from a hash with `fract` on the wrong side of the dot product) | Accepted |
| [340](ADR-340-the-showcase-is-a-rebuild-and-the-river-was-a-wall.md) | The showcase is a rebuild, the river was a wall, and the hills were banded off on purpose (valley 3 generated by a script that reads neither of its outputs, 4 project parameters against valley-2-multicam's 5,455; the Glowmere run is 3.6 m deep against a 0.85 m wade and leaves the map at both ends, so a ford and a backwater are authored and the nav grid goes from 19 regions with 49% stranded to 3 with 0%; the hills are refused on height-above-water 95.1% and on slope 0.4%, not on biome, so eight tree species replace three and 2,593 trees stand where 545 did in the same world, 1,344 of them on the hills where 150 were with the 11.20 m tree line untouched; `decide` gains a stall breaker and `ground` gains a body, both off by default, after four of five characters stopped moving by t = 90 and two closed to 0.238 m) | **Retired** (ADR-344; the river, the riparian gate, the `investigate` lock-on and the two engine fixes survive it) |
| [342](ADR-342-the-engine-advertised-exr-environments-and-could-not-read-one.md) | The engine advertised EXR environments and could not read one (`asset_catalog` has called `.exr` an "environment" all along while `loadImage` routed everything through stb_image, so `--env map.exr` failed *initialisation* with "unknown image type"; routing it to the `readExr` that already existed then exposed tinyexr's refusal of BlenderKit's DWAA, so the runtime copies are derived 2048x1024 half+ZIP and the tool proves they are still scene-linear by exact channel means either side of the encode; neither HDRI is ever drawn as the visible sky, which settles the double-moon, double-sun, HDRI-horizon and seam hazards with one decision and is measured -- the island underside moves 8.8x and 3.1x while the sky patch does not move at all) | Accepted |
| [343](ADR-343-one-dayphase-and-the-sky-you-cannot-have.md) | One dayPhase, and the sky you cannot have (`fract(seconds / cycleSeconds + offset)` with no accumulator, so scrub equals play and 5,000 cycles later is the same phase to 1e-4; the ocean is a flooded terrain because a finite plane cannot reach the horizon -- 218,000 units would be needed -- and the horizon of a flat sea is at the camera's eye height, so the framing is solved rather than nudged; ADR-049's two skyIntensities bite, and driving the IBL one left midnight rendering at 102/110/125 sRGB; and the engine ties the IBL source to the skybox, so HDRI lighting with a procedural visible sky is not configurable and the sky-colour curves are inert in this scene) | Accepted |
| [344](ADR-344-the-showcase-came-home-and-the-hero-table-was-a-photograph-of-the-end.md) | The showcase came home, and the hero table was a photograph of the end (valley 3 deleted on the owner's word, its eight tree layers and its `decide` cast ported into `glowmere-valley-2-multicam` instead; 638 -> 2,872 trees and 152 -> 1,351 on the hills with the 11.20 m tree line untouched, the hill gate re-measured at HAR 95.1% / slope 0.4% on the film's own terrain; `RouteConsiderer` does not port because this river has no ford and 49% of the ground is stranded, and `vane` watches `ember` because `ember` is the only body it can reach; `heroes[].position` for a moving hero is a photograph of the *end* of the director's run and drifts -68 m per save, which is why two shots framed a subject 272 m from its anchor -- corrected, the 42-shot coverage ladder goes 0.149-0.475 at a x1.45 step to 0.251-0.475 at x1.13) | Accepted |
| [345](ADR-345-lighting-and-background-stop-being-one-choice.md) | Lighting and background stop being one choice (the skybox was always whatever the IBL was built from, so an HDRI-lit scene's `zenithColor`/`horizonColor`/`groundColor` curves were computed, tested and unreachable; four vec4s appended to the END of `FrameUniforms` where no asserted offset moves, `skyRadiance` copied into `skybox.wgsl` reading `frame.*`, opt-in and default false; the sky goes from two states to 137/95/90 at dawn and 52/30/60 at twilight, and the decision becomes a predicate whose control fails on the old coupling) | Accepted |
| [346](ADR-346-an-environment-change-is-not-a-world-rebuild.md) | An environment change is not a world rebuild (`dirty_` has no granularity, so swapping one texture id re-flattened 557 entities and 1,069 meshes at 273-293 ms, twice per cycle; resolving the map is now its own step at 35.4 ms with no flatten, texture ids cached by path because `addTexture` appends, and the resolve runs *after* `applyParameters` because the cycle asks for the swap from inside it -- before, it landed a frame late and in a one-frame headless render never at all) | Accepted |
| [347](ADR-347-the-sea-joins-the-cycle.md) | The sea joins the cycle, and the fog stops arguing with the sky (water reflection and deep colour are curves on `dayPhase`, a 6x swing; the grazing view's second horizon was neither the plane's edge nor the reflection -- the control reads 24.1/31.5/55.0 at reflection 0 and 24.1/31.6/55.0 at 3 -- but the fog colour disagreeing with the horizon, so fog now tracks it; and the dark specks were a cumulus bank, not DWAA artifacts, which the lossless decode made *more* numerous not fewer) | Accepted |
| [348](ADR-348-the-foliage-will-not-decimate-and-the-selector-had-no-quality-floor.md) | The foliage will not decimate, and the selector had no quality floor (runtime LOD for imported assets: an 8-triangle closed shell has no edge to collapse, so 137,437 leaves return 1.000 of the source at every rung, and shell thinning -- whole leaves removed by a centroid hash, survivors grown by r^-0.5 -- reaches 0.500/0.200/0.070/0.020 exactly; the sloppy simplifier reaches those ratios too and looks worse, so the strategy is chosen once from the mean shell size; ADR-124's px/triangle rule is a cost rule with no fidelity term and took a 900-pixel hero straight to the bottom rung, so a rung whose error projects past 8 px is refused, and 8 is rendered rather than reasoned; LOD0 stays the source mesh and the rungs live beside scene.meshes; opt-in per node, and offline is untouched) | Accepted |
| [349](ADR-349-the-diagonal-was-the-bank.md) | The diagonal was the bank, and the bokeh was doing the lying (the reported "river water cut off along a hard diagonal" is the waterline: past it the bed is 5-8 m *above* the river surface and 17-37 m off centre, and 96-99.7% of the ground under the waterline has water on it out to 160 m; `terrainCull` off is **bit-identical**, `terrainLod` off moves 0.00051 with the line unchanged, and the same camera moved 50 m east loses the line entirely, which a chunk fixed in the world cannot do; what lies is the out-of-focus bank scatter running continuously across the waterline, so the eye joins bokeh to sparkle into one ribbon; by-catch -- `cameras/valleywide/fov` renders an identical frame at 44 and 18, and "water stops at 288 m" is a threshold artifact that moves to 340 and 375 as the threshold relaxes) | Accepted |
| [350](ADR-350-a-system-the-application-ran-and-did-not-keep.md) | A system the application ran and did not keep (`DayNightSettings` registered no parameters at all and the scene writer never emitted the block, so the cycle was reachable only by editing JSON and a scene that carried one lost it on save -- ADR-225 twice over; the sweep then found `showSkybox`, `proceduralSkyBackground`, `lightFromEnvironment`, `skyBloom` and **17 of 23 `WaterSettings` fields** equally unreachable, including every water property the water-world spec names; colour curves stay scene-authored by the owner's instruction and the sun angles are exposed in degrees because a raw radian is not a knob anybody can reason about) | Accepted |
| [351](ADR-351-a-path-tracer-is-not-what-this-codebase-has-always-called-offline.md) | A path tracer is not what this codebase has always called "offline" (six ADRs already spend that word on a WebGPU quality tier and the ADR-020 batch pipeline -- and `app::RenderJob` renders at `QualityTier::Realtime` -- so the Embree renderer is named for its technique: `avgen::pathtrace` in `src/pathtrace/`; the tinyexr writer is extended to named AOV layers rather than adding OpenEXR; `app::JobSystem` cannot serve as the tracer's parallelism, so the BVH commits through `rtcJoinCommitScene` on AV Gen-owned threads; and Embree 4.4.0 does not compile under Apple clang 21 unless `CMAKE_CXX_STANDARD` is unset around it) | Accepted |
| [352](ADR-352-the-gltf-brdf-is-kept-faithful-and-it-gains-energy-at-grazing.md) | The glTF BRDF is kept faithful, and it gains 68% of its energy at grazing (a white smooth dielectric measures 1.676 directional albedo, confirmed real rather than Monte Carlo by two estimators agreeing to four decimals; metals conserve at 0.9988, so GGX/Smith/Fresnel are right and the fault is the spec's own `(1 - F(v.h))` diffuse suppression; the owner chose to keep the model faithful, so the behaviour is pinned as a band, printed as a caveat in the startup capability report, and watched by an optional directional-albedo probe) | Accepted |
| [353](ADR-353-the-denoiser-is-a-prebuilt-binary-because-building-it-needs-a-compiler-we-do-not-have.md) | The denoiser ships as a prebuilt binary, because building it needs a compiler this project does not have (OIDN's CPU device requires oneTBB with no alternative backend AND ISPC >= 1.21, a third-party *compiler toolchain* its CMake refuses to proceed without; so the official macOS arm64 archive is SHA256-pinned exactly as Dawn is, off by default behind `AVGEN_PATHTRACE_DENOISE`) | Accepted |
| [354](ADR-354-a-starred-character-was-a-wall-and-a-cow-became-part-of-the-hill.md) | A starred character was a wall around itself, and a cow became part of the hill (three aliens travelled 0.00 m for the whole film at every seed because starring them gave each a hero cylinder and a clearance capsule on its own feet, so `TerrainQuery::at` said `InsideHero` at the body it belonged to; heroes an entity drives are now excluded from the *walker*'s two structures and from nothing else, and the control names the three. "Off axis" and "clipping into hills" are one defect: `slopeAlign: 1.0`, inherited from a flat-pad lab fixture, tilting livestock a mean 12-18 and a max 36 degrees into the hillside, with the four birds already at 0.55 as a free control. Every backwards-walking frame in the film is an animal 16.5 m up in the tractor beam. And a render does re-simulate the cast, up to the 90 s seek budget -- the byte-identical pairs were a binary that had never been rebuilt) | Accepted |
| [355](ADR-355-five-frames-a-second-was-a-bounding-box-rescanned-forty-million-times.md) | Five frames a second was a bounding box, rescanned forty million times (three call sites had the Scene in hand and called MeshData::bounds(), which scans every vertex, per entity per frame -- the Tree of Life's 45 entities carry 39.9M vertices, so each frame rescanned them five times over; two one-line substitutions to Scene::meshBounds take the live editor frame from 350 ms to 8.6 ms at the owner's real 6.09 Mpx canvas, and the first of them was worth 261 ms rather than the 87 it looked like, because the renderer calls entityCullBounds twice more per entity; LOD is a separate and much smaller GPU saving and was switched off in both shipped scenes; the headless path printed no CPU breakdown, which is why this survived) | Accepted |
| [356](ADR-356-cmd-click-slices-and-cmd-was-not-the-key-imgui-thought-it-was.md) | Cmd+click slices, and Cmd was not the key ImGui thought it was (the owner asked for a slice tool on every lane; the operations mostly existed and the decision in front of them did not, so `ui::planSlice` is that decision, pure, and the three existing Split faces -- which neither snapped nor recorded undo -- now plan and perform through it. Audio clips, lyrics and actor clip cues gained split primitives; `audio::splitClip` has to advance `inSeconds` by as much as `startSeconds` or the later half plays the wrong part of the take. A cut lands on the grid the thing it creates is dragged on, so the section lane keeps `sectionSnap_`. The audio lane stays a scrub because Cmd+click is not the plain click ADR-103 protected. And `io.KeySuper` is not Cmd: `AddKeyEvent` swaps Cmd and Ctrl under `ConfigMacOSXBehaviors`, so the obvious spelling fired on the gesture macOS had already made a right-click and never on Cmd+click -- caught only by a scripted arm that said MEASURED NOTHING four times first) | Accepted |
| [357](ADR-357-the-arrow-keys-step-by-what-you-are-snapped-to.md) | The arrow keys step by what you are snapped to (they were already bound and bound to a fixed pair -- a frame plain, a beat with Shift -- so a person working in beats got frames from the key they press most. The step is now the active snap mode's unit and Shift the next unit up: beat/bar, frame/second, marker/section, and for Off a view-relative 1%/10% floored at one frame because turning the grid off must not turn the feature off. `beatsPerBar` is read from `seq::BakeOptions` rather than spelled 4, and `snapSection` now reads it too. It returns a unit and a count rather than a time, because `Engine::beatBoundary` falls back from the analysed grid to the tempo and no pure function over a beat vector could) | Accepted |
| [358](ADR-358-a-shadow-casting-key-that-cast-nothing.md) | A shadow-casting key that cast nothing, and the range that is the same number for every scene (`--aov shadow` returns the constant 1.0 over all 1920x1080 pixels of the shipping Tree of Life -- byte for byte what the renderer refuses to write for a scene with `castsShadow` off; ADR-112's automatic range is `0.08 * 2048 / 2.12` = 77.28 m for every scene there is, and this one is shot from 219.3 m, so `environment.shadowRange` lets a scene say where its shadows stop; `lights/<name>/{azimuth,elevation,...}` in world angles and not the rig's camera-relative ones; a project could never set a shader layer's ISF inputs because `loadProject` runs 26 lines before `shaderLayers_.fromJson`; the emission was costing a factor of two in key-to-shadow contrast and comes down, with ADR-338's values kept as a preset; and no volumetric beam, because `volume.wgsl` does not sample the shadow atlas and four dead knobs are the defect one layer along) | Accepted |
| [359](ADR-359-a-leg-is-three-names-and-the-body-has-to-sit-down-first.md) | A leg is three names, and the body has to sit down before the feet can reach (the first IK in this engine: a closed-form two-bone solver with no seed, because an iterative one seeded from last frame is the state ADR-091 forbids; a third `PoseLayerKind` that reads a *chain* and refuses a mask, because half a knee does not reach half a target; the alien rig's foot, thigh and leg are three branches under two parents and bind says so by name; the farm pack binds its legs at 97.9-100.0% of their own span, so `extension` cannot default below 1 and the body has to drop to the lowest contact before any foot can lift -- `GroundSettings::footDrop`, 0 by default; `soleUp` read out of the rest pose because no hoof joint on the pack has an axis within 26 degrees of vertical; and three things only the render caught, including four hooves reporting `solved` on their targets to four decimals with both hind legs folded through the body) | Accepted |
| [360](ADR-360-the-tree-ignores-the-wind-twice-over.md) | The tree ignores the wind twice over, and particles are allowed to drift (`scene/windSpeed` at 0, at its hard max 4.0, and with the field forced on in the scene file all render the same bytes, against a control -- one project parameter, `emissiveBoost` -- that moves 174821 pixels: `wind.enabled` is a gate with no parameter, read from a top-level scene key and written back only when it is already true, so the application could never switch the wind on; and `windDisplacement` is reached only from the procedural scatter, while `vs_main` in common.wgsl is `object.model * position` and nothing else, so no imported mesh could deform at all. Both fixed: twelve more field parameters, a writer that fires on difference rather than on the gate, and a `meshWindOffset` that is a continuous function of WORLD POSITION so the tree's five GLBs share one origin and cannot separate -- `tree_rig.hpp` already documented why per-mesh bases cannot work. Calibrating it in fractions of a 138 m body shredded the canopy, and the culprit was the flutter's spatial phase turning a radian across a single leaf, not the amplitude at the crown. Records the owner's decision to relax ADR-091 for particles only, with the two things that must still be fixed for a render to be reproducible. And withdraws its own first `emissiveBoost` recommendation: a p90/p10 ratio over a fixed region is a probe that cannot fail, and `light_probe.py`'s n.L partition says key-to-shadow is 5.537 at 2.2 against 8.368 at 0.5. **2026-09-21 addendum: the contract holds UNDER THE LOCK and that condition had never been stated.** Its 44 determinism cases fail when another suite runs: `worlds.json` rendered through two fresh engines gave two different sequence hashes while `avgen_tests` was up, and `avgen_tests` itself died with SIGTRAP in `CVPixelBufferPoolCreatePixelBuffer`. Sequentially both are clean and the 44 pass three times with identical assertion counts. The gap in the project's model: ADR-170's lock was understood as protecting GPU work from GPU work with the "CPU suite" on the other side of the line -- **but the CPU suite encodes video through the same system frameworks, so the two were never disjoint and the lock protected less than everyone assumed**) | Accepted |
| [361](ADR-361-two-lists-of-the-same-thing-in-one-window.md) | Two lists of the same thing in one window, and a warning nothing but a person could see (the Help sidebar drew every recently viewed topic a second time under its category and keyed both rows on the document id alone; `CollapsingHeader` carries `NoTreePushOnOpen` and `Indent` has no id effect, so the two lists sat at the same id-stack depth and Dear ImGui reported "2 visible items with conflicting ID" -- the message's "Empty label" line is a static bullet and not a clue, and there are zero empty-label call sites in `src/`. A row's identity is now (which list, which document), the grouping moved to the ImGui-free `ui/help_sidebar.cpp` so a test can see it, and the control -- identifying a row by document alone -- collides 8 times over the shipped content, which is what the warning was counting) | Accepted |
| [362](ADR-362-a-reporter-that-cannot-survive-printing-the-failure-it-found.md) | A reporter that cannot survive printing the failure it found (ADR-358's bus error is two defects; this is the second. `REQUIRE(a.rgba == b.rgba)` over a four-megabyte byte vector makes Catch2 stringify both operands into the report and the process dies inside the failing-assertion handler -- printing `FAILED:` with no `with expansion:`, which is this project's own signature for a *killed* run, so a real failure was disguised as one. Frame comparisons now report a count, a first offset and a max delta through `tests/support/image_diff.hpp`, which is the form `test_gpu.cpp` and `test_resource_lifetime_gpu.cpp` already used by hand; four other call sites converted. The underlying non-determinism is untouched and this says so) | Accepted |
| [363](ADR-363-the-multicam-did-not-grind-to-a-halt-where-anyone-was-looking.md) | The multicam did not grind to a halt where anybody was looking (three instruments, nine data-only arms on one binary: the headless render went 27.57 -> 31.05 ms across the modernisation, +12.6%, and an `oldtrees` arm puts it back within 0.6 ms of before while keeping the ported cast, the layers and the re-baked cut -- so all of it is the eight-layer tree table and none of it is the animation. The editor is 8.9 ms against 8.4. What actually grinds is a **scrub**: 2,755 ms a click, 118,801 body-steps replayed -- and the same measurement on the pre-port project is **3,768 ms**, so `decide` is 27% cheaper per body-step than the `explore` it replaced and the port improved the only number that matches the complaint. The supervisor's hypothesis that the app's save had dropped the camera bake is a real defect with no cost attached: the serializer round-trips both keys, the loss is `releaseDirectedCamera` firing on a viewport drag, and the bake-less arm is 1.6 ms *faster*. Nothing is changed for performance; the `treelod` arm that puts back the distance gates the port flattened buys a median 0.98 ms and is generable rather than applied) | Accepted |
| [364](ADR-364-the-viewport-kept-drawing-underneath-and-the-jobs-were-not-on-the-list.md) | The viewport kept drawing underneath, and the jobs were not on the destruction list (there was no render state in this program at all -- no enum, no paused, no needsRedraw, no throttle -- so an in-app render was pumped as `job_->step(4, 0.010)` while the world went on being submitted into the same device. But "the live view keeps playing" is advertised in the panel and is right for an author iterating, so this is a setting defaulting to suspend rather than a silent reversal; `drawUi` is always true and a test asserts it for all six combinations, because a render nobody can cancel is the worse failure. The evidence is in the product: the panel shows how many frames were not drawn, and a suspension reporting zero is one that is not happening -- 307 frames against a control arm that prints no line at all. The control arm also exited 139: `Application::~Application` carries a commented destruction order that `job_` and `ptJob_` were never added to, so the GPU context died before the readback ring waited on it -- reproduced on a pristine main and fixed here) | Accepted |
| [365](ADR-365-the-movies-never-said-what-their-pixels-meant.md) | The movies never said what their pixels meant (no `AVVideoColorPropertiesKey` in the native backend and no `-color_primaries`/`-color_trc`/`-colorspace` in the ffmpeg arguments, so every file either backend has written since ADR-020 is untagged and every player has been guessing -- correctly, on an SDR deliverable, which is why it survived. Both now tag BT.709/BT.709/BT.709, which is what an 8-bit frame the tone map has already sRGB-encoded actually is; it writes down the interpretation that was already assumed rather than changing a picture, and it is the first thing an HDR path would need. `VideoInfo` reads the tags back from the track's own format description, the arm requires the exact strings rather than non-emptiness, and the control writes a deliberately untagged file through ffmpeg and requires the probe to notice) | Accepted |
| [366](ADR-366-the-path-tracer-had-no-settings-only-fields.md) | The path tracer had no settings, only fields (samples, bounces, seconds, seed, threads, denoise, AOVs and the ADR-352 probe lived in `Application` members with two of the eight hard-coded at the binding site; no project, scene or AppSettings carried a `pathtrace` key, so 512 spp saved and reopened came back 32 -- ADR-350's no-reader-and-no-writer, written by somebody holding that ADR. `app::PathTraceSettings` is now a peer of `RenderSettings` under `"pathtrace"`, deliberately NOT `pathtrace::TraceSettings` because that struct carries an intentionally-wrong control arm a file must never be able to set, with `captureFeatures` derived rather than authored and one translation, `traceSettingsFrom`, that both the panel and `--pathtrace` go through. The trace's output path was unreachable while a trace was selected -- the panel returns before drawing it -- and is now its own field with an Exr save-dialog kind and a refusal of any other extension. The `--pt-*` flags became optional so a project can be the base. The seed is parsed as an integer because 0x853c49e6748fea9b does not survive a double) | Accepted |
| [367](ADR-367-a-value-that-reached-the-gpu-and-was-ignored.md) | A value that reached the GPU and was ignored (`ParticleSystem::softness` was authored, validated, serialised, copied into `ParticleUniforms` and uploaded as `turb.z`, where the token appears exactly once in `particles.wgsl` -- in the comment naming the fields of the struct it sits in. The fourth of this repository's reader-without-a-writer family and the one that travelled furthest before being dropped, and not dormant: fifteen scene files carry values from 0.3 to 3.0 that somebody tuned and that have never changed a pixel. The fade measures view-space Z because that is what `linear_depth.wgsl` holds, deriving the view axis from the billboard basis rather than adding a uniform. The default goes 0.2 -> 0 because 0.2 was never a behaviour, only a number the unconditional writer baked into every scene it saved, so 0 is the only value that reproduces what those scenes already look like. Measured -22.4% brightness against a wall, byte-identical with the wall removed -- and the first draft of that control failed against itself, because two `Scene` temporaries shared a stack address and the pool reset keys on the pointer) | Accepted |
| [368](ADR-368-a-disabled-path-is-a-property-of-the-encoder-not-the-arithmetic.md) | A disabled path is a property of the encoder, not of the arithmetic (the Image/Look cinematic integration -- atmospheric, colour, local contrast, light wrap -- had an obvious home inside `fs_composite`, which already runs every frame and already binds depth and the bloom tiers; folding it in there and gating on `if (amount > 0)` would have cost no new passes and could not have kept section 60's promise, because a branch inside a pass that always runs changes that pass's register allocation and scheduling and "same result" is not "same instructions". The passes are therefore not encoded at all unless an amount is off zero, and `ImageLookIntegration::active()` is the single place that decision is made. Proved twice: the pre-tonemap float hash moves for each of the four amounts alone and for no shape parameter, and one frame of `examples/hero/hero.json` is byte-identical -- sha256 `6a525a5756becac7700ea62d` -- to the same frame from a separate worktree built at the parent commit where the feature does not exist, with two controls that both fire. Also records that the spec's own quotation of the disabled-path guarantee was false: the composite always runs and `pow(x,1.0)` twice, a `max(c,1e-5)` and a 0.18 round trip are not the identity, so section 60 can only ever have been a differential claim) | Accepted |
| [369](ADR-369-a-falloff-with-no-end-inside-a-plane-through-the-eye.md) | A falloff with no end, inside a plane through the eye (the comet's hard straight line across the sky was not the bounding sphere -- `bound * 8` renders byte-identically -- nor the aurora, which a `kind: comet` effect never emits, nor the background layer, nor the tail. It is the coma: an inverse-square wash squared, never zero at any distance, sitting inside `if (th > 0.0)`, which is a half-space test through the CAMERA. A plane containing the eye projects to an exactly straight line, which is why the edge was mathematically perfect and why a sphere's silhouette was the wrong suspect. Windowed on the cosine of the angle off the head so the rejection discards only what is already zero; wedge contribution 5.496 -> 0.995 with the comet's own brightness unchanged to three decimals. The guard fails without the window and only at some arc positions, which is the intermittency the owner reported) | Accepted |
| [370](ADR-370-a-leaf-is-a-card-and-the-canopy-is-measured.md) | A leaf is a card, the canopy is measured, and the air is one description (three things stood between the existing particle system and the brief's falling leaves: particles are round dots with no rotation and no texture, emitters are boxes somebody typed numbers into, and `particles.wgsl` binds no frame uniform so nothing couples `wind::` to a particle. A Leaf card spins in the view plane and squashes its SHORT axis -- a leaf going edge-on gets narrower, not shorter -- with a pointed-ellipse silhouette, a midrib, and two-sided shading for one multiply; velocity stretch is skipped because a smeared leaf reads as a spark. The emitter box is derived at bake from the named node's measured world bounds, sharing ADR-360's bounds walk so the wind body and the leaf emitter cannot disagree about where the tree is. The wind rides in the particle uniforms rather than through a new bind group, and pulls a leaf TOWARD matching the air rather than shoving it. windInfluence defaults to 0 and is byte-identical in a gale. Three tunings were rendered and rejected first: a blizzard that buried the tree, leaves washed to pale cream, and a crown that burst rather than shed) | Accepted |
| [371](ADR-371-the-vortex-lives-in-the-march-and-must-not-be-lit.md) | The vortex lives in the march, and it must not be lit (a world-space term in `volume.wgsl` rather than a new pass, because that pass already gives perspective, depth occlusion, parallax and a resolution knob -- polar construction with the angle sheared by radius, resampled into a frame that winds with the structure, fBM at three spatial AND three temporal rates because one rate reads instantly as a screensaver. The load-bearing decision is that the vortex contributes extinction and emission and does NOT scatter: folded into the shared density it multiplied through `inScatterAt` and swallowed the intensity-22 key light, returning a frame at mean luminance 131 of 255 with its own emission set to ZERO -- the flat haze ADR-358 predicted, reached from the other direction, and invisible as a cause because sweeping emission 16x moved the frame by 0.9 levels. Also: density and emission are PER METRE over a 27 m step, and the first values were three orders of magnitude high. Ships switched off because it is unfinished, not because it is finished -- which is the opposite of the wind's mistake and is said out loud) | Accepted |
| [372](ADR-372-agx-was-undone-with-the-wrong-curve-and-three-things-were-built-but-unreachable.md) | AgX was undone with the wrong curve, and three things were built but unreachable (`agx()` returns an sRGB-encoded value and `fs_main` re-encodes with the exact piecewise IEC curve, but `agx()` undid its own encode with `pow(v, 2.2)` -- not the inverse, worth up to **9 code values of 255** and >= 2 codes across 84% of the range, so a scene-linear grey five stops under mid grey rendered 7 where it should have rendered 16 and the deepest shadows were more than halved, on by default in every shipped frame; the CPU mirror carried the same wrong inverse so it only ever confirmed the shader was consistent with itself. Measured against a worktree at main's tip, three arms all moved inside -2..+9 and not one code wider. Plus three things built and unreachable: `post/output/sharpenId` could not affect a frame because nothing assigned `PostFrameInputs::identifier`; three motion-blur fields were read every frame and registered nowhere; and `Framebuffer::worstAlbedo` was filled per pixel and written to no channel -- wired rather than deleted, with ADR-352's per-material table now printed from the CLI instead of only from a test. And `fogCorrection` compared euclidean ray distance, view-space Z and a euclidean march limit as one quantity, diverging +27% in the frame corner, where `max(sceneDist, dist)` then switched the correction off entirely -- real, and worth 201 pixels of 2.07M at +/-1 on shipped content, which is said plainly because a correctness fix dressed as an image fix buys a re-baseline nobody needs) | Accepted |
| [374](ADR-374-the-vortex-is-a-funnel-and-the-camera-must-be-outside-it.md) | The vortex is a funnel, and the camera has to be outside its mouth (the cost gate ADR-371 left is answered and its premise inverted: step count is within noise and a 4 km march is CHEAPER than a 1 km one, because the cost is pixel coverage of non-zero density, not march length -- so raising `volumeMaxDistance` is nearly free and "bring the disc closer" is the expensive direction. An envelope early-out at 1e-6 is byte-identical and still takes the volume pass 5.636 -> 4.588 ms; at 1e-3 it saves more and changes 5 pixels, so the exact one wins. The slab becomes a funnel because a level camera sees a slab edge-on. Two bugs the frame-band split found and looking could not: `yn` clamps to 0 above the mouth so the funnel extended UPWARD as a full-radius cylinder, and the camera at 232 m from the axis is inside any mouth wider than that -- which is why enlarging the disc made it worse every time. Shipped at +5.5 ms, top -4.08 / low +10.78, tree still 1.54x the lower frame) | Accepted |
| [375](ADR-375-reachable-is-not-findable.md) | Reachable is not findable (every control this work added satisfies ADR-350 -- registered, modulatable, keyable, saved -- and the brief's section 16 asks for something else: the Parameters panel is one flat list in registration order, so "how windy is it" is a scroll through `scene/` between `stylized` and `volumeAbsorption`. Two panels named as the brief names them, with Wind split into what the AIR does and what each TREE does with it, which were one undifferentiated list of paths and are two different questions. An empty section says why it is empty, because an absence somebody can read is not the same as an absence. A wind body is creatable now and not only tunable -- ADR-360's own flagged consequence -- and starts at strength 1, because ADR-360 shipped one at 0 and the owner reported the scene as unchanged. The test is the half ADR-350 never covered: a panel asks for parameters BY STRING, a wrong path neither fails to compile nor throws, and the section degrades into an empty box indistinguishable from "this scene has no wind") | Accepted |
| [376](ADR-376-the-tree-conducts-and-the-canopy-is-never-quite-still.md) | The tree conducts, and the canopy is never quite still (phases 5 and 6: a travelling band that climbs from roots to canopy with separate tier shares, and a shimmer centred on ZERO so it takes light away as often as it adds it, because a one-sided shimmer is a brightening and section 6 says not to do that. Both reuse the wind body's frame rather than establishing a second origin for the same tree. Both are pure functions of time -- the particle relaxation does not reach a hero asset's own light. The failing test cost an hour of wrong hypotheses before the real one: `shimmerScale` and `noiseScale` are in WORLD UNITS tuned for a 138 m tree, so a 4 m fixture falls inside one noise cell, the wave becomes a single constant, and a negative one clamps to nothing. Third time in this branch a number right for a 138 m tree has been wrong elsewhere) | Accepted |
| [377](ADR-377-the-twelve-shot-review-found-what-the-numbers-did-not.md) | The twelve-shot review found what the numbers did not (phase 26: eleven shots fine, one not, and no metric in this branch would have caught it -- at windSpeed 3.4 the canopy smears into streaks. The obvious suspect was the flutter, which ADR-360 proved was the cause the first time, and capping it changed almost nothing; it is the LEAN, whose gentle `pow(h,k)`/smoothstep gradients amount to metres across one leaf at a large enough amplitude. ADR-360's "the lean shreds nothing however large it gets" was true at the amplitudes measured then and false at four times them. Three fixes, all byte-identical on the shipped frame: a crown-travel ceiling with a KNEE (ADR-055's plain asymptote scales leans that were never in danger and moved 839,162 channels), a saturating flutter, and the hard maximum of scene/windSpeed from 4.0 to 1.6 -- the soft range already stopped the slider at 1.5, but a modulation route clamps to the HARD range. Also reports test_wind_gpu:245 failing on pristine main, almost certainly ADR-372's AgX curve moving a silhouette threshold) | Accepted |
| [378](ADR-378-the-radius-did-not-cancel.md) | The radius did not cancel (the red wind test, and three wrong answers before the right one: not ADR-372's AgX curve -- the old tonemap swapped in gives the same value byte for byte, a control I should have run before naming another agent's change -- not the per-instance random, and not a flutter-phase divergence at t=0. `rightmostLit` scans the whole frame, so the upright stalk's rightmost pixel is the barrel's full radius at mid-height while the bent stalk's is the narrower tip apex, and differencing them subtracts a radius difference as well as measuring travel: a systematic 2 px shortfall, always downward. Invisible until the soft ceiling started biting, because the error is absolute and the tolerance was relative -- the same 2 px is 4.4% of an unclamped 30 px and 8.3% of a clamped 24. Both edges now come from the apex, and the guard is 2.5 px: it fails at +15% AND -15%, where the old relative form passed +15% because the bias cancelled it. A guard blind in one direction is worse than a loose one) | Accepted |
| [379](ADR-379-the-vortex-lights-what-floats-over-it.md) | The vortex lights what floats over it (phase 10 and the island's half of 11: two vec4s on FrameUniforms and one term in `atmosphereGroundAt`, borrowing ADR-230's comet-pool shape with the normal term reversed so the underside takes it and the flat top does not. Two mistakes, both found by controls rather than by looking: the first A/B compared vortex-on against vortex-OFF and attributed +33 luminance to the spill when it was the vortex's own volumetrics -- switching an effect off to get a baseline swamps the thing under test, and the real control needed `spill` to exist as its own parameter, so the measurement forced the control surface. And the strength was derived from `emission`, which is an emissive density PER METRE integrated along a 4 km march; scaling a surface irradiance by it is a category mistake and gave 0.24 where 2.5 was wanted) | Accepted |
| [380](ADR-380-the-motes-leave-the-tree-and-the-vortex-takes-them.md) | The motes leave the tree and the vortex takes them (phase 7 and the moving half of 11, and it needed almost no new engine: the attractor plus `orbit` already ARE a vortex force, the canopy emitter is already measured, particles already read the wind. What was missing is the thing that makes it storytelling rather than decoration -- the attractor has to BE the vortex rather than a copy of its coordinates, or it stops describing it the moment anything moves. Measured over 51 s: light leaves the band above the tree, 55.67 -> 45.11, and arrives around and below it. And it takes 30-50 SECONDS of playback to develop, because the motes have 170 m to fall -- my first four arms were five-second renders and showed nothing below the island, not because the force was wrong but because nothing had arrived. An effect whose evidence needs a minute of playback will be judged broken by anyone who checks it the way everything else here has been checked) | Accepted |
| [381](ADR-381-the-comet-reaches-the-fog-through-the-description-the-rock-already-uses.md) | The comet reaches the fog through the description the rock already uses (phase 13 mostly existed -- ADR-230's ground pool already gives the tree a travelling highlight and the underside transient light, worth 2.2 luminance levels here. The missing piece is the fog, and it takes the comet from `frame.skyGroundPoint`, the same description the surfaces use, so rock and cloud brighten from one account. Three errors in order: a 3D distance where `atmosphere_ground.wgsl` deliberately uses xz only, which measured exactly zero; the surface pool's 220 m reach applied to a cloud kilometres across; and the per-metre conversion missing for the THIRD time in this branch, lifting the frame 125 levels. Also a defect I nearly reported and did not -- the pool is as strong 16 s after the comet's window closes, which looked like a broken envelope until the rendered frame showed the comet plainly still flying under `activation: always`) | Accepted |
| [382](ADR-382-the-quality-ladder-the-song-and-two-views-that-would-have-found-the-bugs.md) | The quality ladder, the song, and two views that would have found the bugs (phases 15, 18 and 19. The ladder gains `particleSpawnScale` -- spawn rate and not capacity, because changing capacity destroys the pool and would empty every system mid-shot -- and its comment records at the site the lever nobody should reach for: the volume's STEP COUNT is not it, measured 32 vs 48 steps inside the noise while a 4 km march is cheaper than a 1 km one, because the cost is pixel coverage. Measured Preview at 52.6% of High's particles against a 0.25 scale, which is smaller than the number suggests because the pools are near capacity. Phase 15 is twelve routes through the modulation the hueShift already proves, all resolving, worth 9.9-11.1 luminance levels against a control -- and the long-form component is present but NOT established in size, which is said rather than claimed. Phase 19's two views exist because three of this branch's real bugs were geometry errors a picture would have shown in a second and no metric did) | Accepted |
| [383](ADR-383-the-tracer-gets-a-range-and-the-tone-map-comes-down-off-the-gpu.md) | The tracer gets a range, and the tone map comes down off the GPU (three of the offline audit's findings are one thread: the output layer was ALREADY renderer-agnostic, so the missing piece was the loop above it; AgX and its four siblings existed only in WGSL, which meant a path-traced frame could not become a video frame without a GPU -- the exact property that makes the tracer usable on a shared machine; and the Render panel said "one frame, not a sequence". `FrameRange`/`FrameSequenceDriver` are GPU-free and `RenderSettings` delegates its arithmetic to them, but `RenderJob` is deliberately NOT rebuilt on them -- twenty GPU tests pin it and the machine is contended, which is the refactor-on-an-unverified-assumption the audit warned about. The CPU tone map is PROMOTED from the mirror in test_image_formation_gpu.cpp rather than written twice, and anchored against byte values measured off the shipped pipeline and published in docs/hdr-lab and docs/image-formation -- all seven grey-ramp bytes and both saturated-highlight triples exact -- with ADR-372's historical wrong inverse as a control that must reproduce the OLD bytes, which it does and which caught a real defect in the new API's own srgbByte. Sequences load the project once rather than once per frame, gain motion vectors a single frame cannot have, and produce a BT.709-tagged ProRes movie with audio headless with no GPU) | Accepted |
| [384](ADR-384-the-ufos-teleport-is-a-clearance-floor-a-move-never-reached.md) | The UFO's teleport is a clearance floor the approach never came down off (Phase 1-3 of the abduction sequencing brief, measured and not yet fixed. `StepKind::MoveTo` applies its `clearance` to the position it writes on EVERY frame including the last, and reports Done on `cue.progress >= 1.0`, which is the tween parameter and not arrival -- so the film's approach ends 11.000 m above the destination it resolved (`cruiseClearance` 34 minus `hoverHeight` 23) and `Follow`, in the next beat, resolves the same destination, applies no clearance and writes it straight into `DirectorMotion::position`, which becomes a position with no easing and no blend. Two teleports per cycle, not one: the reported drop at the `beam` beat and an equal +11.000 m when the floor re-engages 0.8 s into the next approach. Control arm: the same film with clearance 34 -> 23 goes from 4 teleports and 11.004 m to ZERO and 0.575 m, the travel speed at 60 Hz. Phase 3 is the spec's Case 3: the Tractor Beam Lab -- different scene, flat ground, 90 m/s, every duration different -- reproduces 11.004 m at the identical boundaries, so the mechanism is the director's and the magnitude is the data's. The hypothesis list the brief leads with is negative for this file: no route, timeline track or saved parameter touches `nodes/visitor/*`, and the camera cut is DOWNSTREAM of the same beat entry -- `UFO Watch` is `followNode: visitor` with `eventBlend: 0.0`, so a hard cut lands one frame before the craft drops and takes the camera with it) | Proposed |
| [385](ADR-385-a-stated-reason-is-not-evidence-and-the-metering-readback-becomes-a-ring.md) | A stated reason is not evidence, and the metering readback becomes a ring (`post/bloom/levels` did not exist because `applyPostJson` said the pyramid is "fixed when the bloom pyramid is created, so there is no parameter to move" -- every clause false: `run()` reads it every frame for both pyramids and a GPU test has set it to 3 since ADR-039, which only means anything if the setting is live. A working control nobody wired, protected by a justification that was false, which is why it outlived five rounds of removing this family: everyone read the reason and moved on. Also corrects `post_processor.hpp`'s "the input is returned unchanged", which the spec had quoted as proof section 60 already existed -- the spec was wrong because the header was wrong. And section 66's CPU readback becomes a two-slot ring: measured at **+1.577 ms of CPU per frame** before (GPU +0.000, so the cost was the sync point and not the six metering passes) and **-0.273 ms** after, i.e. automatic exposure is now indistinguishable from manual. The feared adaptation latency does not exist -- one slot and two slots give a byte-identical EV curve through a ten-fold step, because the loop already had a two-frame floor, and a four-slot control lagging exactly two frames and 0.100 EV is what proves the comparison is not vacuous. No committed frame moves: exposure mode defaults to Manual and no project in examples/ sets it to automatic) | Accepted |
| [386](ADR-386-a-light-nobody-could-make-and-a-name-that-was-an-address.md) | A light nobody could make, and a name that was an address (the two authoring briefs, treated as one because they are one architecture. Three things blocked everything after them: a light had no `position` parameter, so no gizmo could move one -- every transform here is a parameter write and that is what makes it undoable; nothing in the application could create a light at all, `setAuthoredLights` having exactly one caller, the scene parser; and `saveProject` wrote no `lights` key, the fifth instance of ADR-207/230/276/330, so a light added in a panel lived in the window and in no document any render reads. A light's identity becomes an `id` and its name becomes a label, which makes rename free by construction rather than by re-pointing every binding -- derived from the sanitised name when a file gives none, measured at 0 unknown-parameter warnings on the owner's Tree of Life film against 2 for a control. Three drift defects sat between the authored list and its serialisation, each of which alone would have made every project write a `lights` key on every save: a lossy `lightAngles` round trip at ~6e-8 per load, the per-frame parameter writeback contaminating the comparison so a project that merely dims a light recorded its whole rig, and a C++-constructed light keeping an un-normalised default direction where a parsed one was normalised. Editor helpers are picked on the CPU in screen space -- no new `PickSpace`, no shader touched, and helpers structurally cannot reach a render. §7's lock is a guard on live data loss rather than a feature: `releaseDirectedCamera` drops 37 aim-follow and 42 shot-span entries and the next Save writes the loss, observed for real and recovered from git. Measured on `glowmere-stylized`: a spot on changes 56.0% of pixels at worst delta 192, and the SAME FILE with the light disabled is byte-identical to base. Solo is deliberately unshipped -- it would have made the panel the only writer of a parameter final the Inspector cannot name, and `test_repo_hygiene.cpp` caught it on the first full run) | Accepted |
| [387](ADR-387-the-vortex-is-an-effect-and-the-tree-panel-was-a-wrapper.md) | The vortex is an effect, and the Tree panel was a wrapper around one scene (the owner's rule is that first-class UI represents reusable engine concepts and a scene's artistic composition is data inside them; held against it the Tree panel audits as a mixture -- one genuinely generic subsystem it duplicated a global of, three sections that found their subject by sniffing parameter paths for "leaf" and "mote", and node properties reached by the string arithmetic ADR-382 records getting five characters wrong. The vortex stops being a singleton on `Environment` and becomes an `AtmosphereKind::Vortex` instance, chosen over a parallel system because ADR-230's atmospherics family ALREADY is a table-driven universal modulation interface: two constexpr tables buy registration, modulation, timeline keys, presets, save, default routes and serialization with no bespoke code. `AtmosphericFrame` carries one vortex and not an array, because ADR-374 measured the cost as pixel coverage and a second is a second full-screen term -- the data model permits it, the renderer declines and reports it as dropped. Legacy `environment.vortex` is migrated with its values in THREE places -- the scene, the project's own copy of the effect list, and the parameter PATHS -- except at radius 0, which was ADR-371's "off". The third nearly shipped broken: renaming `scene/vortex/*` orphans every route that named it, and the shipped project has five, so the funnel rendered unmodulated with 95.35% of pixels different while a uniform probe said every value reaching the GPU was correct to the last decimal -- a correct value is not a reached value, and what had been renamed was not the number but the permission to change it. The Inspector became an editor and the World overview gained the Objects list it never had, which is what made deleting the panel possible rather than merely desirable. Tree of Life and both Glowmere projects byte-identical: 0 of 518400 pixels, same sequence hashes) | Accepted |
| [388](ADR-388-the-funnel-becomes-a-field-anything-can-ask.md) | The funnel becomes a field anything can ask, and the light gets a way back in (`vortexShape` lived inside volume.wgsl, so the only thing in the engine that could ask where the vortex was was the raymarch -- particles approximated it with an attractor and an orbit force, and the FX spec wants clouds, lightning and seven populations all sampling the same thing. Now a sampler on ADR-055's exact precedent: pure function of packed uniforms, position and time, WGSL as a transliteration of the C++, compared through the PACKED form so a disagreement can only be about the maths. It is also the only version of ADR-091's determinism that is checkable rather than asserted -- two implementations agreeing at arbitrary (position, time) pairs in any order cannot be integrating a frame delta -- with a control that the field is not merely constant in time. Cost, and it is not obvious: taking the uniforms as a PARAMETER instead of reading the uniform buffer is not bit-neutral, because the Metal compiler contracts the arithmetic differently; the shipped frame moves by 62 of 518400 pixels, 60 of them by one code value, none adjacent, and no arrangement of the call site avoids it. One provably-invisible body beat two that can silently disagree. Plus the owner's scattering coefficient, default 0, one table row and one shader line, with a soft range of 0..1 chosen from a measured ladder rather than clamped to 0..0.2 'to be safe' -- which would have been the mis-scaled-knob defect arrived at from the cautious direction. The first draft of it quoted ADR-371's 131-of-255, which was measured on the PRE-FUNNEL slab: against today's shape full scattering is +15 levels, not a wash, and quoting the old number would have been ADR-385's stated-reason-is-not-evidence written by the person holding the evidence) | Accepted |
| [389](ADR-389-the-grain-is-aliasing-and-the-warp-is-the-only-mechanism-left.md) | The grain is aliasing, and the warp is the only mechanism left (the owner reports noise rather than smoke; the diagnosis was ray-march aliasing and measuring it corrected it three times. The step length is 125 m not 83 -- the scene file says 48 steps and the PROJECT overrides to 32, so a measurement taken from the scene file is a measurement of a file nobody renders -- and against `fbm3`'s own finest internal octave that is a 60x undersample. A step ladder converging at 384 says 54% of the lower frame's high-frequency energy is aliasing. The falsification test partly REFUTED the diagnosis: the fine octave alone is 26%, octaves 2+3 are 58%, and the contrast exponent is an independent amplifier worth 33%. Raising steps is not the fix -- ADR-374's 'step count is within noise' is corrected in place, 4.78 -> 8.20 -> 17.5 ms, superlinear, 96 steps spending the whole frame. And the band-limit cannot be a WEIGHT: attenuating the fine octaves and renormalising made the grain WORSE, 1.139 -> 1.307, because at this spacing even the coarsest octave is past Nyquist and summing several aliased signals cancels error that isolating one exposes. It has to be a frequency clamp -- which, applied strictly, erases the funnel into a flat teal wash, the honest answer to what 125-metre samples can carry and the proof the march is starved. A floor ships, documented as the compromise it is. The rule that outlives this: when more samples are unaffordable, coarse noise is the only affordable kind, so the only way to make a resolvable field read as smoke is to give it SHAPE by warping rather than detail by adding octaves) | Accepted |
| [390](ADR-390-the-cosmic-ocean-is-an-atmospheric-effect.md) | The Cosmic Ocean is an atmospheric effect, and the sky can afford to be procedural (chosen as `AtmosphereKind::CosmicOcean` drawn by the existing atmosphere pass rather than a parallel system -- no new pipeline, pass, draw call, render target, texture or entity, because ADR-230's family already buys registration, modulation, timeline keys, presets, save and serialization from two constexpr tables. The design was REVERSED by a measurement: the half-res nebula buffer this project's ADR-139 pattern would have called for is not worth a target and an upsample, because the sky is 79.07% of the frame's pixels and a full-res procedural pass costs ~0.02 ms per five-octave fBM at 1080p. Found by disbelieving a zero -- `background` timed 0.000 ms against the timeline's 65.536 us quantisation floor, not because it was free, and reading it back at 2160p gave 0.459. **A 0.000 in this timeline means below the clock.** Estimated ~0.5 ms against a 2.4 ms budget, and CPU cost goes DOWN because the ~4,540 star and mote instances it replaces are real CPU work and the effect is a handful of floats. Also found three pre-existing defects in the atmospherics family: `AtmosphericFrame::any()` never mentions the vortex, `defaultAtmosphericRoutes` has no vortex arm so a vortex silently inherits the comet's routes, and `toJson`/`fromJson` are a second field list beside the tables) | Accepted |
| [391](ADR-391-the-editor-gets-its-own-viewpoint.md) | The editor gets its own viewpoint (navigating the view was the same act as modifying the film's camera: `setViewportPose` wrote `camera/position`, so on a baked project a drag could only be honoured by destroying the cut -- ADR-386's lock, whose whole price was that Option-drag stopped working -- and "look through this camera" could not be built because no pose existed that the film did not own. The viewport gains a mode: Film (the director's answer, and **every render**), Editor (a pose on `Composition` that is never serialised), Through (pinned to one rig). The override is applied to the FRAME, never inside `resolveActiveCamera`, so ADR-091 holds and `activeCamera()` now reports the film truthfully in every mode -- which free-roam did not: it overwrote the answer with a fictional camera called "Viewport", and its own comment claimed the opposite. Free-roam is removed rather than kept beside it, and two of its properties were false by measurement: it was free of RIGS and not of TRACKS, so a baked `seq::Shot` dragged the "free" viewport around by the face, and a lab test asserted that as correct. Placed after the film's camera is final and before terrain LOD, the rig lights and everything screen-space, so picking, gizmos and box-select follow the frame on screen for free. Two things overrule the choice, both "somebody else is looking at this frame": an output-frame preview mode (Workspace navigates, Output Frame composes) and an open output or share, which are handed the live viewport's own texture. Not persisted, deliberately, which is what makes the render invariant structural -- a RenderJob builds its own Engine from a file that cannot carry an editor viewpoint. The general fact: a view is not a value; state that belongs to the person looking must not be reachable from the artefact, and must be applied as an override on the result rather than as an input to the computation) | Accepted |
| [392](ADR-392-the-family-generalises-in-three-directions-and-is-hand-written-in-five.md) | The family generalises in three directions and is hand-written in five (ADR-230's table-driven atmospherics family IS the environmental simulation layer the brief asks for -- but the table collapses only register/apply/capture; `toJson`, `fromJson`, `sanitise`, the styles, `defaultAtmosphericRoutes` and the panel rows are five more per-kind lists written by hand, and two of those are bare string literals that neither fail to compile nor throw. One aurora field, `edgeBrightness`, appears at nine sites that must agree by hand. Chosen over a `WorldEffect` base class, which would make the next kind safe and leave the three existing ones and all six lists exactly as they are. Instead the contract becomes five checks run per kind, obtaining every path from the registrar rather than from the tables. The finding worth keeping: `toJson(fromJson(toJson(e))) == toJson(e)` is NOT a sufficient round trip -- it holds when a field is missing from BOTH directions, so reading the values back through re-registration is what catches the repo's recurring reader-without-a-writer. Three defects fixed or named: `defaultAtmosphericRoutes` was `if aurora else comet` so a vortex got three routes to paths it does not register; the function has no caller in `src/`, so "Add aurora" still makes a silent aurora; and `resolveAtmosphericEffects`'s `else` arm IS the vortex, so a fourth kind forgotten there resolves as a vortex rather than as nothing) | Accepted |
| [393](ADR-393-the-cell-was-the-halo-and-the-sky-costs-ten-times-the-estimate.md) | The cell was the halo, and the sky costs ten times the estimate (the run that made ADR-390's Cosmic Ocean reachable and then looked at it. `resolveAtmosphericEffects`'s `else` arm WAS the vortex, so a fourth kind resolved as one and was dropped as "the second vortex"; the probe that catches it has to carry a kind the chain does not name, because a probe over the three that exist proves nothing. `effect_conformance`'s own check 4 could not have stood in: it picked "mine" with a ternary whose else was `counts.vortices`, so a new kind would have been compared against the vortex counter and agreed with itself. **ADR-390 §8 is wrong about `any()`**: the instruction to add `|| hasCosmicOcean` was written against a design in which the ocean is a term inside `atmosphereSkyAt`, and the implementation is a separate pipeline -- under which the ADR's own argument against `|| hasVortex` applies word for word. `buildAtmosphericFrame` searched for the vortex a second time with a loop that ignored the activation window while the comment above it said the resolve had applied it. The first render was a sky of BOXES: `coCell` places one body per cell with no neighbour search, which holds only for a profile with compact support, and both the star halo and the planet glow were exponentials -- cut off at the cell edge by a straight line in the two directions the cube grid runs. The general rule: a per-cell body with no neighbour search must have a profile that is zero at the boundary. Measured cost **+5.05 ms** at 1080p against ADR-390's ~0.5 ms estimate and 0.8 ms target -- the calibration was taken from `glowmere-cosmos`'s 2D value noise and applied to 3D domain-warped fBM, ADR-389's coefficient trap in a new form; the nebulae alone are ~2.1 ms. A warning census found 1549, 1462 of them one category in `ui_logic.hpp`, so `AVGEN_WARNINGS_AS_ERRORS` stays OFF and `-Werror=format` goes on by name -- its single instance was a live printf bug in the Render panel) | Accepted |
| [394](ADR-394-embedded-tempo-is-a-different-kind-of-fact-from-a-detected-one.md) | Embedded tempo is a different kind of fact from a detected one (a file's own BPM, read at import, with provenance as a first-class property: `TempoProvenance{None, EmbeddedMetadata, Detected, ExternalClock, UserOverride}` -- named Provenance because `app::TempoSource{Analysis, MidiClock}` already exists and answers which clock drives the beat, not how a number came to be known, and two enums called TempoSource would be the two "shot" types again. Which containers get readers was settled by MEASUREMENT, not by documentation: a probe against this build's pinned miniaudio opens WAV, AIFF, FLAC and MP3 and rejects M4A, Ogg and Opus as Invalid file -- `MA_HAS_VORBIS` is gated on an stb_vorbis include that `miniaudio_impl.cpp` does not make, and miniaudio's AudioToolbox references are all device backend, not decoding -- so an MP4 `tmpo` reader would be a parser no file could reach, which is this session's fifth built-and-unreachable subsystem avoided rather than shipped. One ID3v2 reader serves three of the four containers (bare tag, RIFF `id3 ` chunk, AIFF `ID3 ` chunk) across v2.2/2.3/2.4 with synchsafe-vs-plain frame sizes and unsynchronisation; FLAC reads VORBIS_COMMENT. Hand-written over TagLib on a specific ground: TagLib's marginal value is concentrated almost entirely in the containers this build cannot open. The brief's central tension resolved against its own premise -- there is NO expensive pass to skip, because both tempo paths consume an onset-strength curve the analyzer produces for every other audio-reactive signal anyway, so skipping "tempo analysis" would save an autocorrelation and cost the entire beat grid that BeatTracker, the Auto-director, the sequencer and every beat-driven route consume, and would pass every test the brief asks for. The pass always runs and the tag re-centres and narrows the tempogram's log-Gaussian prior instead: on a 172 BPM envelope with a half-time accent the default prior returns 86.0 bpm at confidence 0.914 -- confidently wrong by exactly an octave -- and the seeded prior returns 171.8, while a deliberately WRONG tag of 300 returns 195 rather than 300, so the prior constrains the search without dictating its answer. Metadata supplies the scalar, analysis supplies the grid, and neither substitutes for the other. Extraction lives in `ClipSources::sync` because `installAudio` receives a mixdown with no path and no container, so a tempo not captured beside the decode is gone. Only the override is persisted: a detected tempo is a measurement that re-analysis reproduces and an embedded one would go stale when the file is swapped, but that a person chose a number is the one thing nothing can recompute. Named gaps left deliberately, including the WAV `acid` chunk -- real and widely honoured, but reverse-engineered with no real file to validate against, and a parser tested only against a fixture in the layout the parser reads is a probe that cannot fail) | Accepted |
| [395](ADR-395-the-mitigations-adr-360-traded-for-get-built.md) | The mitigations ADR-360 traded for get built, and the third fact it did not name (ADR-360 relaxed determinism for particle systems on the owner's decision and named two things that had to become true in exchange; neither was built. `frameNonce()` had six call sites and none of them was particle code, and the bounded warm-up did not exist under any name. The spawn hash was keyed to `frameIndex`, which counts from wherever the render started and in the live application counts from when the window opened -- so an offline render never agreed with the frame the owner was looking at when they pressed render: **two fresh renderers handed the same renderTime and frame indices 0 and 97 differ over 7656 of 147456 bytes, max delta 235**. `FixedStepClock::seek` zeroing the index is NOT the defect and is not changed; the clock's contract is documented and tested, and what was wrong was particles keying randomness to a counter whose meaning is 'frames drawn'. A drizzle holds 2900 at t=2 in a full render, **100** in a partial one opening there, and **2900 exactly** with 36 warm-up frames -- exactly, because emission became `floor(rate*t) - floor(rate*(t-dt))`, which is arithmetically identical to the carry it replaces at constant rate from zero, so no existing render moves by a particle. What it does NOT buy is written down rather than implied: slot assignment comes from the compaction's dead list, a function of pool history rather than of time, so a spliced render is the same field and not the same pixels; closing that means keying the hash to the emission ordinal, which reshuffles every particle scene and is not what ADR-360 promised) | Accepted |
| [396](ADR-396-one-description-of-the-air-becomes-one-function.md) | One description of the air becomes one function, and gets the test that names it (the wind field had three implementations -- `core/wind.cpp`, `wind.wgsl` and a hand-maintained `particleWindAt` in `particles.wgsl` coupled to the second by a comment. The third had already lost `WindSample::phase`, four members where the field has five, and nothing could have said so: the existing 'parity' test renders a stalk in a wind with regionAmount, regionDrift, turbulence and gustAmount ALL ZERO -- the only wind whose tip position can be predicted -- and `turbulence = 0` also zeroes the flutter gain `phase` drives, so it is blind to three of the five numbers. `wind_field.wgsl` now holds the arithmetic once as a pure function of a `WindField`, with no binding and no include, the shape `vortex.wgsl` uses and the reason that field could be parity-tested at all. Two probes, because neither is sufficient -- a correct function nobody calls and a shared call into a wrong function are each green under one of them. The GPU half compares all five members against the CPU over 131 positions x 3 times with every stochastic term on, guards against agreeing about nothing (229 of 393 samples carry a partial gust, 388 a turned direction), and carries ADR-378's lesson as a second case: raise the flutter wavenumber by **1%** and the same comparison must reject it -- **129 of 131 samples rejected on `phase`, 0 on the other four**. The CPU half needs no device: every .wgsl in the tree is include-resolved and must contain the regional wave's coefficients at most once (18 carry it, none twice), the `windSampleFrom` body is byte-compared between the branch and the leaf, and `fn particleWindAt(` must appear zero times so restoring the copy is a test failure rather than a review comment) | Accepted |
| [397](ADR-397-a-pre-roll-is-a-schedule-two-subsystems-share.md) | A pre-roll is a schedule, and two subsystems share it rather than write it twice (ADR-395's particle warm-up and the temporal-media history buffers wanted the same mechanism within a day of each other, on the framing that history is not state but a CACHE OF A PURE FUNCTION -- so a seek rebuilds it by re-running a bounded K frames and nothing accumulates without bound. `src/core/pre_roll.hpp` holds the SCHEDULE and not the work: `planPreRoll` says which timeline seconds a roll consists of and what frame indices they carry, and each subsystem re-runs its own simulation over them, which is the part that is checkable on the CPU with no device in the room. `classifyStep` carries the distinction `ao_renderer.cpp` has the scar of (SYM-TERRAIN-1): a REPEAT has to reproduce, a JUMP has to drop, and treating them alike makes a re-render a different picture from the first. It reads the frame index AND the render time, and both halves earn it -- on the index alone a live seek looks continuous, because `RealtimeClock::seek` moves the second and leaves the counter climbing; on the time alone an offline re-render of one frame looks like a repeat of a frame never drawn. The tolerance is float slop, not a window: half a frame is a Jump, two ulps are Continuous. `arrivalFrameIndex` took a second attempt -- the roll's indices end one below the arriving frame's so a consumer sees Continuous on arrival too, and when there is no room below (a range opening at frame 0) the arrival is shifted up rather than the roll silently shortened; the test asserts the shifted arrival is Continuous AND that the unshifted one would have been a Jump) | Accepted |
| [398](ADR-398-the-meter-does-not-know-the-shot-changed.md) | The meter does not know the shot changed (two defects on one one-shot, both ADR-385's family. `PostSettings::exposureReset` has documented itself as '(scene change, timeline seek)' since it was written and only the first half was true: `Engine::resetCameraState` had **exactly one caller**, the composition install, so a scrub reset the renderer's temporal history and left the METER on its pre-seek reading -- the first frames after a jump from a night interior to a noon exterior were exposed for the interior. And `render_job.cpp`'s pipeline warm-up -- a throwaway renderer for a 1-LSB Metal effect, not a history warm-up and not ADR-397's pre-roll -- drives the real engine and consumed the one-shot, so the first shipped frame saw it already false; harmless only because that job's PostProcessor is default-constructed and meters nothing, which is accidental protection rather than a design. The reset now sits in `seekSeconds` and deliberately NOT in the per-frame update: re-rendering the same second is a Repeat, not a Jump, and re-seeding for one would make a frame depend on whether somebody clicked the playhead. Probe needs no device and is built so it cannot pass for the wrong reason -- frame 2 must NOT carry the reset, which is what stops it passing against a flag left permanently raised, and the playhead is asserted to have moved before the seek's frame is read. Measured with only that one call removed: `CHECK( m.reset() ) with expansion: false`) | Accepted |
| [399](ADR-399-the-two-placeholder-labs-get-fixtures-and-find-something.md) | The particle and volumetric labs get fixtures, and one of them finds something on its first frame (two of the suite's fifteen labs were registry rows with status Planned, no cases and a fixture pointing at somebody else's scene. Both are built to the shape the eight implemented labs have. The design that matters in the particle fixture is that `metered` and `saturated` are THE SAME EMITTER at capacities 8192 and 256, so the control shares the frame with the measurement -- a control in the same frame cannot be explained away by the exposure, the tone curve, the camera or the seed, and one that needs a second run always can be. The volumetric fixture is five identical unlit slabs at 4, 12, 24, 40 and 60 m with height falloff, noise and anisotropy all OFF deliberately rather than by omission: each is multiplicative on density, so a fixture carrying them cannot state the density at a point. Measured: the metered pool settles at rate x lifetime, the saturated one at exactly 256; the transmittance ladder runs 0.7057, 0.5647, 0.3947 down the slabs; and the pair worth quoting, two independent routes to 'the volume did not run' -- **pass disabled vs density zero: identical (921600 bytes)**, with **fog on vs pass disabled: 691200 of 921600 differ** as the guard that stops the first from being an agreement about nothing. The defect found on the first render: an authored particle `burst` was inert, because `registerParticleParameters` seeded `particles/<name>/burst` with a hard-coded 0 while spawnRate, spread and position beside it were seeded from the scene -- the key parsed, round-tripped through `particlesToJson`, appeared in the panel, and did nothing. The comment three lines below it describes the same defect about `extent`. It cost nothing to find: write the fixture, render it once, one of four systems is missing, which is the argument for building the remaining three placeholders) | Accepted |
| [400](ADR-400-the-water-reflects-a-photograph-of-a-different-afternoon.md) | The water reflects a photograph of a different afternoon, and that stays true for now (**measured, costed, deliberately not fixed.** Three accepted documents already described it -- ADR-345's Consequences, ADR-347's opening, and a comment in `day_night.cpp` -- which is three statements and no measurement, so it was measured first. R-B in sRGB bytes over bands either side of frame, dayPhase pinned: dawn sky +45.4 against water -13.1, a gap of **+58.5**; sunset +47.7 against -9.6, **+57.3**; and **noon +7.4**, which is the control and what makes the rest mean anything -- when the baked map and the procedural sky agree about the sky, the gap collapses to an eighth. A constant offset between two regions of the frame would show the same gap at every phase. Three fixes costed and all wrong today: transliterating the analytic sky into `water.wgsl` is an afternoon and is ADR-396's defect one system along, since that sky is ALREADY hand-copied twice and `skybox.wgsl`'s own header warns that the way this goes wrong is two implementations drifting; water calling a shared sky is still wrong because `pbr_shade.wgsl` samples the same cube, so fixing water alone trades 'the sky disagrees with the water' for 'the water disagrees with every other reflective surface', which is art direction and not an agent's call, and it also invalidates ADR-099's glint tuning and ADR-347's measured curve; and the correct fix -- a second prefiltered cube from the analytic sky -- is 10.3 ms a bake, a second resident cube set, a widened group-3 layout across six shaders and a per-phase re-bake budget, which is what ADR-233's deferral machinery exists to keep off the frame. What was built instead is the regression harness ADR-347 said whoever fixes this would have to build first, because no test in the tree asserts water reflection content at all -- every one that touches it sets `reflection = 0` to remove it as a variable. Deliberately not a tripwire: it asserts only what holds either way and PRINTS the table, because a test that asserted the gap would fail the day somebody closed it) | Accepted |
| [401](ADR-401-the-vortex-parity-failure-was-the-parity-test.md) | The vortex parity failure was the parity test (75 assertions red, all on `density`, worst case 3x -- and every candidate in the engine is byte-identical: the contrast curve, the octave weights, `fbm3`, `fbm3Vec`, `valueNoise`, `hash01`, `pcg3d`, the band-limit, the domain warp call site and both entry points. The harness copied the uniform block into its kernel FIELD BY FIELD, ADR-389 added `v6` -- smokeWarp, smokeBillow, detail -- to the real struct on both sides and to that list on neither, and WGSL zero-initialises a `var`, so the GPU ran the vortex with the domain warp, the billow and the third octave switched off while the CPU ran with them on. The shipped fixture leaves warp and billow at 0, so `detail = 0.2` alone -- one octave dropped from a weighted sum and its normalisation -- was worth the 3x. The signature pointed away from the curve the whole time: `envelope`, `radialT` and `depthT` are computed before any noise is sampled and read none of `v6`, so they passed, and the only quantity that could differ was the one downstream of the field that never arrived. **No engine change; neither side was ever wrong and no tuning decision is implied.** The harness now carries the block BY TYPE with a `sizeof` assertion, which is ADR-396's move one level up -- what drifted was not the arithmetic but the list of things to copy, so the fix is to stop keeping a list. The probe that matters, measured with the defect reintroduced: a contrast-curve perturbation PASSES (7 assertions green) while `every number the uniform carries reaches the shader` FAILS on smokeWarp 0/160, smokeBillow 0/160, detail 0/160 -- a probe over the transfer function cannot catch an absence, because an absent field is indistinguishable from an agreeing one from inside a comparison. A 5% contrast error is still rejected on 122 of 127 live samples, and two fixtures that looked reasonable measured almost nothing until the spread was placed from the geometry: `wall` peaks at y=-70 so the dense shell is y in [-140,0] and not down the funnel, and `smokeBillow` at 0.6 maps mid-range noise to zero where the contrast smoothstep then removes it) | Accepted |
| [410](ADR-410-temporal-history-is-a-cache-of-a-pure-function.md) | Temporal history is a cache of a pure function (the Reality/Temporal/Digital brief's §8-§17 make frame N depend on frames N-1..N-K, which ADR-091's "no accumulated state to be wrong about" forbids and §47's offline determinism forbids again. The answer is that no temporal effect may be an accumulator: each declares a bounded K whose *anchor* is a pure function of time, so history stops being state and becomes a cache that can always be rebuilt. Time rift, time freeze and rewind then need no history at all -- they are re-evaluations, `render(g(t))`, available only because ADR-091 made the scene re-entrant on time. Datamosh, the one genuine accumulator, gets a declared I-frame cadence anchored to `audio.beatCount`: the GOP structure that makes it land on the beat is the same thing that makes it deterministic. A cold history is never silently shown -- a seek scrubs cold, a bounded warm-up on settle converges to the true frame, and `framesValid < framesNeeded` is stated in the viewport, the panel, a debug view and RenderStats. Shares ADR-360's never-built warm-up rather than building a second, and `resetTemporalHistory()` rather than a second invalidation hook. Memory: depth is per-channel not global, colour history is half-res RG11B10Ufloat (8x saving), and §8's "object-specific history" is a read-time mask over the ADR-035 identifier target rather than a storage partition -- 25 MB, not 1.86 GB. §45 audited and measured: RG16Float velocity drifts 0.94 px over an 8-frame chain and needs no change; the target describes opaque surfaces only, so a mosh over fog advects with what is behind it) | Proposed |
| [420](ADR-420-one-field-many-subscribers-and-the-vortex-that-ignored-its-own-window.md) | One field, many subscribers, and the vortex that ignored its own window (the brief's §68 says each effect having its own isolated wind handling should stop; measured first, and it is worse than "isolated" -- `wind::sampleWind` has FOUR call sites in all of `src/`, three of them vegetation and one a CLI-only debug overlay, while particles reach the field only through ADR-370's opt-in byte copy that is off by default and set in three example files, and volumetrics, all three atmospheric kinds and water each carry private drift numbers, water having a second `windDirection` of its own. What was missing is not a field -- there are two excellent ones -- but any way for a third party to ask a question of a field it does not own. `world_effects/field_bus` is a publish/subscribe layer over the packed uniforms of ADR-055's wind and ADR-388's vortex that re-implements NEITHER: `sample` dispatches to the owners, so there is no fourth transliteration to drift from the three that exist. `FlowSample` carries its UNITS, because the wind's strength is dimensionless and the vortex's velocity is m/s and calling both "velocity" would be the fifth instance of this repo's unit bug, introduced by the file whose purpose is letting strangers ask each other questions. The subscription is ONE row in `kSharedFloats` plus a structural name, so all three kinds and any fourth get it from one line; 0 is off and every existing scene is untouched. Conformance check 6 is a DIFFERENCE not an inspection -- subscribing a kind to a field that is blowing must change its frame, and influence 0 must leave it identical -- because a shared row buys registration and buys nothing about whether a packer reads it. Two finds: `buildAtmosphericFrame` re-walked `effects` to find the live vortex and tested only `enabled`, under a comment claiming the resolve had applied activation and the lifetime envelope, which it cannot -- the resolve takes `span<const>`. A vortex with `activation: window` ignored its window and one with a `fadeIn` did not fade; the one authored vortex in the repo is `always` with no timing, so the feature nobody used was the only one broken. And `memcmp` over a whole `AtmosphericFrame` compared the two PADDING bytes between `hasVortex` and `vortex`, which member-wise assignment leaves indeterminate and the optimiser had written a float's low half through -- a probe that cannot PASS, found by disbelieving a failure) | Accepted |
| [421](ADR-421-the-stack-eight-scenes-author-and-no-panel-could-touch.md) | The stack eight scenes author and no panel could touch (the deformation brief asks for an ordered, reorderable, individually-modulatable deformation stack; `scene::Deformer` already IS one -- eight slots, seven kinds, local or world space, GPU-evaluated with a CPU reference, serialised, per-slot parameters, authored in eight shipped scenes -- and its entire user interface was the words `"%d deformer(s)"`. Two different failures: the NUMBERS were reachable and unlabelled, because the Inspector's generic grouping flattens every `deform/<n>/<leaf>` under one node with nothing saying slot 1 is a Twist; and the STRUCTURE -- kind, space, order, existence, plus a Sine's `displacementAxis`, a Noise's `axisMask`/`seed`, a Field's field and a Path's spline -- had no writer outside the scene parser. `setNodeDeformers` is unregister/mutate/re-register on `setNodeWindBody`'s precedent, writing both `procedural` and `proceduralRest`; it does NOT regenerate geometry, because deformers are deliberately absent from `structuralHash`, which is what makes an editor affordable on a 42-procedural scene. The rows are PER KIND because registration writes the same nine leaves for every slot whatever it is, so a Bend has a registered `speed` that moves nothing -- and a control that does nothing teaches an artist the system is broken, which is worse than one that is absent. Also a census of `DebugViewOptions` found the same defect in both directions at once: `wind`, `vortex`, `lights` and `lightClusters` implemented, read, and with no checkbox anywhere (the wind arrow grid since ADR-055, the one thing that answers which way the air is moving); and `culling` with a checkbox and no reader, which the CLI already refused to name for exactly that reason. The four get checkboxes, `culling` is DELETED rather than implemented -- `lod` already draws what it promised -- and a new test asserts the general rule: every switch on that struct has a reader and a checkbox. Shown able to fail: adding `Melt` to `DeformerKind` compiles with ZERO errors and fails a named test printing `melt`, which is the whole argument for reading the header instead of trusting `-Wswitch`. `-Werror=format` on, blanket `-Werror` off with the census that settles it recorded in the cmake file -- 3211 warnings, 2584 `-Wmissing-field-initializers`, 2577 of those in one header -- superseding ADR-393's, which counted `src` only) | Accepted |
| [422](ADR-422-a-deformation-operator-declares-whether-its-velocity-is-derivable.md) | A deformation operator declares whether its velocity is derivable (the interface between the deformation and temporal halves of the Reality/Temporal/Digital brief, agreed before either side built on it. The contract: an operator declares whether it preserves vertex correspondence; a preserving one joins `deformChain` and gets motion vectors for free, a non-preserving one must author velocity itself. The framing it corrects is that deformation without velocity breaks EVERY temporal effect -- it does not. `procedural.wgsl` already fills the velocity target by running the whole deform chain a second time at last frame's time through `object.prevModel`, and `pPrev` is computed from `srcPos`, the SOURCE mesh vertex: the same vertex at two times. So §4-§7 and §30-§34 are already correct and need nothing; §18-§25 and §35-§37, which create, destroy or re-index vertices, are the only subset that has to author velocity -- and when they do not, the artefact appears in the consumer rather than in the operator, which is the worst property a defect can have. `VertexCorrespondence` has NO DEFAULT and the traits struct is not default-constructible, asserted with a STATIC_REQUIRE: `Preserved` as a default would give a fragmentation operator silently wrong velocity, which is the exact failure, and `Broken` as a default would be visibly expensive and get flipped back. Seven ADRs in this repo are instances of a field nobody filled in behaving like a field filled in wrongly, and a constructor that will not compile without the answer is the cheapest place to break that chain. The contract's own quotation of the shader is checked by reading the shader, with a control, because a quotation is a stated reason too. And `EffectorOp::Velocity` -- which exists, serialises, appears in the World panel and is dropped on the floor by the only pass a rendered frame takes, along with `::Attribute` and any effector whose field missed the GPU table -- stops being silent: still skipped, now reported once per object/field/op) | Accepted |
| [440](ADR-440-a-project-is-dirty-when-it-no-longer-serialises-to-what-it-was-opened-as.md) | A project is dirty when it no longer serialises to what it was opened as (the "Save changes? Yes / No / Cancel" prompt, and the seven paths that discarded a project without asking -- New, Open, Open Recent, Examples, Engineering Labs, a dropped project, and quit, which was two bare `break`s with nothing between the event and the exit. The obvious basis was measured first and REFUTED: not one example project round-trips, from 279 differing JSON paths on `glowmere-valley-2-multicam` to 19,798 on `night-shift`, whose 33 KB file re-serialises to 1.6 MB -- a hand-written project records the parameters somebody changed and a save writes every parameter registered, so "differs from the file" fires on every project every time, and a prompt that always fires trains the reflex that dismisses it. This also corrects a claim the code makes about itself: `saveProject` and `nodeEditsAgainst` both say a round trip on an untouched project is byte-stable, which is true of the four keys they guard and false of the document. So the baseline is the serialisation taken at the last load or save -- the same builder, split out of `saveProject`, never a second serialiser written to match it. NO `bool dirty_` at every mutation site, whose failure is silent in the direction that loses work, and NO hand-maintained list of noisy keys either, whose forgotten entry is a prompt that fires when nothing changed: the drift is absorbed by MEASURING it, since on a sample where nothing touched the application every difference is by definition the engine's own. Measured drift with no input over 600 frames is 20 paths across eight projects -- `heroes[*]/position` and two `particles/visitor-beam` bases, ADR-386's writeback -- and 0 on six of them. Monotone once dirty, or a quiet second after an edit would absorb the edit. Costs 31 ms on the owner's film and runs at a close and otherwise only on idle frames, throttled to ten times its own measured cost. Cancel is a state machine rather than three `if`s because Save As is asynchronous, and the SECOND cancel -- in the Save As dialog that Yes opens -- lands exactly where the first one does) | Accepted |
| [441](ADR-441-the-cosmic-ocean-is-removed.md) | The Cosmic Ocean is removed, and the owner's eye is the acceptance test the suite cannot run (the owner looked at the shipped effect and cut it -- an art-direction call on a finished implementation, and the one class of judgement no test here can make. Everything the suite could say about it was green: it round-tripped, conformed, resolved into its own bucket, packed, drew, and its nebula pass had its own resolution lever with three arms proving the lever moved. None of that is evidence it was worth looking at. **No authored project referenced it** -- `tree-of-life-floating-island.json`, the project it was designed for and measured against, never named it, and the only files under `examples/` that did were the seven arms its own A/B harness left behind. The integration surface was 93 references across eight shared files, which is the receipt on ADR-500 rather than a refutation: the KIND cost four lines and left with its file, while what did not leave cheaply was the half ADR-500's preamble says the registry cannot collapse -- a new integrator, a GPU struct and a renderer pass, which is exactly what `EffectBucket::CosmicOcean` existed to name. Two guards paid out on the way through, both in reverse: the `static_assert` on `sizeof(AtmosphericFrame)` fired in the SHRINKING direction, 2400 back to 1640, having already caught the frame growing twice that day -- an assertion on `sizeof` catches a member leaving as well as arriving, where a checklist catches neither -- and ADR-392's exhaustive `switch` on `EffectBucket` made the dispatch arm a compile error to delete rather than a silent fall-through into a neighbour's bucket, which is the defect it was bought after. Removed alongside and not part of the ask: the panel offered the same kind TWICE, a hand-written Add button predating the registry sitting below the loop that already drew it from `schema->addLabel`, the two not even taking the same route into the scene; and the Add row ran off the edge of the panel because the loop was `SameLine()` unconditionally and got wider every time a kind was added -- ADR-500's promise of no edit in that file being kept, with the cost paid where nobody was looking. Fewer kinds is NOT the fix: the row wraps now, because the Tornado and the rebuilt Fog Bank will both put it back) | Accepted |
| [442](ADR-442-no-compatibility-shims-while-the-engine-is-still-being-built.md) | No compatibility shims while the engine is still being built (while AV Gen is in active heavy development, removing an effect, a kind or an authored block does **not** require a migration path: strip it from the tracked content that names it, in the same commit that removes the code, and write no load-time alias, deprecated-key reader, defaulted shim or version bump. The condition that makes it correct is that every file naming a cut effect is in this repository, authored by us, and reachable by `git grep`. A shim is worse than a strip for three reasons, and the cheapest one is last. It OUTLIVES the thing it supports, becoming code with no caller -- ADR-350's family, where the tests pass forever and nothing distinguishes working from never called. It CONSTRAINS THE REPLACEMENT, which is the expensive part: the Vortex retirement was first planned as retire-with-alias, and the alias meant the `/vortex/...` JSON block had to keep its name, which meant `world::PlacedMedium` in the new volumetric foundation would have carried a struct named after a deleted kind permanently, to serve files we were about to edit anyway -- the cost of a shim is mostly paid by the code that comes after it. And the migration is almost always SMALLER than the shim: the Cosmic Ocean appeared in zero authored files, so an alias for it would have been a compatibility layer for nothing. Four limits, written in so the rule is not read wider than it is: it does not license breaking ADR-360 silently (stripping an effect changes what a project renders, and that is a deliberate visible edit in a commit that says so, not a frame quietly moving); it does not license deleting without recording; it does not license skipping the grep, because "nothing uses it" is a measurement and not an assumption (ADR-385) -- a census run as a bare `git grep` for the word `vortex` returned EIGHT files when the answer was TWO, because `spatial::FieldKind::Vortex` drives particles in six of them and shares an English word with the atmospheric kind, so the instruction would have deleted six scenes' motion fields and looked like a successful edit; and it does not cover a user's own projects and renders. It expires the day somebody outside this repository has content worth preserving, and whoever notices should supersede it rather than quietly stop following it) | Accepted |
| [450](ADR-450-the-nebulae-get-their-own-resolution.md) | The nebulae get their own resolution, and ADR-390 §6's rejection is reversed (the Cosmic Ocean's 2.556 ms at 1080p is nebulae 1.44 / dust 0.79 / stars 0.13 / base 0.33, the components summing to within 5% of the independently measured total. ADR-390 rejected the ADR-139 reduced-resolution pattern on "~0.02 ms per five-octave fBM" -- a constant measured on `glowmere-cosmos`'s **2D value noise over a sin hash** and applied to `coFbm`'s **3D eight-corner noise with a domain warp in front of it**, which costs a second full fBM before the first is sampled. ADR-389's rule with "an fBM" as the quantity, and the second instance this week, so the general form is the keeper: a per-unit cost constant is transferable only between two things that are the same KIND of thing. Carries ADR-170's worked counterexample: the SAME quantity read as a median at load 20 gave nebulae 17% "not dominant" and as per-block minima in a quiet window gave 56% -- not two estimates of one thing, because on an M2 the GPU shares a memory bus with the CPU and the median was measuring the contention. The first reading was used to argue against this change and argued the opposite of the truth; `tools/ab_minima.py` and `ab_until_clean.sh` exist because of it. Only the nebulae move, because a nebula is the lowest-frequency thing in the frame and a star is the opposite -- reducing the whole effect would buy the same time and cost the thing it is for. Two attachments because the star strata composite BETWEEN the two nebulae, with coverage in alpha because coverage is what occludes them; RGBA16Float and not RG11B10Ufloat, which is renderable only behind an optional feature and whose saving does not exist at 2 MB; two bind group layouts because giving the reduced pass the textures it renders into is a read-write hazard a dummy binding would hide. ADR-393's boundary rule has an upsampling cousin and it was checked rather than asserted: ClampToEdge for the border, and `pixelAngle` from the direction's derivative is automatically 2-4x larger in the reduced pass so the shimmer fades MORE rather than aliasing -- at 20x amplification the quarter-res difference is speckle with no structure, no block edges and no seam. Quarter ships: it moves 0.83% of the frame against a control in which the nebulae themselves paint 80.94%, which is the row that stops the comparison being between two ways of rendering nothing. Measured end to end it takes the ocean from **2.556 ms to 1.835 ms** -- a saving of 0.72 where I predicted 1.35, because the prediction costed the term being REMOVED and left out the one replacing it: the pass clears and stores two attachments and every sky pixel then does two texture fetches to read them back, which is bandwidth and eats 0.63 of the 1.35. Same shape as the fBM error, one level up. So 1.2 ms is not met yet and the next lever is named rather than assumed: cosmic dust is now the largest component at 0.79 ms and is a 3x3x3 lattice, twenty-seven iterations over the whole sky. Found on the way: full, half and quarter rendered BYTE-IDENTICAL frames because an aggregate initialiser left the new `nebulaScale` field at its default -- three identical hashes is what caught it) | Accepted |
| [460](ADR-460-the-grain-is-the-jitter-and-the-shot-does-not-contain-the-cyclone.md) | Most of the grain is the march's own jitter, and the hero shot does not contain the cyclone (the architecture measurement behind ADR-461. The brief's premise was that the vortex reads as procedural noise because the march is too coarse; measured, the premise is wrong twice over. The march is already half-resolution, and quarter-res with 128 steps came out **1.85x MORE expensive** than what ships. The macro structure -- eye, wall, spiral arms -- belongs in the envelope where it is analytic and costs nothing to sample, not in the noise stack where it aliases. And the placement question is not about the vortex: measured off the shipped render, the island and tree occupy +/-15.0 degrees horizontally and -18.0 to +15.7 vertically of a 36x60 frame, so **the hero IS the frame**. A cyclone's features live in a horizontal plane and compress by sin(depression); this frame's steepest available depression is 26.8 degrees and everything at a useful one is behind the island) | Accepted |
| [461](ADR-461-the-jitter-was-the-step-count-and-the-hero-shot-cannot-hold-a-cyclone.md) | The step count was buying the jitter back, and the hero shot cannot hold a cyclone (**32 steps with the march's jitter off equals 256 steps with it on** -- grain 0.5172 against 0.5196, where the shipped 32-step-with-jitter arm reads 0.7312. ADR-389's step ladder was very largely the jitter's variance falling as the step it scales got shorter, not the noise becoming resolvable: the step count was buying back an artifact the march was creating. Ships as `Environment::volumeJitter` beside `volumeSteps` -- **not** in `QualitySettings`, because ADR-035 lets a tier scale sample counts, resolutions and history lengths and this is none of those -- default 1.0 so nothing moves, Glowmere byte-identical at 0 of 921600, and the expression centred on the step's middle rather than its start or the mean sample position moves and every per-metre coefficient goes with it. The ordered-instead-of-white instinct was REFUTED by measurement: interleaved gradient noise at the same amplitude is -11% where the field is smooth and **+8% on the shipped frame**, because where the field is itself aliased a structured pattern exposes error an independent one averages away. Also free: `radius`, `thickness` and `funnelDepth` scale together to give a geometrically identical field N times larger, because everything else is expressed in fractions -- an N-fold rise in effective sample rate against an unchanged 125 m step, grain 3.630 -> 1.458 at N=3. This demotes per-tier `volumeStepScale`) | Accepted |
| [480](ADR-480-the-editor-chooses-its-own-resolution.md) | The editor chooses its own resolution, and a resize stops being a seek (the multicam film ran at single-digit fps in the editor because the canvas renders at the display's BACKING SCALE, and ADR-084's fix for that has been a slider in Settings that nobody has ever moved -- the same diagnosis was filed twice, on two scenes, and both times the finding was "the lever exists, awaiting the owner". Measured first: `tools/resolution_sweep.sh`, six scales, three repeats, minima under the GPU lock, and the GPU frame is AFFINE in pixel count -- **4.63 ms + 8.45 ms/Mpx, max residual 0.24 ms** from 2.74 down to 0.69 Mpx. That supersedes ADR-137's deferral without contradicting its number: the local slope is **0.80** where an editor canvas lives and collapses towards 0 below ~1 Mpx, which is exactly where its 0.44 was taken at 640x400. `app::InteractiveResolution` picks a rung of `QualitySettings::renderScale`; it holds rung 0 unless the GPU is over budget AND is what the frame waits for (the Tree of Life shape, wall 18.42 against GPU 14.68, never reaches the guard), predicts with the naive proportional model because the affine law says that always UNDER-states the saving, and settles in <=5 decisions over 2,000 frames. Live editor only; Offline pins renderScale to 1 and `assertOfflineIsUncompromised` is the test. Measured end to end on a 3.98 Mpx canvas: GPU 40.89 -> 15.14 ms, FRAME 41.55 -> 18.26, `gpu.acquire WAIT` 33.73 -> 10.48, with the main thread's own 7 ms unchanged in both arms -- 24 fps to 55, and every millisecond out of the wait. Three defects were in the way: `setQualitySettings` was a plain assignment and `renderScale` is consumed only by `resize()`, so it was a SILENT NO-OP for every caller ADR-212's ordering fix did not reach -- the four new `scale*` arms would have reported 0.00 ms; `resize()` reset the PARTICLE POOLS through `resetTemporalHistory()`, so every splitter drag threw away a simulation Glowmere's motes need 30-50 s to build; and removing that call was NOT ENOUGH -- the screen reset also cleared `temporalScene_`, so the next frame concluded the scene had been swapped and reset the pools by that route, which only the probe found, still reading 101 alive out of 2,912) | Accepted |
| [481](ADR-481-the-mip-chain-paid-for-a-transfer-function-with-256-answers.md) | The mip chain paid `std::pow` for a transfer function with 256 answers (opening the multicam film spent **2.1 seconds uploading 58 textures**, 37 ms each against 15 ms for 869 mesh buffers -- the largest single item in the load waterfall and the question the previous investigation left open in as many words. It is not the GPU: `uploadTexture` builds the chain on the CPU and `downsample8` filters sRGB in linear light -- correctly -- by calling `std::pow` on every source channel of every level and again on every destination channel, ~1.3 M calls per 512x512 texture. The decode's input is a `std::uint8_t`, so there are 256 possible answers and a table of them is not an approximation of the function but its complete output: **not one bit of any mip level changes**, and that is checkable exhaustively rather than argued about, which is what the test does for all 256. The encode KEEPS its `std::pow` -- its input is a float with no small domain, and an interpolated table there would be a different mip chain for a saving a quarter the size, which is the §50 gate the decode does not need. Measured, three repeats, minima: **texUp 2145.02 ms/58 -> 851.66 ms/58, -60%, -1.29 s**. The saving lands on the FIRST FRAME, where time-to-first-usable-frame is measured; project load itself is unchanged at ~3.2 s because the upload was never part of it. Still on the CPU, still level by level, still a vector per level -- a compute pass or a cached chain beside the asset would take the rest, and both are a different size of change than one lookup table) | Accepted |
| [482](ADR-482-the-scrub-is-a-polyline-scan.md) | The scrub is a polyline scan, and the navigation grid was not the answer (a timeline click on the owner's film is acknowledged in 3.1 ms and answered in **2.8 s**, measured again in this build -- `engine.seek` median 2800.7 ms, all of it `entity re-sim`, 118,801 body-steps. ADR-273 bounded the work in the right currency and named `Navigator::sample` as the per-step cost; the leaf profile goes one level further and says **62% of an entire scrub is `closestOnPath`**, a linear scan over a river's polyline, run per terrain feature per height sample per walker per step of a 5,400-step replay. `explore` is 153 us/body-step against `ground` 8.5 and `wander` 13.6. **The navigation grid is not the answer and here is the number**: ADR-295's mechanism is switched off on this world and says so in the editor's log every run -- it disagreed with the world after 261 sampled walks -- and `charai_probe`'s agree arm says more margin does not cure it (17 unsafe at trust 2, 17 at trust 3, most of it the step test). So the prize was measured before the soundness project was costed: forcing trust takes the seek 3053.7 -> **2371.5 ms, 22%**, and moves a body **0.359 m**, against a control in which both arms are the same code and agree to 0.000 m. A scrub of 2.4 s is as unusable as one of 3.0, and a faster seek that draws a different frame is not a faster seek. What ships instead is a **cutoff** in the block accelerator: find the nearest block by box alone, walk it for a distance some segment actually achieves, and skip every block whose box is further. 3045.2 -> **2439.8 ms (-19.9%)**, with the four cap rows beside it byte-identical as a second statement that nothing moved. The first implementation of that argument was WRONG and the new test is the only reason it is known: `boxDistance` and the segment distance are different expressions, so when the nearest point on a segment is its own box's corner the box can round a few ULPs larger, the strict `>` skipped the block holding the answer, and the function returned FLT_MAX -- **heights off by up to 8.0 m on four terrain styles**. The comparison carries a one-sided slack now, which can only skip fewer blocks. `Feature::blocks` had been documented as "purely an accelerator ... identical result" since it was written and nothing checked it, because the determinism captures compare two runs that both use it; the new probe compares accelerated against unaccelerated **bit for bit** over 4,225 heights per style, with a control that collapses every box and requires the heights to move. Still too slow, and said so: `valueNoise` is the next 25%, and the editor also reports this world's walkable ground is in **four disconnected pieces with 49% unreachable**) | Accepted |
| [483](ADR-483-the-height-cache-and-a-teeth-check-that-passed-for-the-wrong-reason.md) | The height cache, and a teeth-check that passed for the wrong reason (93% of a scrub is inside `WorldMap::height`, which is pure in (x,z) -- so "memoise it" is a correctness argument of exactly the kind ADR-482 was written about, and it was measured before it was built. `AVGEN_HEIGHT_CENSUS=1`, counts only: the **load** phase is 1.82M calls at **3.0% perfect / 0.2% at 4K across 12 threads** -- a lattice swept once -- and **one seek** is 2.83M calls at **28.7% perfect / 26.5% at 4K on 1 thread**. Three unguessable facts decided the design: the seek is single-threaded so the path that benefits needs no lock; 4,096 entries take 92% of the ceiling from 32 KB because a walker re-samples its own neighbourhood and the repeats are TEMPORALLY LOCAL; and the load is worth nothing, so the cache must merely be harmless there, which `thread_local` achieves. **The prediction going in was near-zero and it was wrong** -- the model accounted for the walker moving 5 cm between steps and not for the fan of candidate directions within one step, which re-asks the same neighbourhood from one origin; the busiest coordinate was asked 1,102 times. Interleaved in one session, minima: **5892.1 -> 4591.0 ms, -22.1%**, and four times steadier. **The teeth-check passed for the wrong reason and was one sentence from being reported as verified**: removing the map identity from the entry's TAG left the arm green, because the identity is folded into the HASH INDEX too and a rebuilt map sends the same coordinate to a different slot, so the stale entry is never consulted -- only removing it from both produced the failure, 174 stale heights of 4,225. That is worse than ADR-182 as written: the probe CAN fail and DOES fail, but not for the reason under test, so it certifies a mechanism that is not the one carrying the safety. **Defeat every mechanism that could mask the fault, together, not one at a time.** Two near-misses also recorded: a pooled **62.4%** that averaged phases with opposite access patterns and was twice the truth, and a first timing that said the cache was **2x slower** and was load average 50. `heightUncached` is public so the slow path is something a shipping build still runs -- ADR-482's rule applied to this ADR's own code) | Accepted |
| [500](ADR-500-an-effect-is-one-file-and-four-lines.md) | An effect is one file and four lines (the world-effect registry: ADR-392 measured that ADR-387's two `constexpr` field tables collapsed three directions -- register, apply, capture -- while five hand-written per-kind lists sat beside them, `toJson`, `fromJson`, `sanitise`, the style presets and `defaultAtmosphericRoutes`, plus the panel's rows, so one aurora field appeared at NINE sites that had to agree by hand, two of them bare string literals. `EffectSchema` makes one row all nine at once -- parameter, modulation target, timeline key, preset member, save entry, apply, capture, panel slider, JSON key -- and every shared file iterates the registry instead of switching on an enumerator. `AtmosphereKind::` in `src/` goes from 59 references for 3 kinds to 49 for 5, half of them inside an effect's own file; `atmospheric_params.cpp` 574 -> 236 lines; `ui_logic.hpp`'s nine atmospheric row tables deleted. A new effect costs one file plus FOUR lines in two files -- the enumerator, its entry in `kAtmosphereKinds`, and a declaration plus a reference in `builtinSchemas()` -- and two more could have been collapsed by deriving the kind list from the schemas, which is refused because it would make "every enumerator has a schema" a check asking the schemas about the schemas. A forgotten piece is loud in two places: the `consteval` row factories take the accessors as required arguments, so a Float row that reads nothing is a compile error, and `checkRegistry()` names the kind and the row for everything else. ADR-392's headline defect -- a panel row naming a parameter nobody registered, drawing an empty box indistinguishable from "no such effect" -- becomes unspellable, because the row IS the registration. Conformance survives with its two load-bearing properties intact and now covers five kinds without a line added; three of its checks changed shape, each bought by a failure, including an enum reader that fired on CORRECT code because it matched an enumerator's spelling against a kind's file format and `MeteorShower` serialises as "meteors". Byte-identical: Tree of Life, both Glowmere projects and a hand-authored aurora arm, 0 of 518400 pixels each -- and the two Glowmere arms are stated NOT to be evidence, because neither authors an atmospheric effect, with `--disable volume` as the control that moves. Two proof effects added, a meteor shower (N records in the comet bucket, no shader change) and a volumetric fog bank (the placed medium `volume.wgsl` already marches, with swirl and throat at zero), which is also the honest boundary: a kind needing a NEW integrator needs WGSL) | Accepted |
| [520](ADR-520-weather-is-a-volume-the-camera-carries.md) | Weather is a volume the camera carries, and a particle may have a second life (the natural and weather family -- rain, snow, ashfall, dust motes, fireflies -- needed seven things ADR-015/040 did not have, and each one is the difference between a weather effect that works and one that does not. `volumeFollow` moves the emitter with the camera per axis and `volumeWrap` re-enters a particle on the opposite face, because weather is everywhere and a fixed pool spread over a square kilometre puts four drops in frame -- and because without wrapping a drop has to be born and to die inside the shot, so its opacity ramps at both ends and a field fading in at the top of the frame is the most recognisable tell of computer rain. A `CollisionResponse` of Splash is a SECOND LIFE rather than a burst: the drop is reborn in place as a ground-aligned ring expanding as sqrt(t), because the pool is fixed and eight slots per impact means a downpour with an eighth of the drops, and because a ring is what the eye reads as a landing where three additive dots flying up is not. `sizeVariance`/`sizeSkew` replace a hard-coded `mix(0.7, 1.3, r)`; `dragSizeBias` blends the drag towards `drag / size` so the particles drawn large are drawn fast and the two variations are CORRELATED the way they are in the world, which is the single biggest difference between snow that falls and snow that scrolls. One pulse with a `sync` knob covers both an ember bed (sync 0, every particle on its own phase) and a firefly chorus (sync 1) -- one feature, not two. `clusterCount` draws spawns from persistent centres keyed to the cluster index and NOT to the frame; the first version used the frame-mixing hash and the clusters resampled themselves sixty times a second, a uniform field wearing a costume. And a Henyey-Greenstein term against the key light makes dust visible for the right reason -- not because it is bright but because it is between you and a light -- after the first formulation, `1 + strength * (4*pi*hg - 1)`, was found by eye and then measured to be MULTIPLYING BY A NEGATIVE NUMBER: 74 lit pixels at strength 0, zero at strength 1, the motes not dimmed but clamped away. A phase function redistributes light; an artist control must only add it. `emitMaskField` gates a spawn on a scalar field so a weather front's edge is a density gradient rather than the wall ADR-025's Kill force gives, and an unresolved name leaves the mask OFF rather than zero, because `I typed the field name wrong` and `it is raining nowhere` must not look identical. Nothing here adds a pass, a binding or a march, and `src/world/atmospherics.*` is untouched) | Accepted |
| [521](ADR-521-the-warm-up-never-reached-the-renderer-that-ships.md) | The warm-up never reached the renderer that produces the output (`--particle-warmup` is one of the mitigations ADR-360 traded strict particle determinism for. It was built, capped, validated -- and applied to `Application::renderer_`, while `RenderJob` builds its own `SceneRenderer`. `--range 2:2 --particle-warmup 120` on the particle lab produced sequence hash e52b7fefaa395bcc, byte-identical to no warm-up, with three of the lab's four systems empty; only `burst` appeared, because a burst is added to the emit count directly while a continuous rate over one frame's dt is a sixtieth of a second of emission into a pool that starts at zero. The FOURTH time this defect has been found in this file -- `tier`, `disablePasses` and `qualityArms` each left a paragraph in `RenderSettings` saying so -- and it was hiding something larger: `--range t:t` is the idiom the whole repository looks at one frame with, and for every continuous emitter in every scene it was showing the first sixtieth of a second of the field rather than the field. Recorded and NOT fixed alongside it: `FixedStepClock::tick` returns deltaTime 0 on the first tick, so the first frame of every render has a shutter of 0 and therefore neither velocity stretch nor motion blur -- rain at `--range 3:3` is a field of round dots. The honest fix moves the first-frame hash of every render in the repository, so it is written down instead) | Accepted |
| [522](ADR-522-a-world-macro-was-a-knob-that-moved-nothing.md) | A world macro was a knob that moved nothing (`app::WorldMacro` is a named 0..1 knob with a list of remapped parameter targets -- exactly what a continuous cross-cutting control should be -- and on a freshly loaded project its routes did not exist. Found building Season Shift, which is one macro with 32 targets and which rendered to sequence hash 8b0100eb5f199e12 at macros/season 0.0, 0.45, 0.75 AND 1.0: the same bytes at every value of the only control the effect has. The load log said `0 routes` for a project declaring 32 targets, and `examples/machine/machine.json` -- which nobody was suspicious of -- loaded with exactly its 12 authored LFO and audio routes and none of its three macros' 8. Cause: `Engine::applyWorldMacros` installs them, and the SECOND `params::loadProject` pass sixty lines later replaces the authored routes with a predicate that keeps `fromGraph` and `fromEntity` and nothing else. The comment directly above that predicate describes this defect in the past tense, having been found and fixed twice for the two owners that were known -- which is the actual lesson: an allowlist of subsystems goes stale silently and every new owner is a fresh instance of the same bug. `ModRoute::fromMacro` is the third member, honoured in both directions (kept on load, skipped on save, because the owner rebuilds them and a saved copy would duplicate on every save). machine.json 12 -> 20 routes, season.json 0 -> 32. The test that would NOT have caught it existed and passed: all 32 target paths were already checked to resolve to registered parameters. Every path was real -- reachable is not the same as connected, which is §77's lesson one layer down) | Accepted |
| [540](ADR-540-the-motion-database-does-not-exist-and-the-clips-are-in-place.md) | The motion database does not exist, and every locomotion clip is in place (measured: the whole Glowmere alien corpus is **57.07 s** across 26 clips, 267 channels each, 156,921 keys, 1,712 frames at 30 fps; and `Walking` moves its root at most 3.2 cm in X and 2.7 cm in Z over its 1.03 s cycle -- every locomotion clip is authored in place, so the **future root trajectory** that is motion matching's central feature cannot be extracted from this data at all. The second blocker is that `Importer::importClips` fills only the rigs built in the same `loadGltf` call, so the engine cannot load a motion dataset even if one were licensed. Classical MM is therefore gated behind retargeting and a travelling dataset, and what is built first is contact/phase extraction, phase-matched transitions and inertialization -- which pay for themselves on the 57 seconds that exist. Also records a probe that was **correct and produced a wrong conclusion**: the textbook early-out measured 2.0-2.7x *slower* on a synthetic Gaussian fixture and 0.80x -- a 20% win -- on motion-like data with a near query, so a probe on a synthetic fixture proves something about the synthetic fixture) | Proposed |
| [541](ADR-541-a-motion-provider-that-remembers-lives-in-the-entity-tier.md) | A motion provider that remembers lives in the entity tier, not in `scene/` (every unit of the animation path is a pure function today and each says why -- the player stores *when*, the layer stack integrates nothing, the IK solver is analytic so it has no seed. Motion matching is inherently stateful, so `IMotionProvider::evaluate` takes a `MotionMemory` owned by the `Entity` -- reset with everything else and reconstructed by `EntityWorld::seek`'s fixed-step replay -- rather than keeping state on the shared `SkinnedRig`. Forces one representation change: `EntityState`'s polar `{speed, yaw}` cannot express "moving north-east while facing north", so it gains a velocity vector) | Proposed |
| [542](ADR-542-the-motionpack-is-the-boundary-and-it-carries-its-own-licence.md) | The motionpack is the offline/runtime boundary, and it carries its own licence (one versioned file crosses the seam and nothing else does, which answers three problems at once: there is no way to get motion onto a rig; `Composition` deep-copies all 26 clips per node instance at ~3.5 MB each, so clip memory scales with the cast; and licensing is the binding constraint on content. **100STYLE is CC BY 4.0 and is four million frames of stylized locomotion**, ACCAD is CC BY 3.0 direct from OSU, CMU permits embedding in a commercial product -- while AMASS, LAFAN1, Human3.6M, KIT-ML, AI4Animation and SAMP are all out, and every model in the MDM family inherits AMASS's ban on commercial training. `pack.json` carries a required `license` and the build **fails** without it. If a pack carries a model it is a flat versioned blob, not ONNX: measured on this machine, ORT's arm64 dylib is 41.8 MiB and 4-10x slower than Accelerate, Core ML pays a 0.23 ms dispatch floor per evaluation, a synchronous Metal round-trip is 275 us insensitive to its payload, and a hand-written forward pass is 23.9 us) | Proposed |
| [543](ADR-543-a-leg-is-three-joints-and-a-write-back-rule-not-an-ancestor-chain.md) | A leg is three joints and a write-back rule, not an ancestor chain (`PoseLayerStack::bind` refuses a foot chain whose joints are not each other's ancestors, and on `alien-scout.glb` all four namings an author would try are refused -- so no Glowmere alien has ever had a foot planted. Measured with `tools/motion_probe.cpp` against the real rig: a write-back that transforms a joint's parent **only when that parent descends from the joint the rotation pivots about** is faithful to **1.2e-7** with bone lengths preserved to 0.000000, while the shipped ancestor rule is off by **0.206** -- and its error is *exactly the requested displacement* in every arm, meaning it does nothing at all, which a null-request control arm scores as perfect. The bigger finding is the reach: the leg is **98.5% extended at rest** with **0.0098 of slack**, so a 10 cm step down is out of reach and needs 0.0902 of hip drop -- hip lowering is a precondition for downhill terrain, not an enhancement. Also records that `solveTwoBone` returns `Solved` with a 3.152e-4 lateral residual on a near-straight chain, which is this character's normal pose) | Accepted |
| [544](ADR-544-a-foot-that-cannot-reach-is-a-body-problem.md) | A foot that cannot reach is a body problem, not a solver problem (ADR-543 measured the Glowmere leg at **98.5% extension with 0.0098 of slack**, so foot IK without a body stage is upward-only and terrain adaptation is exactly the downward case -- and ADR-359 had already measured the nine farm rigs binding at 97.9% to 100.0%, so this is every character in the repository rather than one asset. Contact solving gains a stage in front of the limb: the stack gathers every foot layer's `ReachDemand`, `solveBodyCompensation` returns one translation by simultaneous projection onto the intersection of the reachable balls, the body joint moves, and the limbs then solve against targets that have not moved. Pure, allocation-free, **a fixed 8 sweeps rather than a tolerance loop** because a loop whose length depends on its input cannot be rendered twice, and it keeps the best sweep rather than the last because a per-axis clamp makes the sequence non-monotone. `Limited` (a limit that could be raised) and `Impossible` (two limbs pulling apart) are different answers. Measured on the real rig: the same 10 cm step down clamps short with it off and **arrives with the body 0.0902 lower** with it on, with `head.x` lower by exactly the compensation -- the difference between the character crouching and the leg stretching) | Accepted |
| [545](ADR-545-velocity-is-measured-once-not-authored-twenty-times.md) | Velocity is measured once, not authored in twenty places (`EntityState` described velocity as a scalar `speed` along a heading `yaw`, which can only describe a body travelling the way it faces -- it cannot say "moving north-east while facing north", and a walk and a backpedal are the same pair. The obvious fix, a `velocity` field every mover maintains, has **more than twenty writers** across the behaviours, the action tier, root motion, the director, crowd separation and grounding, and twenty writers of a derived quantity is twenty chances for it to disagree silently with the position it describes. So velocity is the **backward difference of `position()`, taken once**, after `applyOffsets` -- correct for every mover at once including the ones that do not think of themselves as movers. `speed`/`yaw` remain intent; `velocity`/`facing()` are measurement, and the difference between them is information. Cleared by `reset` like the root-motion sample beside it, and `dt <= 0` yields zero rather than dividing, because ADR-521 says the first tick of every render reports `deltaTime = 0`) | Accepted |
| [546](ADR-546-a-planted-foot-is-low-and-still-not-stationary.md) | A planted foot is low and still, not stationary (the textbook contact test -- near the ground and not moving -- is exactly backwards on this repository's content. Every locomotion clip is authored **in place** (ADR-540), so the body does not travel and the *stance foot* is the thing that moves, sweeping backwards under the hips at the authored stride speed while the swing foot comes forward. Measured: ground-frame speed found **zero** contacts on `Walking`, `Running`, `Walking_crouch` and `Walking_low_grav`, and a **100% duty cycle on `Flying_jet`**. Removing the root's travel does not help because there is none to remove -- an in-place clip has no ground frame. So planted = low AND not changing height, with the horizontal arm used only where the root genuinely travels. Three further measurements forced: the height band is 15% of the joint's own vertical range rather than an absolute distance, because 0.06 found a false plant in the hesitation at the bottom of `Walking`'s swing and because an absolute band means different things on a 1.66 m alien and a 0.11 m chick; a looping clip's contacts WRAP, so a span may record `start > end`; and a looping clip with one plant of the reference joint is cyclic over its own length, because a one-cycle walk plants each foot once and demanding two throws away the phase of exactly the clips phase matching is for. Phase is not a walk cycle: a one-shot gets one too and says `cyclic == false`) | Accepted |
| [547](ADR-547-a-transition-lands-on-a-phase-and-decays-an-offset.md) | A transition lands on a phase, and decays an offset instead of playing two clips (`AnimationPlayer::play` set `current_.start = now`, so a walk at 73% of its cycle cross-faded into a run's frame ZERO and whether the feet agreed was luck. Phase matching rebases the incoming clock onto the instant of equal phase using ADR-546's tracks and `PhaseTrack::timeAt`; measured on ramp clips that name the instant they are playing, the old path lands on 100.0 and the new one on 106.0. Inertialization replaces the cross-fade: only the INCOMING clip is evaluated, and the pose difference captured at the transition decays by `(1 + yt)e^{-yt}`, which is exactly 1 with zero slope at t=0 so there is no step and no kink. **The offset is recomputed rather than remembered** -- it is a pure function of (previous, previous start, current, current start, clips), which is the only form that survives a scrub landing mid-transition, and storing it would have been the one thing in this class that depended on how the playhead got there. The arm that proves only one clip is read: poisoning the outgoing clip after the transition instant changes nothing. Both features are opt-in and fall back to frame zero and to the cross-fade) | Accepted |
| [548](ADR-548-a-retarget-carries-the-channels-the-rig-actually-animates.md) | A retarget carries the channels the rig actually animates (the unit Phase 0 named as the blocking dependency for everything else -- before it a character's motion was whatever shipped inside its own `.glb`. A `RetargetProfile` is data: joint-name pairs, a root, a scale. Three measurements changed the design. **Rotation-only is wrong for this rig**: retargeting `alien-scout.glb` onto ITSELF reproduced orientations to 0.0396 degrees and missed `foot.l` by 0.38, because the alien is an Auto-Rig Pro deform export whose 89 joints all carry animated translation -- so translation is carried as a deviation from each joint's own rest, scaled by that joint's bone-length ratio, which is exactly the identity on identical rigs. **It is not scale**: the `*_stretch` bones were the obvious next suspect and `Walking` has 89 scale channels of which 0 vary. **It was the time base**: the last 0.08 was one frame, because a retargeted clip starts at zero and this pack's clips start at 1/30 s (ADR-204). Final: the real 90-joint rig reproduces to 0.0396 degrees worst and under 0.01 model units at the feet. Every arm that asserts bone preservation also asserts the target moved, because a retarget returning the rest pose preserves bone lengths perfectly and animates nothing) | Accepted |
| [549](ADR-549-model-space-is-the-ground-and-bvh-is-how-motion-gets-in.md) | Model space is the ground, and BVH is how motion gets in (the one shippable corpus Phase 0 found -- 100STYLE, CC BY 4.0, four million frames -- ships as BVH, which AV Gen could not read, so the open licensing gate led to a door with no handle. Adds a reader that respects per-joint channel order applied intrinsically, keeps `End Site` joints named after their parents, and parses with `from_chars` rather than `atof`. The first travelling content then found **two branches that were no-ops on every existing clip and wrong on every future one**: ADR-546's "ground frame" subtraction of root travel makes a planted foot move BACKWARDS in a travelling clip, so the horizontal contact test rejected all three plants of a three-plant walk -- model space is the ground in both kinds of clip, and the alien's numbers are unchanged to four decimals, which proves the subtraction had always done nothing. And `ClipAnalysis` measured travel at the first parentless joint, which on the alien is an armature wrapper no clip animates; it now uses ADR-337's rule, the lowest-indexed joint the clip translates. Fixture is a walk built rather than typed: hips advancing at 1.2 m/s with a foot held at a fixed world position through stance) | Accepted |
| [550](ADR-550-the-motionpack-is-built-and-its-licence-is-a-required-field.md) | The MotionPack is built, and its licence is a required field (what ADR-542 proposed, as shipped. `buildMotionPack` **fails** without a licence or a source -- not a warning, because a warning in a build log is how research assets ship, and the message says what to do rather than only what is wrong. Redistribution has **three** states and the third is the point: a reader that finds the field absent or unrecognised returns `RequiresReview`, never `Allowed`, because defaulting the other way turns a field nobody filled in into a shipping permission. Provenance is per CLIP and the worst state wins, so one forbidden clip makes a whole pack unshippable. Stores channels rather than ADR-542's sketched pose matrix, because a pack's clips have already been resampled once by the retarget or the import and doing it again is a second lossy step for no gain. Carries an FNV-1a skeleton digest so that playing a pack on the rig it was not built for is loud instead of silent. The real 26-clip Glowmere pack builds, validates PASS and round-trips with every phase sample intact) | Accepted |
| [551](ADR-551-a-foot-asks-the-ground-under-itself.md) | A foot asks the ground under itself, through an abstraction that hides where the answer came from (ADR-359 gave a foot a ground PLANE and ADR-544 gave the body a way to move when a foot could not reach -- and standing three aliens on terrain showed the second could never happen, because `LocomotionState` carries one plane for the whole body, `GroundFollower` seats the body on it, and `plantOnPlane` drops each foot onto the plane its own body is standing on. Reachable by construction. Measured on 0.3499 m of relief across a 0.9 m footprint: not one foot clamped. Now each foot asks an `IGroundQuery` under its own posed tip -- Phase B §17's abstraction, built as the abstraction rather than as the narrow four-points-in-the-seam version that was written first and replaced. Found two shipping bugs: `TerrainReject` answers "may a character WALK here", so treating a reject as "no ground" made a foot fall back exactly where the terrain was steep enough to matter; and an off-by-one gave the left foot the RIGHT foot's chain while the right read past the end of a two-element vector, which produced plausible output and was caught only by asserting the two feet get different planes) | Accepted |
| [552](ADR-552-a-clip-travels-when-its-root-leaves-a-box-the-size-of-its-body.md) | A clip travels when its root leaves a box the size of its body (ADR-546 makes the foot contact test conditional on whether a clip travels, and the switch was written against content that could not exercise it -- ADR-540 had established every clip here is in place. Net displacement over duration, the first reading, calls 100STYLE's `Neutral_FW` -- **131 seconds of continuous forward walking** -- a standing still: net **0.508 m**, 0.004 m/s, because a mocap subject turns round and comes back. Path was **93.030 m**. Cost, measured as worst foot slide in a contact span: up to **12.7x** worse, 0.662 m against 0.052 m on `Neutral_FR`. Path length is the obvious repair and breaks the other half, because an in-place cycle's root sways and sway has a length -- `Running` reads 0.330 m/s, `Fight_leg_kick_1` 0.535 m/s, both travelling, both wrong. Extent against the body's own rest height separates them, because an in-place root is BOUNDED however long it sways. Reports `travelAmbiguous` in the empty band 0.7-1.4 rather than presenting a guess as a reading: measured, this repository tops out at 0.568 and 100STYLE's least mobile file starts at 1.80) | Accepted |
| [553](ADR-553-a-rotation-retarget-cannot-animate-a-rig-that-is-not-a-hierarchy.md) | A rotation retarget cannot animate a rig that is not a hierarchy (24 clips of 100STYLE retarget onto the Glowmere alien with orientation error worst **0.056 deg** and bone-length error worst **0.000000**, and pack validation PASSes -- and the character cannot walk. Pelvis-to-ankle distance is **0.970 on every frame of every clip**, which is its rest value: the foot is welded to the pelvis, and the visible excursion is the lower body sweeping rigidly. The alien's own `Walking` varies 0.69-0.96 because the leg flexes. Cause is ADR-543's rig: `foot.l` is a SIBLING of `thigh_twist.l` and the mapped knee hangs off `rig` entirely, so the foot's only animated ancestor is `root.x` and no mapped rotation can reach it -- the alien's own clips move the foot by TRANSLATION. ADR-548 carries the channels the SOURCE animates, and BVH translates only `Hips`. Every quality gate passed because each measures whether the numbers written were the numbers meant, and none asks whether the joint moved. A.10b's answer as built is NO; the repair is positional retargeting through IK and is Phase B/C work) | Proposed |
| [554](ADR-554-a-seam-published-from-two-places-must-be-written-by-both.md) | A seam published from two places must be written by both, field by field (`LocomotionState` is published by `EntityWorld::update` while the timeline runs and by `::seek` when anyone scrubs, and ADR-360 requires they agree. Asked for the first time in Phase B which path writes which field: **two of three answers were wrong**. `velocity`/`facing` were written by `seek` and not `update`, so the seam carried **0, 0** during live play while the body measured **2.12, 2.12**. `action` was written by `update` and not `seek`, which replays the action tier and discards the result with a literal `(void)` -- and since `action` picks the clip, a body that sits while the timeline runs stood up and walked when scrubbed to; play published 'Sit', scrub published ''. `grounded` was written by neither. All survived because every test asserted on `Entity::state()`, which COMPUTES the value, and none on `Entity::locomotion()`, which CARRIES it -- ADR-553's shape exactly. The guard compares the struct field by field so the next field added is covered the day it is added) | Accepted |
| [555](ADR-555-phase-b-extends-the-layer-stack-it-has.md) | Phase B extends the layer stack it has, and puts the deciders somewhere else (§3 sketches a virtual `IMotionLayer`; AV Gen already has a data-driven, scene-serializable one in `PoseLayerStack`, with masking, weighting and per-layer diagnostics. Two parallel layer systems is §1.2's forbidden monolith rotated ninety degrees -- 'which system owns look-at' would have two answers. So Phase B adds `PoseLayerKind` values. And §3's nine layers are not one kind of thing: foot/terrain/balance/look/reach/secondary EDIT A POSED SKELETON and belong in the stack, while locomotion/speed/stride/turn/start/stop CHOOSE A CLIP AND A RATE and belong in a controller above it, which is §67's own diagram. `MotionContext` is the keyhole -- scene-tier and entity-free, because `pose_layers.hpp` includes nothing from `entity/` and that is what makes a layer a pure function a scrub can replay; `LocomotionMode` is a deliberate five-value projection of the nine-value `Activity` for the same reason) | Accepted |
| [556](ADR-556-a-motion-provider-advances-and-draws-in-two-calls.md) | A motion provider advances and draws in two calls, because a seek replays 5,400 steps and poses once (ADR-541 specified one `evaluate` that advanced memory and posed together; wiring it found that `EntityWorld::seek` replays at a fixed 1/60 step for up to ninety seconds -- **5,400 steps per body** -- and poses the rigs ONCE, so a single call would multiply the product's worst interactive cost by 5,400, while evaluating only at the target time breaks ADR-360 outright. So `advance` runs every step and may not touch a skeleton, and `pose` runs once per drawn frame and must be a pure function of the memory `advance` settled. Not a compromise: learned motion matching is already a Stepper plus a Decompressor. The memory records WHICH provider settled it, because a chain that re-selected at pose time could hand a matcher's database frame to the clip player. Proven reachable rather than asserted: `main`'s binary and this branch give the identical 60-frame sequence hash with the opt-in off and a different one with it on -- and the probe for it was **vacuous until a deliberate break revealed it**, because it asserted the producer's claim instead of the consumer's count) | Accepted |
| [557](ADR-557-a-foot-lock-anchor-is-derived-because-a-scrub-poses-once.md) | A foot lock anchor is derived, not remembered, because a scrub poses the rig once (§14 asks that a planted foot stay fixed while the body moves, and the obvious implementation remembers where it landed. That cannot survive a scrub: `EntityWorld::seek` replays the simulation at a fixed step and poses the rigs ONCE at the end, so a remembered anchor holds a position from a timeline the scrub abolished -- the same family as ADR-554's publish-path defects, avoided by construction rather than found after shipping. Instead: in the body's frame a planted foot slides backwards at the body's speed, so the offset is `-velocity * elapsedInContact`, both of which the seam already carries. Horizontal only, because holding the vertical would lift a foot off a slope. The derivation costs `0.5*a*t^2`, measured at **0.0475 / 0.095 / 0.190 m** for 1 / 2 / 4 m/s^2 across a 0.3 s stance -- which at Glowmere's actual accelerations is **1.6 px** and invisible, and only at the authored peak during a start reaches ~9 px) | Accepted |
| [558](ADR-558-a-timer-whose-expiry-has-no-consequence-is-a-control-that-does-nothing.md) | A timer whose expiry has no consequence is a control that does nothing (§22's attention hold expired, dropped its target, and re-acquired **the same target on the same frame**, because with a lone candidate it was still the most salient thing in the world. From outside it looked like a working selector -- the character WAS looking at the most salient thing -- while the dwell never reset, the ease-out ran to zero and stayed there, and the character never looked away from anything. The timer ran, the branch fired, and nothing downstream could tell; it would have been reported as 'attention feels dead'. Sibling of ADR-182: there a test that cannot come out the other way, here a mechanism that cannot change an outcome. Fixed with a refractory period, which is what makes 'I have looked at that' a fact with consequences rather than a value that resets. Margin and dwell are both required and measured to catch different failures: 600 frames of near-equal candidates give at most 4 switches against over 100 unguarded, and a two-frame 8x spike does not steal attention while the same rival sustained does. Ties break on identity, never list order, because order is an artefact of the perception rebuild and not a fact about the scene) | Accepted |
| [559](ADR-559-a-monotone-metric-is-a-direction-not-a-quality-measure.md) | A monotone metric is a direction, not a quality measure (§38's turn onset rises with the anticipation dial and contains no term for what anticipation costs, so it cannot have an interior optimum: read on onset alone, anticipation 1.0 is **strictly better** at 2.33 m against 0.87 m, and it is the worst setting available -- it cuts the corner by **3.19 m against 0.39 m**. The number is correct, the measurement honest, and following it ships the worst configuration as the best one. So a monotone metric needs a second measurement that moves the other way before it can choose a VALUE rather than a direction. Distinct from testing.md #20 (a question that cannot receive its answer) and #21 (a window too small for the quantity): this measurement is correct, complete and unopposed, and therefore a ratchet. The cheap test: ask whether the metric has a maximum in the interior of the dial's range) | Accepted |
| [600](ADR-600-a-gate-with-nothing-configured-refuses-nothing.md) | A gate with nothing configured refuses nothing, and knowing the hazard does not prevent it (§44's gate rests on 'an unmeasured metric never fails', which is correct -- otherwise every hand-authored clip is rejected for lacking a contact track nobody gave it. Its failure mode is that a caller supplying NO options disables the gate silently: `generateVariants` built its own default `MotionQualityOptions`, which names no contacts and no limbs, so every metric came back unmeasured, nothing could fail, and three deliberately broken variants passed. The validator ran, reported, and was incapable of rejecting. So: **who configures a gate is part of the gate**, and its metrics come from the caller. The larger finding is that I wrote 'a gate that only logs is the dead-knob family' and shipped a gate that could not reject **within the hour**, in the code written to prevent it -- the third independent arrival this session at *prefer the structural fix to the remembered one*, after the tornado agent's six renders and the fog agent's five entries. Awareness decays within the hour; a compile error does not) | Accepted |
| [601](ADR-601-the-solvers-reach-is-a-property-of-the-frame.md) | The solver's reach is a property of the frame, and on this rig the frame changes it (§45. `solveTwoBone` derives bone lengths from the pose it is handed, which ADR-543 requires -- a limb is three named joints, so the solver cannot consult a hierarchy that may not exist. On an ancestor chain that is safe by construction; `alien-scout.glb` is **flat**, every step of both solved chains a sibling hop, so it is not. Across 26 clips the arm's upper segment varies **21.4%** (14.3% on an ordinary walk) while both legs and the forearm read **0.0%** -- and the zeros are an **accident**, not a structure: all 26 clips carry translation channels on all eight joints and three simply never leave rest. Consequence: `maxReach` runs 0.5091..0.5398 m over `Walking`, a band of hand targets whose reachability depends on the frame -- 6.0 cm at cast scale, **0.52 px** in Glowmere's 170 m valley framing and **20.3 px** at character scale. Deliberately NOT fixed: the obvious opt-in lengths field is ADR-600 rebuilt by its own author one day later, and the blast radius is `assets/farm`, whose bulls bind at 100% of their own span) | Accepted |
| [602](ADR-602-a-layer-arrives-and-a-threshold-should-have-been-a-weight.md) | A layer arrives over a stated duration, and a parameter that snaps at a threshold should have been a weight (§46's vertical slice breached its continuity bound in six places and **nobody authored any of them**: 0.803 m from a layer switching on at full weight in one frame, 0.301 m from `strideRatio` stepping at a threshold, 0.231 m from the script's own terrain teleporting, 0.150 m from a weight sweeping in four frames, 0.098 m from a 0.8 m reach in 0.25 s, then 0.072 m which is the walk clip's loop seam and the floor. The blend is **derived from an elapsed time**, never accumulated (ADR-557: the pose tier poses once on a scrub) and elapsed rather than absolute (the stack runs on the rig's clock and the driver on the entity's -- the first implementation mixed them and **nothing failed, because nothing rate-limits a rig with a look layer yet**, which is a coincidence with a deadline rather than a correctness argument). The general lesson: *a neutral value is not neutral when the thing reading it scales by it* -- returning an identity value to mean 'I do not apply here' conflates what the value is with whether it applies, and the second question is a weight) | Accepted |
| [603](ADR-603-two-thirds-of-the-motion-stack-is-a-hierarchy-walk-and-it-cannot-be-hoisted.md) | Two thirds of the motion stack is a hierarchy walk, and it cannot simply be hoisted (§47's baselines: **2.16 ms per frame for a hundred characters**, +0.9% per character from n=1 to n=100, zero net heap blocks in the steady state. The number that does not fit the intuition is the aim layer at 4.68 us against a two-bone IK solve at 2.75 -- because every model-space layer calls `poseToModel` over all 90 joints to read one to three of them, seven times per character per frame. Measured rather than argued (ADR-385): one walk is 2.048 us, so the walks are **14.3 of the 21.6 us, 66%**, and the solves are the small part. Recorded mainly to foreclose the one-line fix: hoisting a single rebuild to the top would give the reach layer a hand from before the lean moved the spine, which is §5's ordering contract violated silently. The correct optimization is incremental, and the expected win is stated before the work so it can be wrong) | Accepted |
| [604](ADR-604-every-character-carries-its-own-copy-of-the-animation-pack.md) | Every character carries its own copy of the animation pack, and at a hundred characters that is 300 MB (§48. `SkinnedRig` holds `Skeleton` **and** `std::vector<AnimationClip>` **by value**, so every instance carries the pack it was loaded from. §47's timing could not have found this -- duplicated data costs memory and cache, not cycles in a loop that never touches the duplicates; reading the header did. On the shipping Glowmere scene: 14 rigs, 15.45 MB of skeleton and clip data, five 90-joint aliens at 3.01 MB each with **byte-identical** clips by FNV-1a over the key times and values, and **12.12 MB -- 78% -- is a second copy**. 12 MB is not alarming, so the recorded number is the extrapolation: a hundred characters is **301 MB of which 298 MB is the same bytes**. The fix belongs at load; what makes it more than five lines is that `analyse` writes `clipPhases` and `clipContacts` parallel to `clips` while `rootMotion` binds by index) | Accepted |
| [605](ADR-605-the-incremental-rebuild-landed-and-my-predicted-win-was-wrong.md) | The incremental rebuild landed, and the win I predicted was wrong by a factor of three (§53. `ensureModel` keeps `model_` valid across the layer loop and recomputes only what a previous layer wrote plus its descendants -- found by **diffing the pose**, not by consulting each layer's mask, because the mask is a claim and a layer writing outside it (testing.md #25, which has already happened once this phase) would hand the next layer a stale model space with nothing to fail. Stack 21.91 -> **14.1-14.3 us** per character; a hundred characters 2.16 -> **1.41 ms**; adding one more model-space layer 2.64 -> **1.05 us** against a 2.10 us walk. **ADR-603 predicted 4.9 us and was wrong by 3x**: the first rebuild each frame follows the clip sample, which changes every joint, so one full walk per frame is a floor. Writing the prediction down beforehand is what made the corrected model visible. §52's 335-quantity baseline is the correctness proof -- **none** of them moved, to 0.1 mm) | Accepted |
| [606](ADR-606-re-read-the-spec-not-your-memory-of-it.md) | Re-read the specification, not your memory of what you built against it (§49, and the third instance in one phase after §41 and §20/§22. Each time a stage this branch's own audit marked **done** was half-done, and each time re-reading the stage's own text found it. §49's second sentence -- no global randomness -- was satisfied; its **first** names `characterSeed` and `layerSeed`, and neither existed. Variation was hand-authored, one `phase` per character typed into the scene file, and **hand-authored spread is not a seed, it is the absence of one done by hand**. The failure is not carelessness: in each case the audit cited a true fact that answered a *neighbouring* question. Now `seedPhase` is a stateless bit-mixer, `seedPhase(0,0)` is exactly 0 so unseeded layers are byte-identical, and `driveLayers` seeds every layer from **names** rather than indices, because an index changes when someone reorders a scene file. §51's row-by-row audit is the model, and the rows that FAILED were worth more than the ones that passed) | Accepted |
| [607](ADR-607-a-requirement-whose-cost-lives-in-another-section.md) | A requirement whose cost lives in another section will be read as a preference (C §4 says the database should be "shared between character instances" -- housekeeping, until §6 four sections away prices it at **152 bytes/sample, 144.96 MB at a million**, which makes a hundred characters each holding one **14.5 GB**. The clause did not change; the number arrived from elsewhere and made it enforceable. A third failure mode distinct from ADR-606's neighbouring-question audit and ADR-182's blind probe: a requirement that is correct, unambiguous and **read as optional** because the section stating it contains no reason to act. ADR-604's mistake one tier up and two orders worse, and neither would come from a profiler -- a loop that never reads the duplicates does not slow down. The same rule then caught work an hour old: §15 demands **real** motion distributions and §17 names **1,700 frames, average and worst-case latency, build time**, and the §6 benchmark was synthetic, started at 10,000 and reported only a minimum. Rule for the rest of C: **synthetic data may time the scan; only real data may judge a filter or a first stage.** Real Glowmere: 1,738 samples -- §17's first scale IS the content -- 0.252 MB, best 8.88 / average 9.25 / worst 17.46 us, **955 characters per frame worst-case**, so **§16 is justified by the million case and by nothing in the repository today**) | Accepted |
| [608](ADR-608-a-declaration-that-satisfies-the-spec-and-nothing-that-consumes-it.md) | A declaration that satisfies the specification, and nothing that consumes it (C §10. `MotionFeatureConfig` carries exactly the seven weights §10 asks for, with defaults, serialised and round-tripped -- **the structure matched the spec perfectly and only the reads did not exist.** Five of seven had **zero uses** outside their own declaration; the two that were read were `> 0` presence tests, so `phaseWeight` at 2.0 versus 0.5 changed nothing. The search summed every dimension at weight 1: the opaque scoring function §10 forbids, with a tuning surface bolted to the outside. **Worse than an absent control, because an absent control is obviously absent** -- this one is present, documented and inert, so a tuner moves five dials, sees nothing, and concludes the FEATURES are wrong. It misdirects the next person's diagnosis. A fourth distinct shape alongside ADR-606, ADR-182 and ADR-607, and the same family as the field with no producer and the tag with no reader: **four instances across two subsystems**, and the test nobody runs is to grep the field name outside its declaration and count) | Accepted |
| [609](ADR-609-recall-is-not-a-quality-measure-for-a-search.md) | Recall is not a quality measure for a search, and the two-stage win came from the stride rather than the prefix (C §16. **Disclaimer before justification**: on real content the linear scan is fine -- 1,738 samples, 17.46 us worst case, **955 characters per frame** -- and §16 exists for the million-sample case at **36.5 ms per query**. Swept on real Glowmere motion: 94.4% recall sounds fine and **hides that its misses land 86.9% of the way to a random sample**; the 99.6%-recall plan at stride 8 is still 70.2% when wrong, so **raising recall did not make the failures benign**. ADR-559's family in a new place -- recall is monotone in effort with no term for how bad a miss is -- and the opposing quantity is severity measured against the database's own cost spread. The denominator took three attempts: a ratio reported **2957x** because a query sits near a sample so the best cost is near zero, and **a ratio whose denominator can approach zero measures the denominator**. The cheap prefix bought nothing: the whole win is the stride, and only stride 4 with the FULL prefix is safe -- 84 samples scored against 1,738, 99.6% recall, 3.1% worst case) | Accepted |
| [610](ADR-610-graded-continuity-was-measured-and-lost.md) | Graded continuity was implemented, measured, and lost, because a distance penalty makes standing still free (C §11. The binary term penalises a sample two frames later in the same clip exactly as hard as one from an unrelated clip, so grading by transition distance is the obvious reading. Measured over 300 steps of a matching loop on real Glowmere motion: **identical jump rate (0.50/s) at every rate from 0.5 to 4.0, and the loop stalls on 13% of steps against the binary's 0%.** The reason is structural -- a penalty proportional to distance from the current sample is **zero for the current sample**, so not moving is free and the matcher freezes. A freeze is not continuation, and it is the worst outcome wearing the appearance of the best because a frozen matcher also reports zero jumps. Kept at its inert default so a future attempt can see it was tried; any working version must penalise *not advancing*, needing the previous sample too. **The fixture was vacuous first**: it queried with `sampleNext[current]`, so continuity never decided anything and it reported 0.00 jumps for every configuration -- ADR-182 in a test an hour old -- and **the vacuous version carried the evidence of the freeze (index step 0.15 against 1.82) while reporting a perfect score**) | Accepted |
| [611](ADR-611-a-rate-metric-cannot-tell-nothing-bad-from-nothing.md) | A rate metric that counts bad events cannot distinguish "nothing bad happened" from "nothing happened" (three sections of C produced the same failure in hours: §16's recall silent on severity, §11's jump rate identical while the loop **stalled on 13% of steps** -- a frozen matcher reports zero jumps, the best possible score on the only number anyone would print -- and §17's rejected count, where an emptied candidate set gives a very fast meaningless query. Every bad-event rate needs a companion counting that something happened at all. Distinct from ADR-559 because the tell differs: a monotone metric recommends an extreme, **this one awards a perfect score to a dead system**, and dead is often the worst outcome rather than a neutral one. Also states the form past ADR-182 this session earned: §11's vacuous fixture printed 0.00 jumps for every configuration **and already contained the defect** -- mean index step 0.15 against 1.82, in the same table. **A probe that cannot fail can hold the proof of a real fault and hand it to you labelled as a pass**, so read the rest of the row before discarding the result. And a corollary from §12: a penalty that works is often **invisible in the winner's breakdown**, because its effect was on the candidate that lost) | Accepted |
| [612](ADR-612-the-provider-seam-does-not-blend-and-that-is-why-glowmere-has-it-switched-off.md) | **AMENDED -- its central inference was false; see ADR-613.** The provider seam does not blend, which is why `proceduralMotion` is off in Glowmere -- and it costs 0.36 m per transition (**if you are asking why the Glowmere aliens have the flag off, this is the answer.** The providers have parity with `AnimationPlayer` in steady state but implement **no transitions and no inertialization**, and Glowmere's aliens change gait constantly, so switching them on trades a blending player for a non-blending one. That caveat was recorded without a magnitude; Phase C §30's transition loop supplied one **by accident**, as the number both arms of a contact experiment agreed on: **0.3566 m mean foot jump at a transition, 1.6437 m worst** -- **21% and 95% of body height**, and **7.0x** the 0.0510 m a foot moves between two ordinary frames of a walk. Not a harness artefact -- `MatchMotionProvider::advance` records `transitionStart` and nothing reads it for blending. Unusually well-conditioned, because it was taken before anyone intended to fix it. Makes §32 the fix rather than a routine section, with the last outstanding Phase B integration item as its named beneficiary) | Accepted |
| [613](ADR-613-the-provider-seam-inertializes-and-what-it-costs.md) | The provider seam inertializes -- what that fixed, what it did not, and what it costs (ADR-612's fix, with three qualifiers that matter more than the headline. **The provider the product actually installs is not the matcher.** `MatchMotionProvider` is constructed nowhere in `src/`; `buildChain` adds only `ClipMotionProvider`, which was **0.4484 m mean foot jump at a gait change, 8.8x the bar** -- worse than the matcher's recorded defect -- because its header argued it need not blend since that is `AnimationPlayer`'s job, and the player is not called at all when a provider poses the body. Both now inertialize, sharing one implementation at the seam: matcher 0.0892 -> 0.0205 m, clip provider 0.4484 -> 0.0029 m, against a 0.0510 m bar. **The halflife is derived, not chosen** -- the size of the jump cancels out of the trade between a decay fast enough to move the foot and one still running when the next transition arrives, leaving only the soonest-repeat interval and the frame length. **The mean at a transition is unreadable alone**: an inertialized pose at the instant of a switch *is* the outgoing pose, so the foot is held one frame and the metric reads ~0 for any blend that merely defers the motion -- every table reports the worst single frame beside it, and on ADR-612's own forced cross-clip population that is still **0.4112 m, 8.06x the bar**. Depth is measured: the second blend slot halves the mean, the third buys 10%. Costs **6.0x on `pose`**, 3.43 -> 20.69 us per character per drawn frame) | Accepted |
| [614](ADR-614-the-scale-experiment-and-a-mechanism-it-could-not-test.md) | 20 at scale -- the stride plan survives, the mechanism that predicted otherwise is **untested rather than refuted**, and recall is the wrong instrument (100STYLE, CC BY 4.0, **434,478 samples against Glowmere's 1,738**. **This ADR retracted its own headline the day it was written.** Measured at 21 dimensions, stride-4 recall fell 99.0% to 95.2% while duplicates rose, and that was reported as refuting the proposed mechanism. Re-run at 33 -- the dimensionality every earlier Phase C density figure used -- **recall does not fall at all: 98.3% to 98.4%**, and the control reproduces the published 61.68% at radius 1.3945 exactly. The honest statement is that the prediction concerned a corpus the matcher *can* resolve and 100STYLE is **further under** the limit (radius over mean NN 0.95 to 1.71, duplicates 61.68% to 91.66%), so it cannot test it: **that needs a different weighting, not more data**. The lesson is one already on the record -- a comparison across dimensionalities is not protected by a constant artefact -- and the fix for it is to re-run the control, not to caveat it. What genuinely does not transfer is **the cheap prefix**: free on Glowmere, where it scored 94.8% against full-prefix's 94.5%, and **-23.1 recall points** at scale. Cross-clip coverage improves 3.53x to 1.90x, halving what ADR-613's residual must absorb. Cost spread transfers unchanged across 250x. **And §30's contact verdict is no longer scoped to a corpus that could not test it**: on 100STYLE's TR transition files contacts are NOT inert -- but the first run was degenerate, the search returning the seed sample on 600 of 600 steps so the arms agreed to four decimals INCLUDING the switch count, and the fix was a perturbation in the matcher's own units (the derived duplicate radius, 5.8x the fixed 0.05). With power, the two measures disagree: **-16.2% per transition and +7.5% over the run**, because contacts switch 28% more often. Not inert; sign unresolved. Also retires a statistic and gives its tell: **three orders of magnitude between a mean and a median means a denominator approaching zero** -- the third such ratio in Phase C) | Accepted |
| [615](ADR-615-the-character-motion-seam-is-staged-and-dark.md) | The character-motion seam is staged and dark -- one lab scene enables it, and that is one fact rather than six findings (**exactly one scene in the repository sets `proceduralMotion`, and it is a lab**, so the provider chain, the motion controller, vector intent, the trajectory seam, root-motion adaptation and trajectory prediction are all unreachable from the product **together and for one reason**. Sharpest instances: `CharacterIntent` has **no producer anywhere**, so `entity.cpp`'s `if (state_.intent.valid)` is dead and every character takes the polar path; `Entity::motionState_` is touched in exactly one place in the tree, `.reset()`; `MotionRequest::futureSeconds` has one reference, its own declaration, while `trajectory.hpp` claimed 'both ends are closed here'. **Ruling: document, do not wire, do not delete** -- wiring is a product change with a per-frame cost no spec section needs, and ADR-441's cut-it-outright is about *cut* features where these are *unfinished* ones. Each is commented where the symptom would be met, not in a phase log. **Its worst instance is `character_ai.hpp`, the file that declares itself normative and is read first before any character work: its status block told readers that six shipped subsystems -- masks, the layer stack, IK, root motion, `ISkeletonQuery`, `Entity::setSkeleton` -- do not exist, its four outstanding work items are all done, and the grep it instructs you to run to confirm 'no masks' now returns hits: a document handing you a falsification procedure that falsifies the document.** `behaviors.cpp` then cites that dead fact as the rationale for having no head movement, so a reader builds a second whole-body approximation -- **stale text causing new bad code, which is the tier above wasting time or causing a misdiagnosis**. Adds a tenth shape to the catalogue: `LocomotionState::grounded`'s comment still said 'written by neither and read by nobody' after the write half was fixed -- **a stale description of a repaired thing, which misleads in the opposite direction from the other nine and is invisible to a dead-symbol sweep because the symbol is alive**) | Accepted |
| [616](ADR-616-resolution-is-a-weighting-and-recall-measures-identifiability.md) | Resolution is a property of the weighting -- and the recall instrument measures identifiability, which is why the mechanism still cannot be tested (**one dial moves the corpus from far under the matcher's resolution limit to just under it**: `rootVelocityWeight` 1 to 16 takes duplicates 85.95% to 58.61% and radius/meanNN 1.47 to 0.97, so ADR-614's 'testing it needs a weighting, not more data' is confirmed for the first half. **The second half fails**: between weight 16 and 64 the resolution does not change and stride-4 recall falls 100% to 83.6%, while the *seed-return rate* falls 96.5% to 19.9% -- so what moved recall is a property of the probe, not the corpus. **The instrument's discriminating power and the corpus's resolvability ride on the same perturbation**, so this design cannot vary one and hold the other; the mechanism needs queries that do not come from the database at all. Also fixes three instrument defects found by running it against itself: `duplicateRadius` was an **unweighted** length compared against a **weighted** distance under a comment asserting they shared a space (Glowmere 61.68% to 57.71%); the first weight sweep was a **dead control**, assigned after the database was built, printing the weight it was given and producing four identical arms; and the plan perturbation was fixed rather than derived. Robust across everything: **the cheap prefix costs 17 recall points at scale** where on Glowmere it scored exactly what reading every dimension scored) | Accepted |
| [617](ADR-617-documentation-that-cannot-rot.md) | A document that tells you how to check cannot go stale the way one that tells you what is true can (**0 of the 12 `file:line` coordinates in `character_ai.hpp`'s normative inventory land on the type they name** -- one points at a blank line, one at a closing brace -- and none was wrong when written. Its §2 heading, *'Perception: the layer that genuinely does not exist'*, had become a **build instruction to write a second `GridPerception`** in the file whose whole purpose is stopping parallel agents from doing that. **Patching lost three rounds** because of a structural fact the audit named: *the copy that goes stale is the one furthest from the code being changed* -- when ADR-274 falsified `"zero call sites"`, the two copies beside the code were rewritten and the normative header was not. Deleting would also lose; the file has a real job. **So §0 and §2 were rewritten out of the class of statement that rots**: purpose and invariants instead of inventory (an invariant is falsified by a design change someone does on purpose; inventory by an edit nobody notices), **the grep instead of the answer** -- a line saying 'run this to find the implementations' cannot be wrong about how many there are -- names never coordinates, old text kept as history, and the reason written into the file so the next person tempted to add an inventory meets the argument first. The idea is not new to the file: **the old text told you to grep for `jointMask` to confirm masks did not exist, and that grep returns hits -- the document supplied the evidence against itself and nobody ran it.** Companion rule from ADR-616: **a sweep prints its arms, because a dead knob is invisible in an aggregate and obvious in a column**) | Accepted |
| [618](ADR-618-a-save-turns-fifteen-layers-off.md) | A save turned fifteen layers off on the shipping cast -- a key the parser read and the writer never wrote (`PoseLayer::weight` was parsed and never serialised. **Both halves were individually careful**: the parser keeps an authored weight only for `Manual` layers, because a driven layer's is per-frame state; the writer treats weight as per-frame for *all* drives, which is true except for the one case the parser singles out. `"weight"` is in `kLayerKeys`, so no ignored-key warning fires. **Live**: `glowmere-valley-2-multicam` authors fifteen manual layers at weight 1.0 -- both stride warpers and secondary motion on all five aliens -- so one editor save turns them all off, in a file that still loads cleanly, and the layers resolve `Inactive`, which nothing distinguishes. **The mirror of every other finding in this programme**: those were fields written and never read; this is a field read and never written, and it destroys authored work rather than wasting memory. The write is placed *before* the loop's Foot/Reach early-out because there are two `push_back` sites and a key added to one is a key the other drops. Teeth-checked at 30 of 55 assertions. Turned up a second defect, recorded not fixed: **`fromJson` on a `toJson` document cannot reload a scene with relative references**, since the document carries no path) | Accepted |
| [619](ADR-619-publishing-an-activity-is-not-publishing-a-turn.md) | Writing the activity is not publishing the turn -- a standing character rotating at 120 deg/s was classified `Idle` on 599 of 600 frames (`LookAt` turns the body and announces it by writing `state.activity`; `Gait::select` discards a *locomotor* proposal on purpose and re-derives the gait from `speed` and `turnRate`, because hysteresis must live in one place. Both correct. But **`LookAt` was the only turner in the engine that never wrote `turnRate`** -- the `turn` helper, `Spin`, `Wander`, `Explore` and the action tier all do -- so a body standing and rotating arrived as speed 0, rate 0, and came out `Idle`. Measured: **599 of 600 frames turning, 0 classified Turn, 599 Idle; after, 599 Turn and 0 Idle** -- a classification inverted on every frame, not a margin that moved. `guard-post.scene.json`, whose sentry stands at a post and turns to look at things, is the scene whose stated purpose it defeats; `lookAt` is authored in eleven. The guard is the decision, not the assignment: `LookAt` claims the activity only when nothing else has, so it claims the rate on the same condition. Second instance in one day of testing.md #39 -- two locally correct decisions composing into a defect neither side's tests can see. Records an unfixed companion: `turnRate` is never zeroed per frame, so a `lookAt`-only character can also read a stale rate from a behaviour that stopped turning) | Accepted |
| [620](ADR-620-an-authored-acceleration-is-a-limit.md) | An authored acceleration is a limit, not decoration -- applied only where it was authored (`GaitSettings::accel`/`decel` were parsed, stored and written back, and read by nobody for a behaviour-driven body. With `rook`'s own numbers the **worst fall was 3.0685 m/s in one frame against an allowed 0.1103, 28x**: the whole walking speed lost in a single frame. Applied centrally after every speed writer and only where authored, with the defaults unchanged. Three near-misses: **a zero default would have frozen every action-driven body**, because `approach` returns the speed unchanged at rate 0; **scrub broke even though the limit ran on both paths**, because `drift` is rebuilt in 2 steps and a ramp needs about 41, so the replay depth was what mattered; and **the serialiser wrote the defaults unconditionally, so the first save would have promoted every body to authored**. The ramp exposed `playbackRate` keying on the gait's activity rather than the body's speed, and two test detectors that counted acceleration as failure (ADR-622)) | Accepted |
| [621](ADR-621-the-suite-was-passing-the-emptier-world.md) | A test's answer depended on what ran before it -- and it passed in the configuration where the world was emptier (`test_abduction_poc` failed in the full suite and passed alone. **First hypothesis was RNG sensitivity and it was wrong** -- it passes alone under the suite's own seed, checked. An order bisect found that *a single preceding test, any of several*, reproduced it exactly: a culprit category, not a culprit test. `scene::generatorRegistry()` is a process-global filled only by `Engine`'s constructor and emptied by `clearGenerators()`, which two tests call, one from a **destructor**. This test builds no `Engine` and loads a scene with **forty mushroom generators**, so alone it produced no geometry and **the obstacle field was missing forty obstacles** -- while the test asserts animals do not walk into things. **The suite was passing it in the configuration where the world was emptier**, which is worse than a wrong measurement because nothing about a pass invites inspection. Fixed by registering what the scene needs and asserting it; now deterministic at 2,628 stalls in every order. Determinism then revealed an honest failure: the 4.0% is ADR-620's ramp, longest stall **0.43 s**, and the detector cannot tell *accelerating* from *stuck* -- the same definitional gap as the farm pack's `frozenWhileMoving`, left open deliberately) | Accepted |
| [622](ADR-622-one-definition-of-stopped.md) | One definition of "stopped" -- two detectors, a rate floor and a threshold that had never been introduced (after ADR-620 made acceleration real, `stalledPercent` and `frozenWhileMoving` both went red because both were written for a world where a body was at speed or stopped and never between. **Three parties held private definitions of one word**, the third being the farm pack's `rateMin: 0.005` against the frozen detector's own literal 0.02, so a body could be officially playing its clip and officially frozen at once. Fixed with one definition read from one place: `withinAuthoredRamp` -- **a body inside its authored ramp is accelerating, and accelerating is not stalled**, an exact question because `approach` moves the speed by exactly accel*dt while climbing -- and `kVisibleClipRate`, one number for 'advancing enough to see', derived from 1.2 frames of clip per second at 60 fps. **Both detectors to zero, no residue.** The floor is labelled as a compensation for the farm assets' missing idle clip. **Decided by the owner, 2026-09-21: option B**, the farm floors raised to 0.02 across 80 bodies in five live scenes, leaving the ADR-262 control and a render snapshot untouched. Measured cost: worst over-travel of the legs goes from 0.27 to 1.08 mm per frame, about 4x in the tail and +37% at the median; the legs already outran the body before the change. Guarded by a test that the floor stays visible and survives a save) | Accepted |
| [623](ADR-623-the-matcher-is-wired-opt-in-with-the-clip-provider-behind-it.md) | The matcher is wired, opt-in per character, with the clip provider behind it (supersedes ADR-615 for the fallback seam; owner-delegated ruling. One `motionMatching` key; the database is built at load and shared per skeleton (ADR-650); each §35 failure falls through to the clip provider, and a healthy matcher, shown by a control arm, does not. The wiring found four defects: the chain handed one provider's memory to another; **a seek never replayed provider memory at all**, advancing it once after the replay, so every scrubbed provider pose disagreed with play; the chain was built after the first advance; and requests were compared in world units against a database in asset units. Default off is shown by a Glowmere state digest equal to the pre-wiring commit's, with a control arm that differs) | Accepted |
| [650](ADR-650-retarget-offline-once-per-skeleton-and-share-the-database.md) | Retarget offline once per distinct skeleton, and share the database among characters on that skeleton (Phase C §42/§43, measured on the six Glowmere aliens: a runtime retarget costs **197 µs per pose against 2.33 µs** for pre-retargeted motion, **84.5×**, which is 710 ms of CPU per second at 60 characters; all six aliens share one skeleton digest, so one pack and one database serve the whole cast. Revisit if many distinct skeletons arrive; the strategy test re-opens it if the retarget gets within 5×) | Accepted |
| [560](ADR-560-the-fog-bank-has-no-shape-and-the-second-medium-is-a-silent-no-op.md) | The fog bank has no shape, and the second placed medium in a scene is a silent no-op (a fog bank and the Cosmic Vortex authored together render **byte-identical** to whichever of the two is listed first -- the other contributes not one pixel, and `AtmosphericCounts::dropped` has exactly one reader in the tree and it is a CPU conformance finding, not the UI, against two comments that say it is "reported ... not a silent no-op". The brief's §3 premise is wrong: a fog bank's envelope is `smoothstep(0,0.22,rr) * rim(rr) * exp(-y^2/t^2)` -- monotone in radius, uniform in angle -- so with the noise off it is a grey card and with it on it IS the noise, which is ADR-460's finding one effect over and worse, because the vortex has macro structure now and a fog bank has none of it. **Eight times the steps does not reduce the grain** and on the exposure-normalised figure raises it 8%; the march's own jitter is worth -16.5% at identical exposure and the fBM stack +27% at matched exposure. Also: the grain metric has an **exposure sensitivity ADR-389 does not mention** -- a density ladder that adds nothing but density moves it 15% -- and every fog bank has an unclosable 22%-radius hole at its centre because `eyeWallWidth` defaults to the vortex's 0.22 and is not a row on the fog panel) | Accepted |
| [561](ADR-561-every-fog-bank-shipped-with-a-hole-in-it-and-the-control-that-would-have-shown-it.md) | Every fog bank shipped with a hole in it, and the one control that would have shown it (two live defects, fixed ahead of the rebuild and recorded separately so they can be dated rather than disappearing into a redesign. **`innerVoid = 0` does not remove the eye**: the term is `smoothstep(innerVoid, innerVoid + eyeWallWidth, rr)` and `eyeWallWidth` defaults to the 0.22 ADR-374 hardcoded for the vortex, which `applyStyle` never reset -- so every fog bank's density climbed from EXACTLY ZERO on its axis to full at 22% of the radius, a 200 m clearing inside a 900 m bank with no row on the panel to close it. It survived in **three** places and the test found the two that were not obvious: the preset, `radialProfile`'s `max(w, 1e-3)` and `packVortex`'s floor. A width of zero means NO EYE now, in `core/vortex.cpp` and `shaders/vortex.wgsl` together. Second: **`cloudNoise` was declared by the vortex and not by the fog bank**, so §4 D, §44 bar 1 and the Definition of Done -- all one parameter -- were unreachable from the fog panel and every diagnostic arm had to be hand-authored into JSON. Third, and why the tests can exist: the `world::Vortex` -> `vortex::VortexField` conversion lived inside `volume_renderer.cpp` and nowhere else, which is ADR-401's finding one call site earlier; it is `world::vortexFieldOf` now) | Accepted |
| [562](ADR-562-the-march-carries-media-not-a-vortex.md) | The march carries media, not a vortex (the fix for ADR-560's headline defect and the shared volumetric foundation. `AtmosphericFrame` carries `MediumSlot media[4]` -- 16 `vec4` of packed lanes and a kind tag -- in place of `bool hasVortex; Vortex vortex;`, with per-kind packing on `EffectResolve::pack` so a new medium is still one file and four lines. **Lanes rather than a struct per kind** because the frame already carries `CometGpu`/`AuroraGpu` and the vortex was the exception, and because an array of `vec4` deletes the padding-comparison bug `frameDiffers` has already had once. **ADR-390's refusal to pack in the world was checked rather than inherited**: it rested on `packCosmicOcean` taking a quality tier, and neither `packVortex` nor `packTornado` does -- the reason an ADR gave is not the same as that reason still being true. Each slot gets a per-pixel **vertical-cylinder** ray interval, because every term in this family's field is a function of `length(rel.xz)` and `rel.y`; steps outside it cost a comparison instead of a field evaluation. Two independent measurements say a global step count is WRONG rather than slow -- ADR-560's 7% from a smaller footprint, and `agent/tornado`'s 24 m column broken into three blobs at 31 m spacing. Also: `engine.cpp` held a THIRD copy of the vortex conversion assigning **17 of 24 members**, publishing a field with no eye wall while the march drew one, undetected because there are **zero flow subscriptions of any kind** in authored content) | Accepted |
| [563](ADR-563-a-fog-bank-has-a-shape.md) | A fog bank has a shape, and structure without appearance cannot be evaluated (the brief's §46 B. `shaders/fog.wgsl` gives the bank an elliptical rotatable footprint, an asymmetric vertical profile built from a base and a falloff, and edge softness -- all analytic, no noise, which is a constraint on the file rather than a description. ADR-560's inverted assertion is FLIPPED: the field varies with angle at detail zero, and making the footprint circular again drops the spread to 1.19e-07 and fails the case. Also: **ADR-562's kind tag was never uploaded** -- set on the CPU, compared by `frameDiffers`, dropped by the renderer, so the slot contract advertised a tag no shader could read. Built-but-unreachable one day old inside the foundation written to fix that family, which says the family is **the default outcome of writing a producer and a consumer in separate commits**. And the SECOND ordering inversion: §46 C is deferred after §23/§24, because its primitives are judged by eye and five primitives that read as solid slabs are five slabs -- a spec's ordering is a hypothesis about the codebase) | Accepted |
| [564](ADR-564-fog-density-is-an-optical-depth.md) | Fog density is an optical depth, so a preset can ship one (§23/§24. `density` was an extinction PER METRE, so the optical depth through a bank scaled with its SIZE: the shipped `Valley Mist` 0.0016 gives 0.45 at a 100 m radius and **4.03 at 900 m** -- invisible to opaque slab, across a range an artist crosses by dragging the RADIUS slider. So **no preset could ship a correct density**, and every fog arm rendered during ADR-560 and 563 was a wash or nothing at values the presets themselves ship -- the shape was fine and the unit was wrong. Now normalised by the bank's long-axis crossing, so 1.0 means "you can just see through it" at any size. Break demonstration: restoring per-metre gives depths of 800 / 3200 / 7200 where the fix gives one constant. Also a trap: comparing two sizes by whole-frame mean luma reads 49 vs 84 and looks like a refutation, because that metric **conflates coverage with opacity**) | Accepted |
| [565](ADR-565-the-detail-is-one-multiply-and-the-twin-needs-a-witness.md) | The detail is one multiply, the twin needs a witness, and the reserved lane needs a check (§13/§14/§44. The macro detail is `1 + amount * (n*2 - 1)` -- identity at zero, mean-preserving BY CONSTRUCTION where the vortex's `mix` raised the frame's mean luminance by 20.7 levels with the detail OFF, and one octave at a period tied to the bank's radius. The CPU/GPU twin gets a parity test because the two drifted for ten minutes within a day of being written, and noise is where a transliteration drifts SILENTLY -- it looks like a tuning choice. Lane 15's reservation becomes a sentinel written before `pack` and checked after: the tag is written after, so the tag wins and the PACKER's value disappears, which is a wrong appearance rather than a wrong shape. And a dispatch silently invalidates every probe aimed at the pre-dispatch path -- three in one file, one of them guarding a shipped fix. **Written after the fact: seven files cited this number for a day before the file existed**) | Accepted |
| [566](ADR-566-a-fog-volume-is-a-primitive-you-place.md) | A fog volume is a primitive you place, and a bound is a claim the field can refute (§46 C / §9. Five local primitives -- sphere, ellipsoid, box, capsule, cylinder -- behind one NORMALISED distance: 1 at the surface, zero past 1.35, so the rim, the early-out, the ray interval and the CPU twin all keep working unchanged. `FieldType::Choice` is a float index everywhere a number is wanted and a NAME in the file, because appending a primitive must not change what every saved bank is. And the larger half: the march's ray-interval bound was a CLAIM nothing checked, short by `bankLength` horizontally (**71% of peak density outside it** at the slider's end) and using a GAUSSIAN's three-sigma vertically for a profile that is now an EXPONENTIAL (**56% above the ceiling** at low falloff) -- both exactly right at the defaults, which is why a day of rendering never showed either. A generous bound costs only field evaluations; a tight one deletes part of the medium invisibly. First frame in the fog work that shows a placed volume rather than a wash) | Accepted |
| [567](ADR-567-one-height-layer-two-readers-and-a-test-that-they-agree.md) | One height layer, three readers, and the test that they are a function and its integral (Phase D's first step. ADR-058 has the volumetric march multiply by a height profile and the surface fog integrate the SAME layer with a closed-form antiderivative -- two expressions in two files that must be a function and its integral, and **nothing checked the relationship**: the existing cases test the sign and the degenerate case, and both pass against any monotone F. Grepping for the expression rather than the feature found a THIRD reader, `particles.wgsl`, restating it inline. Now one definition in `height_fog.wgsl` (no bindings, so a test can compile it alone) and a GPU case that integrates the profile numerically and compares. **It also constrains the phase**: §7's Height Curve cannot be `exp(-(bd)^k)`, which has no elementary antiderivative -- the curve must be a linear blend of families that each integrate. Break demonstrations in metres of air: dropping the `max` gives 110 against 143.8, an antiderivative off by a constant gives 21.4 against 75.0) | Accepted |
| [568](ADR-568-the-height-layer-gets-a-floor-and-a-top.md) | The height layer gets a floor and a top, and both had to be integrable (§7. The engine had ONE exponential, which never reaches zero (no clear atmosphere above the mist) and always reaches zero (no global haze floor) and has one shape (no curve). Two of §7's six controls already existed under other names -- Height is `fogHeight`, Ground Density is `volumeDensity`. The other two are `fogUpperDensity`, an affine rescale, and `fogHeightCurve`, a **linear blend between two families that each integrate** -- the exponential and a compact quadratic that reaches exactly zero at 2/falloff -- because ADR-567 established the surface pass integrates this layer and `exp(-(bd)^k)` has no elementary antiderivative. Defaults bit-identical, asserted on the GPU rather than argued. Measured on the sky band of one frame: exponential 201, haze floor 231, compact curve **142** -- a definite line above which there is no fog, which no setting of the exponential produces) | Accepted |
| [569](ADR-569-two-fogs-one-atmosphere.md) | Two fogs, one atmosphere, and they cross at 79 metres (§5/§6. A FINDING, no behaviour change. The engine renders fog twice with two laws and two independent densities: surfaces get `exp(-(d*fogDensity)^2)`, exponential SQUARED, and the march gets Beer-Lambert `exp(-volumeDensity*absorption*d)`. ADR-058 made them share the height geometry and nothing has made them share the density or the law. They agree at exactly one distance -- `volumeDensity*absorption/fogDensity^2` -- and disagree everywhere else. Median crossover **79.3 m**. The census is `tools/fog_law_census.py`, in a file because two people ran "the same census" and got 120/57/57/0 against 90/30/23/1 -- neither wrong, differing on two invisible axes: whether `_`-prefixed probe scenes count as shipped, and whether a key's PRESENCE or its NON-ZERO VALUE counts as using a law. Reconciled: of 90 shipped scenes, 30 have both laws active, 23 the surface law alone, and 1 the march alone -- `volumetric-atmosphere-lab.scene.json`, the lab built to exercise the march. Both laws are in use, so an inference that one never ships does not survive the count, and option 2's migration is 53 scenes and not 36, and in `glowmere-valley-2` a surface at 400 m is 99.2% fog colour while the medium in the same 400 m passes 38%, a **48x** disagreement about the same air. Squaring the exponent makes extinction grow linearly with distance, which IS §7's Horizon Density, unlabelled, in a pass that was not thinking about it. Three ways out are set out; §7's last control is blocked until the owner rules) | Finding |
| [570](ADR-570-the-shared-self-shadow-march.md) | The shared self-shadow march, and what it costs (§20/§22. The march had NO shadowing, so a bank was exactly as bright on the side facing the light as on the side away from it -- §20's backlit and moody fog unreachable by construction. Now each sample marches a short ray toward each light that lights the air, through the placed media's own density, dispatching through `mediumShape` so every kind gets it and `agent/tornado` does not write a second one, and clipped by ADR-566's bound so a ray far from any medium evaluates no field. It is §22's answer with no second system: a shaft out of the same density. Measured asymmetry 0.018 -> **0.134** with the far side falling 5.6x. OFF by default because interleaved minima say 8.13 ms -> 23.00 / 33.36 / 56.82 at 2 / 4 / 8 steps with three lights -- and the cost is LINEAR IN THE LIGHT COUNT: one light is +10.6 ms where three are +31. Which surfaces a finding: `volumetricStrength` defaults to 1.0, so every light in every scene lights the air, a 3x multiplier nobody chose. An early-out was tried and measured at +1.57/+0.85/-0.78 -- mixed sign, no effect established, removed) | Accepted |
| [571](ADR-571-the-fog-moves-through-the-world-and-its-density-has-a-curve.md) | The fog moves through the world, and its density finally has a curve (§15/§16/§24, and §17 blocked. The macro detail drifted by an offset in NOISE space: the direction was hard-coded, so §16's first control did not exist, and the speed was divided by `detailScale` and the radius, so one number meant a different speed in every bank -- ADR-564's unit defect again. Now a true advection, `position - velocity x time`, which satisfies `detail(p + v*dt, t + dt) == detail(p, t)` EXACTLY, and that identity is what §15 means by moving through space rather than shimmering. And §24's response curve, whose three terms are each the identity at their default -- which finally makes `Contrast` reach the field: the row has existed since the kind did, all three styles set it, and nothing has read it since ADR-563. Both needed lanes, and `grep -o 'mediaLane(s, [0-9]*u)'` found lanes 2, 6 and 8 have ONE reader in the march and it is the arm a fog bank never takes -- ADR-562 §9's rule used before writing instead of after breaking. §17 needs `EffectResolve::pack` to be handed the resolved flow: a shared-hook signature change, flagged rather than made) | Accepted |
| [572](ADR-572-the-flow-reaches-the-packer-and-sets-a-direction.md) | The flow reaches the packer, and it sets a direction (§17. `EffectResolve::pack` gains the resolved flow -- every implementor changed in one commit, no overload beside the old hook, because ADR-441 takes no shims and a half-converted hook is the state where two versions disagree about which is authoritative. The rejected alternative was sampling the wind inside `fog.wgsl`, which the march reaches for free: refused because that file declares NO BINDINGS on purpose, which is what lets the parity harness compile it alone against its CPU twin. **The flow sets the DIRECTION and never the speed**, and that is a units decision -- the wind publishes a dimensionless strength and a vortex metres per second, and this codebase has made that mistake four times. A direction is unit-free, so one control cannot mean two things. Tested: drift magnitude is exactly `driftSpeed` across flow magnitudes 0.01 to 40, and a Normalised 0.3 and a MetresPerSecond 22 give the same answer. Break: reading the magnitude gives 4.51 and 3.02 where the artist asked for 6.0) | Accepted |
| [573](ADR-573-a-control-nobody-can-reach.md) | An effect with no control is the same defect as a control with no effect (§27, and §19/§25/§26 assessed. §19 is satisfied and was checked rather than believed -- the march clamps to scene depth and the composite upsamples depth-aware. §27 was not: `volumeLocalLights` gates the clustered lights' contribution to the fog, reaches the shader, defaults to 0, and **had no registered parameter** -- no panel row, no automation, no modulation, editable only by hand. Twelve shipped scenes set it, every Glowmere among them; `volumeMaxDistance` was in the same state with 32. This is the MIRROR of the four dead knobs this branch found: ADR-421's "a control that does nothing teaches an artist the system is broken" has an equally expensive converse -- a capability with no control teaches them it cannot be done. Both registered and drawn. The test moves the parameter and checks the Environment follows, because a registration test passes for a parameter that is registered, drawn, keyframed and dropped; two versions of it failed against working code because the panel writes BASE and `applyParameters` reads FINAL, and `resetFinals()` is the seam) | Accepted |
| [574](ADR-574-the-reason-was-false-about-its-own-four-lines.md) | The reason for leaving it unreachable was false about its own four lines (a correction to ADR-573, which flagged `fogHeightAmount` for its owner because its omission was "documented as deliberate". ADR-385: a stated reason is not evidence, and escalating a question a grep settles spends someone's attention. Measured: the comment says the mist-layer share and the styled hemisphere "are copied, not picked" -- and `styledSkyAmbient` and `styledGroundAmbient` are PICKED FROM REGISTERED PARAMETERS two lines below it. The surviving half is circular: nothing animated it because with no parameter nothing could. The coupling is live -- shader reads it, renderer uploads it, two GPU cases exercise it, 9 of 90 shipped scenes set it by hand -- and nothing duplicates it. So exposing it is a correction, not a decision. The save writes the parameter's base now: a control you can move and cannot keep is not a control. **ADR-421's converse stated as its own rule: a capability with no control teaches an artist that the system cannot do it, and nobody files a bug about a feature they do not know exists**) | Accepted |
| [575](ADR-575-where-the-ground-is-and-where-the-glow-is.md) | §18 is one route working and three with no GPU resource; §26 is one term (§18 investigated as the brief asks: of its five named routes, **scene depth and world height work**, `FieldKind::SdfDistance` is a declared kind that returns **0 on both CPU and GPU** so a scene naming it gets no fog at all, and terrain heightmap and scene collision need a GPU resource that **does not exist** -- terrain height lives only in a CPU `TerrainQuery`. Recommendation, needing a decision because it crosses systems: bake a terrain height texture ONCE (the map is static, so it is the opposite of the per-frame CPU traversal §18 forbids) and bind it to the volume pass; it would serve three of the five routes and the water and grass that already ask a CPU query. §26's missing fourth term -- emission's height influence -- built as a per-kind ARM reusing the density's vertical profile. §25's height and distance colour named, costed and deliberately not built. **The parity harness had no slot for the new GPU term and passed a deliberate break; it reads two vec4 per sample now**) | Accepted |
| [576](ADR-576-a-declared-kind-may-not-answer-with-silence.md) | A declared kind may not answer with silence (a defect split out of ADR-575's §18 investigation, which had bundled a correction with a feature. `FieldKind::SdfDistance` is declared, documented, selectable and evaluates to **0 on the CPU and 0 on the GPU**, so a density field naming it gives the march `volumeDensity * 0` -- **no fog at all**, with nothing in the log, the panel or the file to read. REFUSED at load now, with a message naming the kind, the field, what would have happened and the alternatives. Not removed: `Scene::sdfs` exists and `SdfRenderer` draws it, so the seam is real rather than vestigial; 0 tracked files name it so removal buys no migration; and refusing is one branch where removal touches a name round trip, an exhaustive test loop and the GPU fallback. **A test that CERTIFIED the silent zero is now a test of the refusal**, and an existing assertion that was passing for the wrong reason -- an empty `reference` -- is fixed. The third face of the family: a control that does nothing blames the system, a capability with no control blames the feature, **an option that evaluates to nothing blames the author**) | Accepted |
| [577](ADR-577-the-grain-is-measured-and-the-tier-is-a-trade.md) | §29's mandatory artefact is already absent, and the tier that reintroduces it is a trade (Phase H. §29 forbids animated grain and the march produces it by construction -- `stepJitter` hashes the frame nonce and nothing accumulates. Measured instead of assumed: at the shipped 32 steps the temporal SD is **1e-4 of the medium's brightness**, a fortieth of one level in 255, so **the least expensive technique that produces stable results is the one already in the tree** -- no history buffer, no reprojection. And `spatialRatio` is **1.000** at 32 steps and above, so the speckle in this branch's own dense arms was NOT the jitter but the medium undersampled, which I had attributed to jitter without measuring. At **16 steps** with tau 12 the grain reaches **1%** and is visible -- and `QualityTier::Preview` sets `volumeStepScale = 0.5`, so Preview marches 16. The header's claim that the lever "buys almost no time" does not generalise: measured on a placed medium with the engine's own interleaved A/B, **+4.26 ms of 15.14, 28%**, all three pairs positive. A real trade, the renderer's to resolve. §32 is already done (ADR-562 §4 + ADR-566's bound); §31 is scoped by ADR-562's own comment and not built. **The first measurement was of camera motion and a control caught it**) | Accepted |
| [578](ADR-578-the-count-that-was-missing-found-a-vortex-in-every-arm.md) | The one number §39 asked for that nothing reported, and what it found in an hour (Phase I. §39's list -- GPU ms per pass, CPU ms, resolution, step count, a machine-readable record, an interleaved A/B -- is all present and good except **active volume count**, which is the item ADR-560's headline defect was about. `VolumeStats` gains `media`, `mediaDropped` and ADR-570's `shadowSteps`, with a test, because instrumentation without a guard is the same family as every dead knob here. **The first arm rendered after it reached the log printed `media=2` where one had been placed**: the Tree of Life SCENE declares a Cosmic Vortex and a project's effect list MERGES with the scene's rather than replacing it, so every fog arm this branch ever rendered had a nebula in it and three generators' comments said otherwise. The comparisons stand -- the vortex was constant across every arm in a set, which is the one case where an artefact is harmless -- but the descriptions did not, and they are corrected where they live. Memory is the one §39 item still missing) | Accepted |
| [579](ADR-579-a-preset-that-continues-is-not-a-starting-point.md) | A preset that continues is not a starting point, and a heading drawn twice is a defect (§36/§37. Enumerating the panel as an artist meets it found a section heading drawn TWICE on one page -- `.sec` marks a section start, so two rows with one name draw it twice with unrelated controls under each. It had been fixed once before, so it is a CHECK now: a registry guard over every kind and page, which caught two on its first run including one in the VORTEX that nobody had noticed. §37's ten presets replace the three -- but the presets were not starting points: `applyStyle` resets the Vortex struct under a comment explaining exactly why, and **that argument covers half the parameters** -- everything in `values` since ADR-566 survived, so a preset applied after an artist set Shape to Box got a box. Reset from the SCHEMA's defaults so a row added tomorrow is covered. **A hidden instrument tuned the ten and found two wrong** -- Horror Fog reading EXACTLY ZERO at its own centre -- because ADR-571's threshold subtracts after the vertical profile. And the case that caught it was asking the wrong question three ways, including a 2% tolerance that the density curve legitimately breaks: **a tolerance is a proxy for a property; when a feature lands that the proxy forbids, assert the property**) | Accepted |
| [580](ADR-580-a-tornado-is-a-vertical-phenomenon-and-the-engine-already-has-the-solver.md) | A tornado is a vertical phenomenon, the shipped shot has no vortex in it, and the engine already has a fluid solver (Phase 1 of the Tornado brief. The shipped Tree of Life hero frame, rendered here, contains no funnel, no eye and no silhouette at all -- the teal wash IS the vortex, seen edge on. ADR-461 concluded the hero shot cannot hold a cyclone because a cyclone's features are horizontal and compressed by `sin(8.80 deg)` = 0.15; a tornado's are vertical and scaled by `cos` = 0.988, a ratio of **6.46**, and a 600 m column at 1000 m fills 92% of the 36 deg frame with 15 deg of clear frame either side of the island. The existing Vortex is a hurricane -- its whole macro structure is a function of `(rr, angle)` -- so §2's 'do not just rename it' is not a temptation, the implementation computes the wrong thing. The architecture is Burgers-Rott (`u_r = -ar/2`, `u_z = az`, `u_theta = (G/2 pi r)(1 - exp(-ar^2/4nu))`, divergence-free because `-a + a = 0`) on a bent guide curve, with density as four analytic terms -- condensation shell, radius curve, debris skirt, helical striations -- and noise advected in closed form at zero cost. Vorticity confinement is REFUSED in the analytic tier because there is no numerical diffusion to confine and a dead knob is ADR-421's defect. The find that decides it: ADR-032's `spatial::GridField` + `shaders/simulate.wgsl` is already a GPU semi-Lagrangian solver with advection, injection, Jacobi diffusion and dissipation, deterministic in renderTime, and `volume.wgsl` already samples grids -- §24's hybrid is a configuration, not a build, needing one new `FieldKind`. Its trap, also measured: `kCatchUpSteps = 240` means `--range 6:6` spins a 60 Hz grid up 240 of 360 steps, so the silhouette may not live in the grid -- which is what §45 demands anyway. Also: `main` at 4d2a224e does not compile, `sizeof(AtmosphericFrame)` 2400 against an assert of 1640, because `frameDiffers` reads none of the Cosmic Ocean) Phases 7-8 appended as §10: integrated onto ADR-562's medium slots, where three defects surfaced -- `MediumSlot::kind` had ZERO readers a day after the foundation shipped; this branch's packer wrote a colour into lane 15, the tag's lane, which had the tag landed first would have presented as **a tornado intermittently rendering as a comet**; and the §68 wind lean was vortex-specific, a silent no-op for any other kind, named by `flow-reaches` within a minute. With the tag not yet live the march read a tornado's lanes as a vortex -- height as radius, taper as extinction per metre -- and it was predicted BY NAME in advance and still cost six renders, because knowing a hazard by name does not make it recognisable in the moment. What broke it is the transferable part: **identity across unrelated inputs says the input is not reaching the computation at all**, and **the first question about an unresponsive control is not 'what value' but 'is this code running'**. Hero replaced in both halves together, census by parsing (2 files, not the 8 a grep returns), five routes retargeted rather than deleted, two tests moved from naming a KIND to following the ROLE. Phase 8, before any value moved: the vortex at **6.16 ms** against the tornado at **1.31 ms** on one tree, interleaved, minima -- **4.7x**, and SMALLER than the lab's 6.5x would have implied, which is what refusing to quote the lab ratio protected against. The ray interval's -24% is recorded as an association, not an attribution. The tornado is roughly frame-neutral against having no placed medium at all, where ADR-374 called its predecessor the most expensive term in the scene. And the seam §32 warned of, named rather than tuned past: the debris skirt asserts a ground contact that an island in open space does not have) | Accepted |
| [581](ADR-581-a-solver-with-no-users-renders-the-same-frame-at-six-seconds-and-thirty.md) | A solver with no users, and it renders the same frame at six seconds and at thirty (ADR-032's GPU semi-Lagrangian grid -- injection, advection, Jacobi diffusion, dissipation, a CPU reference and unit tests -- has **zero authored users**: 0 files with a non-empty `grids` array, and all 183 `"kind": "grid"` hits are procedural point *distributions*, an unrelated subsystem sharing the word. Counted by parsing, not grepping, which is the third time tonight that distinction mattered. Writing the first grid scene found the next collision: the scene-level `"fields"` key is `entity::FieldDesc` (ADR-097), while the `spatial::FieldSpec` a grid resolves against is a node of `kind: "field"` -- the first attempt parsed, loaded, logged three fields and simulated nothing. Measured: `--range 6:6` against the last frame of `--range 0:6` differs in **99.5% of pixels**, mean luminance 113.70 against 187.21; and `--range 6:6` and `--range 30:30` are **byte-identical**, hash `5496d004173787ee`, because both reset, skip to `target - 240` and run the same 240 steps from the same zero state. `kCatchUpSteps = 240` grants 67% of the sim at t=6, 13% at t=30 and 4% at t=90. This does NOT violate ADR-360 -- two renders of the same range give 0 differing pixels -- what it breaks is the unwritten promise that `--range t:t` shows the scene at t, which is ADR-521's defect one subsystem over. Recorded, not fixed, with the four options and their costs) | Accepted |
